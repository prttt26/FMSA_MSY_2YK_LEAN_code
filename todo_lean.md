# Lean Proof Tasks: FMSA Yukawa Mixture Theory

This file is the **status index** for all Lean 4 + Mathlib proofs in `LeanCode/`.

> **⚠ Directory restructure (2026-08-06).** The layering was cleaned up (see `CONVENTIONS.md`):
> `Analysis/` + `FMSAPoly/` are the two bases; then `HardSphere/ → HSMixture/ → YukawaOZ/ →
> YukawaOZMix/`, with sinks `FreeEnergy/` and `Closures/`. Concretely: **`YukawaDCF/` was split and
> renamed** into `YukawaOZ/` (single-component + generic-scalar base, 11 files) and `YukawaOZMix/`
> (N-component mixture, 24 files) — "DCF" dropped since the layer also carries RDF/`g`/`h`/closures.
> **New `Closures/`** holds the non-FMSA closures (`FirstOrderClosures` split into
> `ClosureExpansions` + `DPClosureMap`). Reverse-reference back-edges fixed by moving `AppendixWDet`/
> `ArcAmplitudeBound` **down** to `HSMixture/` and `MixtureRowSum` **up** to `YukawaOZMix/`. Group
> names are directory-independent, so the tables below are unchanged; only *file paths* in prose that
> say `YukawaDCF/…` should be read as `YukawaOZ*/…`.

Detailed proof records (statements, proof sketches, pitfalls, Lean API notes) are in:

- [proof_notes_hard_sphere.md](proof_notes_hard_sphere.md) — Groups 2, 3, OZ (pure HS foundations)
- [proof_notes_baxter.md](proof_notes_baxter.md) — Group BAXTER (Baxter Q-factor + Wiener–Hopf route to the PY closed form)
- [proof_notes_matrix_q0.md](proof_notes_matrix_q0.md) — Group M (multi-component HS Baxter Q̂₀ matrix identity, rank-2 det reduction, det-positivity monotonicity M.5–M.8)
- [proof_notes_yukawa_dcf.md](proof_notes_yukawa_dcf.md) — Groups 1, 4, GAP, C, 5 (Yukawa DCF derivation)
- [proof_notes_breakpoints.md](proof_notes_breakpoints.md) — Group IB (inner-core mediated breakpoints)
- [proof_notes_yukawa_wh.md](proof_notes_yukawa_wh.md) — Group Y1 (first-order Yukawa RDF via Wiener–Hopf; the general foundation)
- [proof_notes_mixture_rdf.md](proof_notes_mixture_rdf.md) — Groups MML, MZERO (the N=2 mixture inner **RDF** `h₁` Mittag-Leffler HS-pole series + `det(Q̂₀)` zero family)
- [proof_notes_mixture_dcf.md](proof_notes_mixture_dcf.md) — Groups MRS, MPOLY (the N=2 mixture inner **DCF** as a finite closed form: real-space Baxter route + polynomial coefficients)
- [proof_notes_failures.md](proof_notes_failures.md) — Groups chsY, P, GA (method-failure analysis)
- [proof_notes_closures.md](proof_notes_closures.md) — Groups PYE, HNCB (first-order PY / HNCB closures of the YK-tail FMSA-DP construction)
- [proof_notes_free_energy.md](proof_notes_free_energy.md) — Groups F, FW (free energy integrals; White-Bear FMT/BMCSL)

**Source markers:**
- **[chsY]** — `pdf/FMSA_chsY.pdf` (analytical multi-species MSA solution)
- **[LN]** — `pdf/lecture_notes_OZ_Yukawa_poly.pdf`

---

## Open / Unfinished Items

*One line per item; details live in the linked `proof_notes_*.md`.*

### Sorries — actual `sorry` proof terms in source files

*(none)*

### Axioms — `axiom` declarations assumed without proof

**Math axioms (7)** — general-analysis facts, not physics-closure assumptions
(registry [MATH_AXIOMS.md](MATH_AXIOMS.md)). They split cleanly by Lean home, and the split is
**mechanically checkable** — both axiom tables on this page must stay in sync with

```
grep -rn "^axiom [a-zA-Z_]" LeanCode/ --include=*.lean | sed 's|LeanCode/\([A-Za-z]*\)/.*|\1|' | sort | uniq -c
```

⚠ **That grep spans math *and* physics axioms — it is the count for BOTH tables, not this one alone.**
As of 2026-07-30 it returns **10 raw hits = 2 + 8**:
2 false positives (prose lines that happen to begin `axiom `, in `PYOZ_GHS.lean:278` and
`HalfDiskArgumentPrinciple.lean:43`) and 8 real axioms — **7 math + 1 physics**
(`HSMixture/`, `pyhs_mixture_no_spinodal`, in the *Physics axioms* table below). So the bucket
counts are **`Analysis` 7 + `HardSphere` 0 + `HSMixture` 1** — the 1 HSMixture being
`pyhs_mixture_no_spinodal` (physics).  The 7th math axiom (Analysis) is
`matRadialShell_bounded_injective` (`Analysis/MatrixRadialWienerHopf.lean`, MML.8 value route,
committed + **abstracted to `Analysis/` in raw-integral form 2026-07-30**: the matrix analog of the
scalar `radialShell_bounded_injective`, arbitrary matrix kernel; `MixtureRDFUniqueness.lean` re-exposes
it as a theorem via definitional bridges → `matOzStar_unique`). The 7 math axioms:

* **`Analysis/` (6)** — general-purpose, project-independent Mathlib vocabulary: MA.1 (×2), MA.7,
  MA.13 (`wiener_causal_resolvent`), `radialShell_bounded_injective` (OZ.10, **abstracted to an arbitrary kernel `C` and
  migrated here 2026-07-19**), and `zeroFree_lowerHalfPlane_of_homotopy` (**MA.14, abstracted +
  migrated here 2026-07-21** when `baxter_no_open_lhp_pole_core` was retired — see below).
* **`HardSphere/` (0)** — ✅ **`HardSphere/` now contains no domain-referencing axiom at all.** The
  last one, `MA.14 baxter_no_open_lhp_pole_core`, was **RETIRED to a theorem 2026-07-21** via the
  `η`-homotopy of the open-lower-half-plane zero count (`BaxterLowerHalfPlane.lean`), leaving only the
  abstract `zeroFree_lowerHalfPlane_of_homotopy` in `Analysis/`. The whole OZ exterior-regularity
  cluster (6h, 6j, 7a, 7b) was already axiom-free.

⚠ **Count history.** This header read `9` while the table listed `10` rows — the 7a/7b split of
`ozExterior_smooth_deriv` was never added to it. Reconciled 2026-07-19: `10 → 9` by retiring 6h, then
**`9 → 8` by retiring 7a `ozExterior_smooth_repr`, then `8 → 7` by retiring 7b
`ozExterior_deriv_integrable`** (`OZFIX.25`: both proved via (★DIFF), the differentiated renewal
equation; 7b's three summands via compact support, the proven `ψ ∈ L¹`, and Mathlib's Young). Then
**`7 → 6` by retiring MA.8 `contourIntegral_eq_of_homotopic_loops` 2026-07-24** — its "Mathlib lacks
homotopy/winding machinery" gap **closed** upstream (Mathlib `v4.31.0` gained the Poincaré lemma for
`1`-forms, `CurveIntegral/Poincare.lean`); the content is now the axiom-free bundled `C²` theorems in
`Analysis/ContourHomotopy.lean`. **Re-derive the count from the `grep` above, not from this
header.**

The **exterior OZ integrability cluster (6g/6h/6j) is now entirely axiom-free**: 6g was proved from
the start, clause 6j (`ozExterior_conv_sin_integrable`) was DERIVED from 6h (2026-07-19), and 6h
(`ozExterior_triple_shell_sin_integrable`) was **RETIRED to a theorem 2026-07-19** (`OZFIX.24`) — it
had been axiomatized on an **effort** argument ("formalizing the triple `lintegral` chain is
substantial"), not a gap argument, which put it in tension with Group MA admissibility rule (c).
Proving it — rather than abstracting a side condition — was the right call, as with MA.10/11/12.
**Triage heuristic for the remaining 6: an axiom whose docstring justifies itself by effort *and*
supplies its own proof sketch is a retirement target; one that cites a genuine Mathlib gap (no Wiener
algebra, no winding number, no P.V. API, no L∞ Plancherel) is not.** ⚠ **Gap justifications go stale —
re-verify against the *current* Mathlib, not the docstring's date.** Recon 2026-07-24 (`Explore` sweep
of `.lake/packages/mathlib`, `v4.31.0`) **re-confirmed the gaps for MA.1, MA.7, MA.13, MA.14, MA.15
are still real** (no winding number, argument principle, Rouché, residue theorem, Hurwitz, Wiener
algebra, or P.V./Hilbert transform), but found **MA.8's gap had closed** (new `CurveIntegral/Poincare.lean`)
⇒ MA.8 retired. Cauchy higher-derivative formulas are also present now (no MA-axiom uses them);
`meromorphicOrderAt`/`divisor` exist but are **not** linked to any contour integral (so the
argument-principle gap that MA.14 rests on stands).

| Task | Axiom | File | Meaning / proof path |
|------|-------|------|----------------------|
| MA.1 | `circleIntegral_eq_sum_of_small_circles` | `Analysis/ContourDeformation.lean` | Keyhole/slit contour deformation: a big circle's integral = the sum over finitely many disjoint off-center hole circles. Mathlib has no winding number / residue theorem. Powers the genuine theorem `circleIntegral_eq_sum_two_pi_I_mul_of_simple_poles` and MA.11. See [MATH_AXIOMS.md](MATH_AXIOMS.md). |
| MA.1 | `halfDiskBoundary_eq_sum_of_small_circles` | `Analysis/ContourDeformation.lean` | Same deformation for the **upper half-disk** boundary (`[−R,R]` + arc) — the shape Fourier inversion closes; provably not reducible to the circle axiom. Powers `halfDiskArgumentPrinciple_sum`. See [MATH_AXIOMS.md](MATH_AXIOMS.md). |
| MA.7 | `sokhotski_plemelj_upper` | `Analysis/SokhotskiPlemelj.lean` | Sokhotski–Plemelj upper boundary value, integrated form (P.V. existence taken as a hypothesis, so no distribution theory). Mathlib has no Hilbert-transform / P.V. API. **No consumer yet.** See [MATH_AXIOMS.md](MATH_AXIOMS.md). |
| MA.13 | `wiener_causal_resolvent` | `Analysis/WienerRenewal.lean` | **Wiener's `1/f` theorem** for the causal `L¹` convolution algebra `ℂ·δ ⊕ L¹(0,∞)` (maximal ideal space = closed RHP, Gelfand transform = Laplace transform): symbol `1−q̂` nonvanishing there ⇒ causal resolvent `R ∈ L¹`, `R = q + q ⋆ R`. **Reshaped 2026-07-21** from the former bespoke `volterra_renewal_tendsto_zero`, which is **now a THEOREM** proved from this: resolvent formula (Fubini) + `MA.10` uniqueness + moving-window `L¹` tail (decay) + Young (integrability). Irreducible: `‖q‖₁ > 1` in the physical regime so the Neumann series diverges; Mathlib has no Wiener algebra / Laplace transform / Paley–Wiener / Hardy spaces (re-verified 2026-07-21). See [MATH_AXIOMS.md](MATH_AXIOMS.md) MA.13 and `proof_notes_pole.md` POLE.11 decay half. |
| MA.14 | `zeroFree_lowerHalfPlane_of_homotopy` | `Analysis/ZeroCountHomotopy.lean` | **Homotopy invariance of the open-lower-half-plane zero count** (argument principle / Hurwitz): a jointly-continuous family of entire functions `H t`, with all closed-LHP zeros inside `‖z‖<R` and never vanishing on the real axis, keeps its open-LHP zero-freeness from `t=a` to `t=b`. Fully general (no Baxter/Yukawa), abstracted here 2026-07-21. Mathlib has **neither Rouché nor the parametric argument principle**. **Retired the former domain axiom `baxter_no_open_lhp_pole_core`**, which is now a THEOREM (`BaxterHermiteBiehler.lean`) proved by the `η`-homotopy `Qhat_complex_ne_one_of_im_neg` (`BaxterLowerHalfPlane.lean`). See [MATH_AXIOMS.md](MATH_AXIOMS.md) and `proof_notes_pole.md` POLE.11-general. |
| MA.15 | `radialShell_bounded_injective` | `Analysis/RadialWienerHopf.lean` | **L∞ Wiener–Hopf injectivity** for a radial shell operator with an *arbitrary* kernel `C` supported in `[0,σ]` and coercive symbol `1−ρĈ ≥ ε>0`. **Abstracted + migrated to `Analysis/` 2026-07-19** — the concrete `oz_linear_op_bounded_injective` is now a THEOREM instantiating it at `C := c_HS` (support from `c_HS_outer`; `radial_fourier`/`oz_linear_op` unfold definitionally). The L∞ analog of the *proved* (L²) MA.12: Plancherel coercivity does not survive to L∞, so the space — not the physics — is why this one is assumed. ⚠ `hCsupp` is load-bearing (symbol integrates `(0,∞)`, the operator only `[0,σ]`; false without it). Consumer: `OZ.10`. See [MATH_AXIOMS.md](MATH_AXIOMS.md) MA.15. |

**Physics axioms (1)** — one **pre-placed, consumer-less** axiom for the *mixture* track.
✅ **The single-component track is at ZERO physics axioms**: `pyhs_no_spinodal` (OZ.20), was retired **2026-07-19**. Nothing imports the mixture axiom, so
it is inert for every scalar result (`#print axioms g0_HS_contact_value` does not list it).

| Task | Axiom | File | Meaning / why axiomatized |
|------|-------|------|---------------------------|
| MRS.0 | `pyhs_mixture_no_spinodal` | `HSMixture/MixtureNoSpinodal.lean` | **The only physics axiom; pre-placed, NO CONSUMER YET** (mixture track, Group MRS / `MixtureInnerDCF`; same status as MA.8). `det Q0_mat_c_phys(ik) ≠ 0` for real `k≠0` — the N-component form of the now-PROVEN scalar `pyhs_no_spinodal`. `k≠0` required (`q0_entry_c` divides by `s²`/`s³`). **Must use PHYSICAL coefficients**: with free `Qp`/`Qpp` the claim is FALSE (M.3 counterexample in `MatrixQ0.lean`: `n=2`, equal diameters, `Qpp≡0`, `Qp≈−13.59` ⇒ `det=0`). Numerics: two independent determinant routes agree to `2e-15` (300 mixtures × 4 `k`, `N=1..4`); `min_k\|det\| ≥ 0.517` (800 mixtures, `η≤0.55`) and `≥ 0.375` (harsher 250-trial, `η≤0.62`, ratio ≤20). **Not provable by the scalar `k⁶` route** — `N` species give ~`(N²+3N)/2` exponentials; term-wise dominance fails 300/300 for `N≥2` (phase artifact) and `ρ(M)<1` fails even at `N=1` (77%) ⇒ needs a winding argument, MA.14-class. ✅ **`n=1` bridge DONE 2026-07-19** (`MixtureNoSpinodalN1.lean`, `MRS.0b`): `pyhs_mixture_no_spinodal_n1` = the axiom's statement at `Fin 1`, proved **without** the axiom from the scalar `pyhs_no_spinodal`; `#print axioms` = **standard three only** ⇒ at `n=1` the axiom is not merely consistent but **redundant**. Chain: moments (`etaMix_n1` gives exactly the scalar `heta_def`) → coefficients (`Q0phys_n1`/`Qppphys_n1` = `q_prime_py`/`q_doubleprime_py`) → kernels (`qhat_complex_eq_mixture_kernel`) → `Matrix.det_fin_one`. This is the ONLY mechanical soundness test a consumer-less axiom admits, and it passed with no statement bug. **`MRS.0` = the axiom + its `0b` validation only**; the `HSMixture/` library reorganization that placing this axiom triggered is tracked separately as **`MRS.9`** (`proof_notes_mixture_rdf.md`). See [MATH_AXIOMS.md](MATH_AXIOMS.md). |


### Numerically verified — not proved in Lean

*(The three axioms that carry a numerical witness. Standard-math axioms — the two contour-deformation
MA.1's, S–P MA.7, homotopy-Cauchy MA.8, L∞ Wiener–Hopf MA.15 — are not "numerical" and are omitted.)*

| Axiom (task) | Claim | File | Numerical witness |
|------|-------|------|-------------------|
| `wiener_causal_resolvent` (MA.13) | Wiener `1/f`: symbol `1−q̂ ≠ 0` on the closed RHP ⇒ causal resolvent `R ∈ L¹`, `R = q + q⋆R` | `Analysis/WienerRenewal.lean` | `verify_ma13_route.py`: spectral radius `=‖q‖₁>1` (Neumann diverges); simulated renewal decay rate `= Im k₁` (Ornstein–Zernike), `ψ ∈ L¹` |
| `zeroFree_lowerHalfPlane_of_homotopy` (MA.14) | Argument-principle/Hurwitz: open-LHP zero count is homotopy-invariant | `Analysis/ZeroCountHomotopy.lean` | `verify_ma14_route.py`: deflated zero count `= 0` (robust in `R,ε,η`); two elementary routes refuted at `k=−it₀` |
| `pyhs_mixture_no_spinodal` (MRS.0) | `det Q0_mat_c_phys(ik) ≠ 0` for real `k≠0`, N-component | `HSMixture/MixtureNoSpinodal.lean` | 300 mixtures × 4 `k` × `N=1..4`, two det routes agree `2e-15`; `min_k\|det\| ≥ 0.375`; `n=1` bridge PROVED redundant |

### Conditional-theorem hypotheses — ✅ all resolved (ledger)

*Theorems that were proved carrying one explicit open hypothesis (`theorem T (h : …) : P`, not a
bare `axiom`). **As of 2026-07-17 the live table is empty — every tracked hypothesis has been
discharged, closed, or falsified.** Kept below as a disposition ledger for traceability; do not
re-open. Genuine axioms live in the Axioms table above.*


### Open tasks

**Single-component / HS-Baxter track: essentially closed as of 2026-07-21.** The former big open
items are done — see *Recently closed* below. The only genuinely-open item is the **mixture** RDF
collapse (a separate `N`-component track).

| Task | Title | Depends on | Notes |
|------|-------|-----------|-------|
| `PYE.4` *(closures)* | Abstract equivalence `DP-map[g_HS·(−βu)] = OZ+PY first order` — the **function-space lift** | PYE.1–3, PYE.5–7 ✓ | ◑ **substantially reduced 2026-08-03**: the lift is now built except for ONE named classical input. Proved: half-line uniform + **L¹** tail-fit density (`exists_yukawa_tail_fit_halfLine`/`_L1`), the transform stage `‖Û₁(k)‖ ≤ ‖Ψ‖_{L¹}` uniformly in k (`norm_transform_le_L1`), and the reduction `tailFitWHConvergent_of_L1_tendsto` + capstone `exists_tailFit_whConvergent` — so `TailFitWHConvergent` is **derived**, not assumed. **Remaining = `WHProjectionL1`**: `Q̂₀⁻¹`'s real-space kernel is L¹ ⇒ the projection is L¹-bounded — Wiener's 1/f for the causal algebra, i.e. **MA.13/MA.15-class** (kept as a hypothesis, NOT axiomatized; non-vacuity certified by `example`). ⚠ two traps recorded: the **amplitude route is a dead end** (`closure ↦ amplitudes` unbounded — §4(b)'s degenerate pairs), and a **sup-norm-in-k** hypothesis would be *wrong* (causal projection is Riesz-type). → [closures](proof_notes_closures.md) PYE.4 |
| `HNCB.2–3` *(closures)* | fixed-rate first-order HNCB = closure expansion + finite `(I−L)⁻¹` + DP map | **HNCB.1 ✓** (2026-08-03), PYE.1/PYE.2 ✓ | **HNCB.1 is CLOSED** — `exp`-form half + contractive pivot `c₁ = −βu + B·h₁` + the `B ∈ [0,1)` contraction bound, all axiom-clean. **Remaining in the group: HNCB.2 (affine pull-back `K̃' = a + L·K̃`) and HNCB.3 (`K̃* = (I−L)⁻¹a`)**, both reusing PYE.2's linearity; HNCB.4 is a boundary marker, not work. → [closures](proof_notes_closures.md) Group HNCB (Inventory section) |
| `MML.8` *(mixture, RDF-only)* | Inner-**RDF** Mittag-Leffler assembly/collapse | MML.4–12 ✓, Y1.3 ✓, MZERO.1/5 ✓ | ⚠ DCF reading **REFUTED** by (★) → superseded by Group MRS; survives as the RDF `h₁` assembly. All deps done; **remaining = the REAL-SPACE identification** of the summed ML series with the inner-core value on the annulus `(max(σ₀/2,λ₀₁), R₀₁)` (matrix analogue of `OZFIX.12`/`OZFIX.22`) — the only open item in Groups MML+MZERO. `Ĥ₁=S₀·Ĉ₁·S₀` puts every RDF HS pole in the `S₀=(I−Ĉ₀)⁻¹` factors (no Yukawa data) ⇒ residual input is a hard-sphere statement; Yukawa half closed by Group MRS. → [mixture_rdf](proof_notes_mixture_rdf.md) MML.8–12 |



---

## Task Status

### Group MA — MathAxioms: General-Purpose Math Axioms & Missing-Mathlib Machinery *(analysis)*

*Cross-cutting infrastructure group (2026-07-15): surveys the project for missing-Mathlib-machinery
gaps, pre-places admissible axioms (named classical theorems only, numerically pre-checked,
narrowest form — see the admissibility discipline in [MATH_AXIOMS.md](MATH_AXIOMS.md)), and proves
what's provable. Lean home: `LeanCode/Analysis/` (project-independent, no `FMSA.*` def usage).
Registry + task records: [MATH_AXIOMS.md](MATH_AXIOMS.md). Axiom declarations are cross-listed in
the Axioms table above.*

| Task | Title | Status | Lean file |
|------|-------|--------|-----------|
| MA.1 | Founding pieces (re-filed from the `OZFIX.10` Route B effort): circular + half-disk contour-deformation axioms, their derived residue-sum theorems, and the **axiom-free** `jordan_lemma_arc_bound` | ✓ DONE | `Analysis/ContourDeformation.lean`, `Analysis/JordanLemma.lean` |
| MA.2 | **Mittag-Leffler expansion** (simple poles + bounded on expanding circles ⇒ circle-grouped pole expansion) | ✓ DONE (**proved from the circle-deformation axiom; its own axiom RETIRED**) — ~430 lines. First stated with an *ordered*-partial-sum conclusion, which is **false** (pole-pair-splitting counterexample); fixed to the classical grouped form. See [MATH_AXIOMS.md](MATH_AXIOMS.md) bug 2. | `Analysis/MittagLeffler.lean` |
| MA.3 | **One-pole Fourier kernel** `∫_{-R}^{R} e^{ixr}/(x-k₀)dx → 2πi·e^{ik₀r}` (`Im k₀>0`, `r>0`) — genuine theorem, `#print axioms` = halfDisk axiom + standard three; with MA.2 this decomposes `OZFIX.10`'s blocked monolithic arc estimate | ✓ DONE (no new axiom) | `Analysis/MittagLeffler.lean` |
| MA.4 | **Van der Corput first-derivative test** (`\|φ'\|≥λ`, `φ'` monotone ⇒ `‖∫e^{iφ}ψ‖ ≤ (3/λ)(‖ψ(b)‖+∫‖ψ'‖)`) | ✓ DONE (**proved; axiom RETIRED**) — ~370 lines. The textbook `C¹` form is **true but unprovable** here (needs Stieltjes IBP / 2nd MVT, neither in Mathlib); restated at `C²` (no consumers ⇒ free). See [MATH_AXIOMS.md](MATH_AXIOMS.md) bug 3. | `Analysis/VanDerCorput.lean` |
| MA.5 | **Jensen counting bound** (no poles + finite divisor support ⇒ `circleAverage(log‖f‖) ≤ M·logR+C`) | ✓ DONE (**proved, no axiom**) — ~90 lines from Mathlib's Jensen. First stated over the *literal* zero set, which is **false** under junk values; fixed to divisor support. Discharges `MZERO.11`'s `hJensen`. See [MATH_AXIOMS.md](MATH_AXIOMS.md) bug 1. | `Analysis/JensenCounting.lean` |
| MA.6 | Relocate misfiled pure math `HardSphere/→Analysis/`: `ResidueAtSimplePole.lean`, `BanachPoleFamily.lean` (+5 consumer imports; namespaces kept) | ✓ DONE (`lake build` green) | `Analysis/ResidueAtSimplePole.lean`, `Analysis/BanachPoleFamily.lean` |
| MA.7 | **Sokhotski–Plemelj boundary values** (upper, integrated form; P.V. existence as hypothesis) — user-requested pre-placement, numerically pre-checked (3 test functions, linear-in-ε convergence) | ✓ DONE | `Analysis/SokhotskiPlemelj.lean` |
| MA.8 | **Homotopy-invariance Cauchy** (free-homotopy-through-loops of loops in open `U`, `f` holomorphic ⇒ equal contour integrals) — was pre-placed as the intended future primitive for the contour-deformation axioms MA.1 | ✓ **RETIRED to a THEOREM 2026-07-24 — axiom removed** (gap closed upstream). Mathlib `v4.31.0` gained the Poincaré lemma for `1`-forms (`ContinuousMap.Homotopy.curveIntegral_add_curveIntegral_eq_of_hasFDerivWithinAt`, `CurveIntegral/Poincare.lean`), so the "Mathlib lacks homotopy/winding machinery" justification is stale. Proved axiom-free in `Analysis/ContourHomotopy.lean`: `contourIntegral_holoForm_eq_of_homotopic_loops` + Cauchy null-homotopic form `contourIntegral_holoForm_eq_zero_of_nullhomotopic` (`#print axioms = {std three}`). Two reusable pieces: `holoForm f z = (id ℂ).smulRight (f z)` (the `1`-form `f dz`), and `holoForm_fderiv_symm` (Cauchy–Riemann **closedness**: `fderiv ℝ (holoForm f)` symmetric ⇐ `ℂ`-differentiable, i.e. `v•(u•c)=u•(v•c)`). Stated in Mathlib's bundled `Path`/`Homotopy` + `C²` vocabulary — a **free strengthening** (MA.8 had no consumer; keyhole homotopies for MA.1 are smooth), which also **sets up the MA.1 retirement** (build the keyhole `C²` homotopy, apply this). Design keys: `t := range φ` (compact ⇒ `closure t = t`, dodges the `ContinuousOn ω (closure U)` trap) and supply `dω := fderiv ℝ` explicitly (⇒ symmetry needed only for all directions, never on the tangent cone). Only the axiom-free `circleIntegral_eq_unitLoop_integral` bridge remains in `HomotopyInvariance.lean`; the raw-function axiom + its raw null-homotopic corollary (both consumer-less) were removed. Continuous-`H` (non-`C²`) form still a Mathlib gap (#24019), irrelevant to the intended use. | `Analysis/ContourHomotopy.lean`, `Analysis/HomotopyInvariance.lean` |
| MA.9 | **Radial Fourier inversion** — left inverse of `radial_fourier`: `f(r) = (1/(2π²r))∫₀^∞ k·F(k)·sin(kr)dk`, plus the antiderivative form a jump discontinuity forces (`k·F ~ 1/k ∉ L¹` there, so the pointwise hypothesis genuinely fails) | ✓ DONE (**never axiomatized — proved** from Mathlib's `fourierInv_fourier_eq`; fully closed, no `hC` hypothesis). Also gives reusable `fourier_of_odd`/`fourier_of_even` bridges. The `4π/k` cancels ⇒ radial inversion = sine inversion + ~20 lines. First antiderivative form had an **unsatisfiable `∀ k` hypothesis** (vacuous at the junk `k=0`); fixed to `∀ k ∈ Ioi 0`. See [MATH_AXIOMS.md](MATH_AXIOMS.md) bug 4. | `Analysis/RadialFourierInversion.lean` |
| MA.10 | **Volterra integral equation of the second kind — existence & uniqueness** (continuous kernel, on compacts): `u(r) = g(r) + ∫ᵃ^r K(r,t)·u(t)dt` has a unique solution in `C(Icc a b, ℝ)` — unconditionally (no smallness on `K`, no bound on `b−a`). **Consumers: `OZFIX.15` (claim (A), the ψ construction) AND `OZ.10` (`oz_fixed_pt_unique`: Baxter gives `(I−K)=(I−K₊)(I−K₋)` with each one-sided factor Volterra ⇒ invertible ⇒ uniqueness, no compactness/Fredholm) — one lemma unblocks two physics axioms.** | ✓ **DONE 2026-07-17 (never axiomatized — proved, axiom-clean, ~130 lines).** Route: **iterate bound, not Bielecki** (reuses `C(Icc a b, ℝ)`'s existing metric — no new normed structure). Engine `volterra_iterate_bound`: `|Tⁿu(r)−Tⁿw(r)| ≤ Mⁿ(r−a)ⁿ/n!·dist u w` — the local `(r−a)ⁿ` (not `(b−a)ⁿ`) is what lets the induction close; sup form is a one-line weakening. Then `FloorSemiring.tendsto_pow_div_factorial_atTop` picks `n` with constant `<1/2` ⇒ `ContractingWith (1/2) T^[n]` ⇒ `isFixedPt_fixedPoint_iterate`+`fixedPoint_unique`. **No statement bug** (contrast MA.2/4/5/9) — first form stated was the one proved. Confirmed genuinely absent from Mathlib *and* absent from `docs/1000.yaml` (that list tracks *famous* theorems; Picard–Lindelöf is on it and formalized) — but `ODE/PicardLindelof.lean` is a **worked in-tree precedent** for the same `(M·L)ⁿ/n!` skeleton, which is exactly why this was provable, not axiom-worthy. Consumer-facing form = `volterra_existsUnique_of_continuous` (`M` from compactness of `[a,b]²`, stated pointwise); `volterra_convolution_existsUnique` covers the renewal kernel `K(r,t)=q(r−t)`. **Follow-on (belongs to those tasks, not `Analysis/`): discharge `OZFIX.15`(A)'s and `OZFIX.17`'s explicit Volterra hypotheses by instantiating at `q := q0_poly` on `[σ,R]`.** See [MATH_AXIOMS.md](MATH_AXIOMS.md) MA.10. | `Analysis/Volterra.lean` |
| MA.11 | **Argument principle / zero-count** — for `f` meromorphic on a disc, `(1/2πi)∮_{C(c,R)} f'/f = Z − P` (interior zeros minus poles, each weighted by order). **User-requested 2026-07-17 (via the hcollapse session).** | ✓ **DONE 2026-07-17 — DERIVED THEOREM, no new axiom** (`Analysis/ArgumentPrinciple.lean`, ~90 code lines). As predicted, it is **MA.1 applied to `F := logDeriv f`**. The genuinely new content is the per-circle bridge `circleIntegral_logDeriv_zpow_smul_eq`: `∮_{C(k,r)} logDeriv((·−k)^n·g) = 2πi·n` for `g` analytic-nonzero on the disk — **axiom-clean** (`#print axioms` = standard three). Proof = the pointwise split `logDeriv((·−k)^n·g) = n·(z−k)⁻¹ + logDeriv g` (`logDeriv_zpow_smul_eq`, from `logDeriv_mul`+`logDeriv_fun_zpow`), then `∮ n·(z−k)⁻¹ = n·2πi` (`integral_sub_center_inv`) + `∮ logDeriv g = 0` (Cauchy–Goursat, `logDeriv g` analytic since `g` analytic-nonzero). Assembled into `argumentPrinciple_sum`/`argumentPrinciple_count` via the MA.1 deformation axiom ⇒ `∮_{C(c,R)} F = 2πi·∑ nᵢ`; `#print axioms` = `circleIntegral_eq_sum_of_small_circles` + standard three, exactly as forecast — **no new axiom for MA.11**. **Design note:** the per-hole local factorization is an explicit hypothesis (mirroring `ContourDeformation.lean`'s `circleIntegral_eq_sum_two_pi_I_mul_of_simple_poles`), *not* derived from `meromorphicOrderAt` — that is only a *punctured-nbhd-of-center* fact and does not reach the outer contour without a removable-singularity gluing (out of scope). **Rouché NOT included** — it needs winding-number / homotopy machinery Mathlib lacks (a separate task, not 'MA.1 applied to f'/f'). **⚠ Consumer caveats unchanged:** (1) *necessary-not-sufficient for `POLE.10`* — exhaustion still needs `\|G_baxter\|>0` on a pole-avoiding contour (POLE.5) + one-zero-per-disk (POLE.8/9); MA.11 removes only 'no zeros elsewhere in the bulk'. (2) *off hcollapse's winning path* — POLE.10 serves only `OZFIX.14`'s circular Fourier routes (b)/(c); MA.11's real value is **general/future** (mixture MZERO exact counting, `h_explicit=ψ/r`). See [MATH_AXIOMS.md](MATH_AXIOMS.md) MA.11. | `Analysis/ArgumentPrinciple.lean` |
| MA.12 | **Krein / Wiener–Hopf half-line unique solvability** — would let `OZ.10` (`oz_fixed_pt_unique`) be derived. Scoping brief in [MATH_AXIOMS.md](MATH_AXIOMS.md). | ✓ **DONE 2026-07-17 — PROVED, no new axiom** (`Analysis/WienerHopf.lean`, ~110 code lines, axiom-clean). **The spec's premise was wrong:** it assumed axiomatization because Mathlib lacks WH/Toeplitz/index/winding theory (confirmed absent — all unformalized in `docs/1000.yaml`). That holds for the *full index theorem* but **not** for the recommended (Q2) **positive-symbol** case, which has an elementary route the general theorem lacks — **coercivity via Plancherel**: for `u∈L²` supported on `[0,∞)` and real symbol `a≥ε>0`, `⟪T_a u,u⟫ = ∫a|û|² ≥ ε‖u‖²`; if `T_a u=0` on `[0,∞)` the pairing is 0 ⇒ `u=0`. **No winding number.** Verified numerically (`wh_coercivity_check.py`: operator lower bound = inf symbol). Built on Mathlib's L² Fourier isometry `Lp.fourierTransformₗᵢ` + Plancherel `Lp.inner_fourier_eq` + `L2.inner_def`. **Key scope simplification:** the half-line aspect needs **no Hardy subspace** — just a *support hypothesis* on `u` (the ℝ-pairing collapses to the `[0,∞)`-pairing). Main result `wienerHopf_positive_symbol_injective` + reusable `mulLp_coercive`/`fourierMul_coercive`. Open questions resolved: (Q1) L² [bounded bridge separate], (Q2) index-free positive-symbol, (Q3) injectivity-only, (Q4) absence confirmed, (Q5) OZ→WH translation consumer-side, (Q6) general `a∈L∞`. **⚠ Caveats (spec §5 preserved):** (i) stated in **multiplier** form `T_a=𝓕⁻¹(a·𝓕)`, not convolution — consumer identifies `I−K=T_a` via the L² convolution theorem; (ii) on **L²**, not bounded — the L²→bounded bridge re-introduces exterior decay (`baxter_exterior_regularity`), so MA.12 makes **OZ.10 citable, not free**; (iii) positive-symbol only, not the full index theorem. See [MATH_AXIOMS.md](MATH_AXIOMS.md) MA.12. | `Analysis/WienerHopf.lean` |
| MA.13 | **Paley–Wiener / Wiener-algebra renewal decay** — a right-compactly-supported kernel whose Laplace symbol `1−q̂(z)` is nonvanishing on `{Re z ≥ 0}` gives a decaying renewal/Volterra solution `ψ→0`. Consumer: the general-`η` exterior-decay of `baxter_exterior_regularity` (via `POLE.11`). | ✓ **AXIOM (legitimate) + WIRED, 2026-07-18.** Unlike MA.12, this is a **genuine axiom**: recon confirmed Mathlib has no Wiener `1/f`/Wiener-algebra inversion, no L¹ convolution Banach algebra, no Laplace transform, no Paley–Wiener/renewal theory — and **no elementary shortcut** (the classical proof needs the Gelfand-spectrum identification of the Wiener algebra with the compactified half-plane; Mathlib *has* the abstract `WeakDual.CharacterSpace.exists_apply_eq_zero` but not the concrete algebra or its character-space, a hard from-scratch development). **Statement-fix (caught while wiring):** dropped the over-specified `t<0` support clause — unsatisfiable by `q0_poly` (a nonzero polynomial for `t<0`) and mathematically inert (only `q|_{[0,S]}` enters the equation and symbol); weakened to `S<t→q t=0` (= `q0_poly_outer`). **Numerically verified** (`ma13_verify.py`/`ma13_dense.py`): dense **negative** kernels (`‖q‖₁≥1`, matching `q0_poly`'s sign) with symbol nonvanishing on the RHP decay; any RHP zero ⇒ exponential growth (symbol hypothesis load-bearing, non-vacuous for the dense regime). **Wiring** (`HardSphere/BaxterRenewalDecay.lean`, axiom-clean apart from the MA.13 axiom): `baxterPsiOuter_tendsto_zero_of_symbol` instantiates the abstract axiom at `q0_poly`/`baxterForcing`/`baxterPsiOuter` (after the `[σ,∞)→[0,∞)` shift) ⇒ `baxterPsiOuter→0` from the concrete symbol nonvanishing; helper `baxterPsiOuter_continuousOn_Ici` (axiom-clean). The symbol hypothesis `1−∫₀^σ q0_poly·e^{−zt}≠0` on `{Re z≥0}` (⟺ `Q̂≠1` on `{Im k≤0}`) is **MA.14**'s output — left as an explicit hypothesis so this is independent of MA.14's in-progress file. See [MATH_AXIOMS.md](MATH_AXIOMS.md) MA.13. | `Analysis/WienerRenewal.lean`, `HardSphere/BaxterRenewalDecay.lean` |
| MA.14 | **Hermite–Biehler root location** — `Q̂≠1` on the closed lower half-plane (all physical η<1) ⇒ every Baxter pole in the open UHP; supplies MA.13's symbol-nonvanishing hypothesis. | ✓ **AXIOM (bounded core) + END-TO-END LOOP CLOSED, 2026-07-18** (processed by another session). The axiom was **shrunk** from the whole closed LHP to the bounded core `G_baxter_ne_zero_on_lower_core` on `{Im k≤0, k≠0, ‖Npoly‖≤‖Dpoly‖}`: the outer region became the axiom-clean THEOREM `G_baxter_ne_zero_of_norm_dominant` (a zero forces `‖Npoly‖=‖Dpoly‖·e^{σ·Im k}≤‖Dpoly‖`; the core is bounded since `‖Npoly‖∼‖k‖³` overtakes `‖Dpoly‖∼‖k‖`), and the whole-LHP `qhat_complex_ne_one_of_im_nonpos` + `baxter_pole_im_pos` are now THEOREMS on the core axiom. True ∀η<1, numerically confirmed incl. the worst ray (`G_baxter(−bi)` is *real* and `<0 ∀b>0`, the negative imaginary axis where the norm bound fails and nonvanishing is a genuine HB phase fact). **End-to-end loop CLOSED** (`HardSphere/BaxterExteriorRegularityGeneral.lean`): the symbol bridge `qhat_symbol_nonvanishing` (`z=ik`: `∫₀^σ q0_poly·e^{−zt}=Q̂(−iz)`, `Re z≥0⟺Im(−iz)≤0`, `z=0` removable) feeds MA.14 into MA.13's wiring ⇒ general-η `baxterPsi_bounded_Ici` + `r_mul_ozBaxterFixedPt_tendsto_zero` (the two exterior clauses of `baxter_exterior_regularity`), both with `#print axioms` = standard three + `volterra_renewal_tendsto_zero` (MA.13) + `G_baxter_ne_zero_on_lower_core` (MA.14 core) ONLY. **Remaining:** prove the HB core (MA.11 + explicit winding count, needs the real-axis case `BAXTER.16`). **⚠ Abstraction to `Analysis/` REFUTED (2026-07-18, `ma14_*.py`):** the general quasi-polynomial family `ik³−p₀k²+ip₁k+2p₂ − (i(p₁+2p₂σ)k+2p₂)e^{−ikσ}` over arbitrary real `p₀,p₁,p₂,σ` has **counterexamples to every clean hypothesis** (no-real-zeros 1260/1500; sign-pattern 670/2000; +structural constraint `p₀+p₁σ+p₂σ²=0` still 55/1057), so a project-independent abstract axiom would be **false-as-stated**. Root cause = the stability is the **HS-PY no-spinodal physics**, not a generic algebraic fact: `G_baxter=(−ik)³(1−Q̂)` and on ℝ `1−ρĈ=\|1−Q̂\|²`, so *no LHP zeros ⟺ no spinodal*, and the counterexamples are literally spinodal systems. **⇒ keep MA.14 concrete (HS-specific is correct, not a defect); the winding bridge is dis-recommended (needs an absent lower-half-disk arg-principle = a new axiom, and only re-expresses BAXTER.16).** **✅ ACTED ON 2026-07-18 — this insight IMPLEMENTED:** `G_baxter_ne_zero_on_lower_core` RETIRED, split into PHYSICS axiom `pyhs_no_spinodal` (the single strict fact `1−ρĈ>0` on ℝ, `BaxterNoSpinodalEquiv.lean`, re-homed as OZ.20) + `baxter_no_open_lhp_pole_core` (strict open-LHP); MA.14 theorems + general-η decay now rest on those (+MA.13). No elementary proof of no-spinodal (SOS ⇒ ≥0, min→0 as η→1 ⇒ no uniform gap); future = η-interval-arithmetic (not pursued). **The whole HS cluster (MA.14, `baxter_exterior_regularity`, `oz_fixed_pt_unique`) funnels to ONE physical root = general-η **`BAXTER.16`**, which — since `1−ρĈ=\|1−Q̂\|²≥0` is proven — collapses to the single strict fact **`1−Q̂(ξ)≠0` on ℝ ∀η<1** ("PY-HS structure factor never diverges"). See [MATH_AXIOMS.md](MATH_AXIOMS.md) MA.14. | `HardSphere/BaxterHermiteBiehler.lean`, `HardSphere/BaxterExteriorRegularityGeneral.lean` |
| MA.15 | **Bounded (`L∞`) Wiener–Hopf injectivity, radial shell operator** — arbitrary kernel `C` supported in `[0,σ]`, coercive symbol `1−ρĈ ≥ ε>0` ⇒ `I−K` injective on bounded exterior-continuous functions vanishing on the core. | ✓ **AXIOM, abstracted + migrated to `Analysis/` 2026-07-19.** Created by abstracting the former OZ.10 axiom `oz_linear_op_bounded_injective` (a domain-referencing math axiom) into a project-independent statement; that concrete result is **now a THEOREM** instantiating it at `C := c_HS` (support from `c_HS_outer`; `radial_fourier` and `oz_linear_op` unfold definitionally). **Stays an axiom**, unlike its `L²` twin MA.12 (`wienerHopf_positive_symbol_injective`, *proved* via Plancherel coercivity): that argument does not survive to `L∞` (a bounded function need not be `L²`), and the classical bounded route needs Wiener-algebra resolvent inversion — the same gap as MA.13. The asymmetry is the **function space, not the physics** (coercivity enters only via a hypothesis discharged from `pyhs_no_spinodal`, so no physics reaches `Analysis/`). **⚠ statement trap caught while abstracting:** the support hypothesis `hCsupp` is load-bearing — the operator integrates `C` only over `[0,σ]` while the symbol integrates `(0,∞)`, so without it coercivity could come from mass the operator never sees and the statement is FALSE. Consumer: `OZ.10` ⇒ retired the physics axiom `oz_fixed_pt_unique`. See [MATH_AXIOMS.md](MATH_AXIOMS.md) MA.15. | `Analysis/RadialWienerHopf.lean` |
| MA.16 | **Leibniz rule with a variable upper limit AND an `r`-dependent integrand** — `d/dr ∫_a^r K(r−t)·φ(t)dt = K 0 · φ r + ∫_a^r K'(r−t)·φ(t)dt`. Consumer: `OZFIX.25` (★DIFF). | ✓ **DONE 2026-07-19 — PROVED, no new axiom** (~200 lines, axiom-clean). `K` need only be differentiable **a.e.**; regularity across the kink is carried by one `LipschitzOnWith` hypothesis on the window of arguments that occur. **Split at the base point** (`∫_a^r = ∫_a^{r₀} + ∫_{r₀}^r`) rather than the spec's `r ↦ (r,r)` chain rule, which would have needed *joint* differentiability; the moving-endpoint half needs no ε-δ argument (subtracting `K 0` leaves an `O(|x−r₀|²)` remainder under the **same** Lipschitz hypothesis). Factored into two reusable halves — `hasDerivAt_intervalIntegral_param` takes an **arbitrary** upper limit `b`, so it serves OZFIX.25 steps 2 *and* 3. Instantiation smoke-tested against `q0_poly`/`baxterPsiOuter` before closing. ⚠ `Continuous φ` forces the consumer to pass the `max`-clamped representative of `baxterPsiOuter` (it jumps at `σ`). See [MATH_AXIOMS.md](MATH_AXIOMS.md) MA.16. | `Analysis/ConvolutionLeibniz.lean` |

### Group 1 — Closed-Form Integral Identities *(yukawa_dcf)*

| Task | Title | Status | Lean file |
|------|-------|--------|-----------|
| 1.1 | I₁ antiderivative | ✓ DONE | `YukawaOZ/I1I2Integrals.lean` |
| 1.2 | I₂ antiderivative | ✓ DONE | `YukawaOZ/I1I2Integrals.lean` |
| 1.3 | I₁/I₂ vanish at ℓ=0 | ✓ DONE | `YukawaOZ/I1I2Integrals.lean` |

### Group 2 — Hard-Sphere Baxter Factor *(hard_sphere)*

| Task | Title | Status | Lean file |
|------|-------|--------|-----------|
| 2.1 | φ₁, φ₂ auxiliary formulas | ✓ DONE | `HardSphere/BaxterFactor.lean` |
| 2.2 | det(s) non-vanishing | ✓ DONE | `HardSphere/BaxterFactor.lean` |

### Group 3 — Wiener–Hopf Structure *(hard_sphere)*

| Task | Title | Status | Lean file |
|------|-------|--------|-----------|
| 3.1 | B₁+D₁ = T_U identity | ✓ DONE | `HardSphere/Splitting.lean` |
| 3.2 | Support of T_S on (−∞, R_ij] | ✓ DONE | `HardSphere/Splitting.lean` |

### Group chsY — FMSA_chsY Formula Failure *(failures)*

| Task | Title | Status | Lean file |
|------|-------|--------|-----------|
| 4.3 | (1+A)² ≠ 1−g² — root cause of HSY spike | ✓ RESOLVED | `FMSAPoly/OriginCheck.lean` |

### Group 4 — Single-Component Reduction *(yukawa_dcf)*

| Task | Title | Status | Lean file |
|------|-------|--------|-----------|
| 4.1 | b_ij N=1 collapse formula | ✓ DONE | `YukawaOZ/BijReduction.lean` |
| 4.2→M.9 | g + a·exp(−z) = 1 — *re-IDed to* **M.9** *(Group M, hard-sphere)* | ✓ DONE | see M.9 — `HardSphere/SingleCompIdentity.lean` |
| 4.4 | Full N=1 reduction Eq.41→42 | ✓ DONE | `HardSphere/SingleCompReduction.lean` |

### Group M — Multi-Component Baxter Identity *(hard-sphere Baxter matrix)*

| Task | Title | Status | Lean file |
|------|-------|--------|-----------|
| M.1 | Abstract matrix identity Ĝ+Â·c=I | ✓ DONE | `Analysis/MatrixIdentity.lean` |
| M.2 | N=1 limit: Ĝ₀₀=g, Â₀₀=a | ✓ DONE | `HardSphere/MatrixN1.lean` |
| M.3 | det(Q̂₀) ≠ 0 multi-component | ✓ **DONE, axiom-clean** — unconditional `Q0_mat_phys_isUnit_det` (`#print axioms` = `[propext, Classical.choice, Quot.sound]`); conditional Gershgorin `..._of_diag_dom` kept as historical partial | `HSMixture/MatrixQ0.lean`, `HSMixture/Q0DetRankTwo.lean` |
| M.4 | Unconditional `det(Q0_mat_phys) > 0` (in fact `≥ 1`) for all `z > 0` | ✓ **DONE, axiom-clean (`Q0_moment_det_pos` axiom RETIRED → theorem, 2026-07-16)** — Route B: `det = 1+(a+d)−(bc−ad)`, `moment_key` (`bc−ad ≤ a+d`) ⇒ `det ≥ 1`; core is the elementary 2-var kernel `kernel_nonneg` (`G=M·Φ+M·Φ`), summed via `per_pair_f`. See [proof_notes_matrix_q0.md](proof_notes_matrix_q0.md) M.4 | `HSMixture/Q0DetRankTwo.lean` |
| M.5 | `nAux_eq_mAux_div_two` (`nAux u = mAux u/2`) | ✓ DONE | `HSMixture/Q0DetRankTwo.lean` |
| M.6 | `one_add_half_sq_lt_cosh` (`1+u²/2 < cosh u` for `u>0`) | ✓ DONE | `HSMixture/Q0DetRankTwo.lean` |
| M.7 | `ratioPM_strictAntiOn` (`pAux/mAux` decreasing on `(0,∞)`) | ✓ DONE | `HSMixture/Q0DetRankTwo.lean` |
| M.8 | `moment_ad_le_bc` (**`bc ≥ ad`** for all N) | ✓ DONE | `HSMixture/Q0DetRankTwo.lean` |
| M.9 | g + a·exp(−z) = 1 — single-comp Baxter contact identity *(ex-4.2)* | ✓ DONE | `HardSphere/SingleCompIdentity.lean` |
| M.10 | Concrete Q̂₀=P̂+Ê·exp(−z·σ_min) *(ex-B.2)* | ✓ DONE | `HardSphere/QhatDecomposition.lean` |
| M.11 | Coefficient algebra (1−g²)−a²c²=2acg *(ex-B.3)* | ✓ DONE | `HardSphere/SingleCompIdentity.lean` |


### Group GAP — FMSA_GA_matrix_mix Algebraic Foundation and Polynomial Determination *(yukawa_dcf)*

| Task | Title | Status | Lean file |
|------|-------|--------|-----------|
| 2.3 *(ex-B.1)* | Shifted-exponent integral (pure HS Baxter factor; moved from Group GAP 2026-07-17) | ✓ DONE | `HardSphere/BaxterFactor.lean` |
| B.2→M.10 | Concrete Q̂₀=P̂+Ê·exp(−z·σ_min) — *re-IDed to* **M.10** *(Group M, hard-sphere)* | ✓ DONE | see M.10 — `HardSphere/QhatDecomposition.lean` |
| B.3→M.11 | Coefficient algebra (1−g²)−a²c²=2acg — *re-IDed to* **M.11** *(Group M, hard-sphere)* | ✓ DONE | see M.11 — `HardSphere/SingleCompIdentity.lean` |
| GAP.4 | Origin BC automatic for FMSA_GA_matrix_mix | ✓ DONE | `YukawaOZ/InnerOriginBC.lean` |
| GAP.5 | Degree bound exisr: deg P_{ij} ≤ 4 (no r^n for n≥N with N=4) | ✓ DONE | `YukawaOZMix/MixturePolyCoeffs.lean` |
| GAP.6 | Origin uniqueness: only A_{ij}=−E_{ij}(0) forced at r=0 | ✓ DONE | `YukawaOZMix/MixturePolyCoeffs.lean` |
| GAP.7 | No contact BC: B,C,D,E^{(4)} not fixed by r=R_{ij} | ✓ DONE | `YukawaOZMix/MixturePolyCoeffs.lean` |
| GAP.8 | Laurent extraction: all five coefficients from R_{ij}(s) at s=0 | ⚠ **VACUOUS — reopened 2026-07-16**: `poly_coeff_from_laurent` is an `∃` closed by 5 `rfl`s with an **unused** `AnalyticAt` hyp — asserts only "five reals exist". **Root cause: `P_ij` does not exist as a Lean object** (it is MPOLY.5's deliverable), so the intended claim had nothing to predicate on ⇒ GAP.8 is *half* a theorem, completable only with MPOLY.5. **Anchor now supplied**: `taylor4_coeff_unique`/`poly4_eq_zero_of_littleO` (`MixtureLaurent.lean`, axiom-clean) make "the order-4 Taylor coefficients" well-defined. **Still open**: the `aₖ = R⁽ᵏ⁾(0)/k!` bridge (`AnalyticAt`→`ContDiffOn`→`taylor_isLittleO`→`taylorCoeffWithin`) + the `P_ij` side. ⇒ MPOLY.4 "extends GAP.8" inherits NOTHING; GAP.10 must not lean on it | `YukawaOZMix/MixturePolyCoeffs.lean` |
| GAP.9 | D_{ij} generically nonzero for unlike pairs | ❌ **FALSIFIED 2026-07-17 — the claim is FALSE: `D_{ij} ≡ 0`.** The shipped `fmsa_double_prop` closed form gives, for an unlike pair, `r·c⁽¹⁾` piecewise with r³ coefficient **identically zero on both pieces** (inner `(0,λ)` is degree 1; outer `(λ,R)` has r³ and r⁵ cancelling **exactly**) — verified `≡0` (≤2e-12) over 12 random parameter sets **and at GAP.9's own cited point** σ=[1,1.2], ρ*=0.5, T*=1.5, where GAP.9 claimed `D_01=−3295` (that number came from the superseded `FMSA_GA_matrix_mix` route). Structural cause: the `[λ,R]` and `[−R,−λ]` rate-0 polys have **exactly opposite** coefficients for all k≥3, so the `(−1)^k` from `𝒲(−r)` kills every **odd** k≥3. Also the classic PY structure `r·c(r) = −λ₁r − 6ηλ₂r² − ½ηλ₁r⁴` has **no r³ term** ⇒ `D ≡ 0` was expected; N=1 was never special. The old argument ((a) no parity symmetry, (b) ΔQ cross-terms) is a **non-sequitur** — "no symmetry forces D=0" ⇏ "D≠0". **Still true, retained:** `no_odd_symmetry` (only refutes a *forcing* argument), `d_ij_nonzero_example`, and `dij_cubic_nonzero` — but the last is a **Laplace-space `q0_entry` z-Taylor** fact, a different object from the real-space `D_ij`, and must not be cited as evidence for it (re-scoped) | `YukawaOZMix/MixturePolyCoeffs.lean` |
| GAP.10 | Degree: `natDegree ≤ 4` always; `= 4` **iff** leading coeff ≠ 0 | ⚠ **RELAXED 2026-07-17 (was "exact degree = 4", which is false as an unconditional claim)** — ✓ DONE in the relaxed form, axiom-clean: `poly_natDegree_le_four` (**unconditional `≤ 4`**), `poly_natDegree_eq_four` (`= 4` given `a ≠ 0`), **new** `poly_natDegree_eq_four_iff` (`= 4 ↔ a ≠ 0`, the sharp characterisation). Why relaxed: the FMSA leading coefficient **changes sign** across physical parameters (`fmsa_double_prop`) ⇒ vanishes on a codim-1 set; and on an unlike pair's **inner** piece `(0,λ_ij)` the polynomial is **degree 1** (leading coeff ≡ 0). Also there is **no single `P_ij`** on `(0,R_ij)` for unlike pairs at all ([LN] Eq.(101) ansatz falsified), and GAP.10's `P_ij` would come via GAP.8, which is **vacuous** ⇒ no source. The lemmas are sound generic polynomial algebra; FMSA application is limited to **one piece with a verified nonzero leading coefficient**. Docstring coefficient-convention error fixed (leading = `E⁴ = R(0)/4!`, *not* `R⁽⁴⁾(0)/4!` — that is the constant term under GAP.8's order-reversing map). See `proof_notes_mixture_dcf.md` Group MPOLY | `YukawaOZMix/MixturePolyCoeffs.lean` |

### Group IB — Inner-Core Mediated Breakpoints *(breakpoints)*

*Split out of Group GAP once the mediated inner-core breakpoint work outgrew it.
**IB.1–IB.8 were formerly B.11–B.18**; the old B.19 (hard-sphere `λ_ij` kink) moved to Group OZ as
**OZ.18** (it is FMT hard-sphere, outside the Yukawa mediated chain). The Lean identifiers in
`YukawaOZMix/InnerDecomp.lean` were made **task-ID-free** (e.g. `b11_…`→`terms_II_III_zero`),
so IB.* can be renumbered without touching Lean source. Proof records:
[proof_notes_breakpoints.md](proof_notes_breakpoints.md).*

| Task | Title | Status | Lean file |
|------|-------|--------|-----------|
| IB.1 | Terms II+III ≡ 0 in the inner region (unconditional: `alpha = r−σ_a−R_ij < 0`) | ✓ DONE | `YukawaOZMix/InnerDecomp.lean` |
| IB.2 | Term IV geometry: `Δ_ai = −R[i,a]` ⇒ `Alm=0`, `c_exp=d_exp=r*=R[a,b]+R[i,a]`; sub-term ≡ 0 below `r*` (unconditional) | ✓ DONE | `YukawaOZMix/InnerDecomp.lean` |
| IB.3 | mediated ≡ 0 on `(0,R_ij)` when no `(a,b)` satisfies A∧B; **binary N=2 ⇒ mediated ≡ 0 for any σ** | ✓ DONE (both; size chain `σ_a<σ_b<σ_j` via `active_pair_size_chain`) | `YukawaOZMix/InnerDecomp.lean` |
| IB.4 | Piecewise rigidity: when is the inner-core residue a *single* polynomial? *(redone 2026-07-17)* | ✅ **REDONE, axiom-clean** — real content = **piecewise rigidity**: `single_poly_forces_pieces_eq` (`f=P₁` on `(0,λ)`, `=P₂` on `(λ,R)`, `=Q` on `(0,R)` ⇒ `P₁=Q=P₂`; via `Polynomial.eq_of_infinite_eval_eq` + `Set.Ioo_infinite`) + `not_single_poly_of_pieces_ne` (`P₁≠P₂` ⇒ **no single Q exists**). Instantiated at the measured witness ⇒ no single `P_ij` for unlike pairs ⇒ [LN] Eq (101) fails for unlike, holds for like (λ=0, no interior breakpoint). **The original `residue_is_polynomial` is kept but now flagged CONDITIONAL** (all content sits in the hypothesis `hdecomp`; `hdecomp` is false for unlike pairs; its claim to license `_update_polycorr` does **not** follow) | `YukawaOZMix/InnerDecomp.lean` |
| IB.5 | Sharpness witness: σ=[1,4,8], `(a,b,j)=(0,1,2)` satisfies A∧B and `r* < R[0,2]` | ✓ DONE, no sorry (`ternary_148_active`) | `YukawaOZMix/InnerDecomp.lean` |
| IB.6 | r** switch identity: `qP(u_lo_bj)=0` ⇒ integrand vanishes at moving boundary ⇒ C¹ at `r**=r*+(3d_b−d_j)/2` (no slope jump) | ✓ DONE (`qP_at_uLo_zero`, `ivIntegrand_at_uLo_zero`; full Leibniz-C¹ + curvature stay numerical) | `YukawaOZMix/InnerDecomp.lean` |
| IB.7 | r** interior ⟺ Condition C (`d_a+2d_b<d_j`); `C ∧ B ⟹ A`; both mediated knots interior iff `d_a+2d_b<d_j<3d_b` | ✓ DONE (`rstarstar_interior_iff_C`, `condC_activeB_imp_activeA`) | `YukawaOZMix/InnerDecomp.lean` |
| IB.8 | Mediated knot completeness: `u_hi_eff=r` under A∧B (upper limit never switches ⇒ only `r*`,`r**` knots) | ✓ DONE (`uHiEff_eq_r`, `uHiEff_eq_r_of_active`) | `YukawaOZMix/InnerDecomp.lean` |
| IB.9 | *(opt.)* First-order **DCF** breakpoint set on `(0,R_ij)` is exactly `{λ_ij}` (unlike: 1 knot, C¹; like: none) for all N — mediated `r*` and intermediate `λ_im` add **no** DCF knot; **IB.6–IB.8's `r*`/`r**` are stepwise-poly/RDF-only, the DCF does not inherit them** *(new 2026-07-17)* | ✓ **support geometry CLOSED (2026-07-18)** — `YukawaOZMix/MixtureConvolution.lean` (axiom-clean, sorry-free): kernels `bMixEntry`(`ℬ`)/`pMixEntry`(reflected `𝒬⁻`), mediated `bConvP`/triple `pbpConv` + support lemmas. **`pbp_breakpoints_subset` + the 4 edge identities `pbp_edge_eq`/`_lu`/`_ul`/`_uu`: ALL four breakpoints of `P_im⋆ℬ_mn⋆P_jn` (`{−R_im,−λ_im}+{R_mn}+{−R_jn,−λ_jn}`) evaluate to the `m,n`-FREE set `{−R_ij,−λ_ij,λ_ij,R_ij}` — intermediate species cancel in EVERY edge ⇒ `r*=R[a,b]+R[i,a]` is NOT a DCF breakpoint at all (it's the *stepwise-poly* IB.6–8 object, not inherited); on `(0,R_ij)` only `λ_ij` survives interior.** ⚠ **Corrects the 2026-07-17 "coefficient-merging residue" claim — WRONG: `r*` is not an edge, so pure support geometry closes it (as the proof note said).** **Analytic half — N=1 DCF smoothness FULLY CLOSED (2026-07-18), `YukawaOZMix/MixtureDCFSmooth.lean` (axiom-clean, sorry-free): headline `innerDCF_N1_contDiffOn`** — `fun r ↦ −bConvP(r) − (P⋆ℬ)(r) + pbpConv(r) − pbpConv(−r)` (`= 2π√(ρ²)·r·c₁(r)` by `innerDCF_N1_oddPart`) is `ContDiffOn ℝ ⊤ (Ioo 0 R_00)`; N=1 ⇒ `λ_00=0` ⇒ no breakpoint in the open core ⇒ **no interior knot**. Atoms: `expQuadClosed_outer_eq` (OUTER per-pole term collapses to a **pure exponential** `K·e^{−zy}` — `y`,`y²` cancel) + `outer_perpole_integral`/`aligned_perpole_integral` + `expQuadClosed_contDiff`/`expQuadClosedPos_contDiff` (poly·exp ⇒ `ContDiff`); `bConvP_contDiffOn`/`pConvB_contDiffOn` (`ℬ⋆P`, `P⋆ℬ` terms); **`pbpConv_pos_contDiffOn`/`pbpConv_neg_contDiffOn`** (the two `P⋆ℬ⋆P` terms). **The `pbpConv_eq_intervalIntegral` `hint` gate was discharged by PIECEWISE INTEGRABILITY, not continuity** (`pbpIntegrand_intervalIntegrable`): split at the jump `t₀=x−R`, each half `bConvP(x−t)` equals a continuous closed form (outer/aligned) ⇒ each half integrable, `IntervalIntegrable.trans` glues — the jump is irrelevant to a Lebesgue integral, so NO `bConvP` continuity / no `R`-matching / no `bConvP(0)=0` needed. `pbpConv(−r)`'s `[−r,0]` piece vanishes by support. **GENERAL-N FULLY CLOSED (2026-07-19, axiom-clean): `dcfOdd_contDiffOn_upper` (`(λ_ij,R_ij)`) + `dcfOdd_contDiffOn_lower` (`(0,λ_ij)`) + `dcfOdd_contDiffOn_like` (like pairs whole core) — the only interior knot of `c^(1)_ij` is `λ_ij`, for EVERY N and pair.** ⚠ The earlier "general-N needs aggregate cancellation" was a SIGN ERROR: with signed `Mix.lam=(σ_l−σ_k)/2`, `R_in−λ_jn=R_ij` identically (`R_sub_lam_eq`), so terms are clean `{±λ_ij,±R_ij}` term-by-term — NO cancellation, mechanical extension (`Wmix` matrix double-sum + index-general `bConvP_contDiffOn_aligned`/`pConvB_contDiffOn_aligned`/`pbpConv_forward`/`pbpConv_reflected`/`pbpConv_contDiffOn_midAligned`, reflected via `.comp Neg.neg`). | `YukawaOZMix/MixtureConvolution.lean` + `MixtureDCFSmooth.lean` |


### Group C — FMSA_GA_matrix_mix Consistency *(yukawa_dcf)*

| Task | Title | Status | Lean file |
|------|-------|--------|-----------|
| C.1 | N=1: corrected formula = FMSA_pure | ✓ DONE | `HardSphere/SingleCompReduction.lean` |
| C.2 | N=1 like-pair inner formula bounded: `(1−G²₀₀)·exp(z(R−r))` ≤ C on (0,d) via exp-cancellation | ✓ DONE (`c1_n1_twoexp_bounded`) | `HardSphere/SingleCompReduction.lean` |
| C.5 | Leading Yukawa-pole residue of the first-order MSA amplitude. **CORRECTED:** exact single-tail residue is `[Q̂₀⁻¹·K·Q̂₀⁻ᵀ]_{ij}` (doubly-propagated), **not** `K·G` (the numerical shorthand is a leading-order approx; `K·G²` at N=1) | ◑ core (`c5_residue_eq_K_mul_Ginv`) + single-tail exact residue (`spectralAmp_residue`, `spectralAmp_residue_n1`) DONE; multi-tail + Wiener–Hopf derivation of `b_{ij}(s)` → **Group Y1** | `YukawaOZMix/YukawaPoleResidue.lean`, `YukawaOZMix/SpectralAmplitude.lean` |

### Group Y1 — First-Order Yukawa RDF/DCF (Wiener–Hopf derivation) *(yukawa_wh)*

*The ε¹-order analog of Group BAXTER (same **algebraic** Wiener–Hopf machinery — causal/anti-causal
split + support + residues, **not** the Hilbert transform of [LN] §6.3 which Mathlib lacks; see Y1.3
— applied to the first-order OZ equation `H̃₁(I−C̃₀)=(I+H̃₀)C̃₁`). Split off as the full
concrete-C.5 derivation. Source: **[LN]** §5–§6. Records: [proof_notes_yukawa_wh.md](proof_notes_yukawa_wh.md).*

✅ **Group Y1 is CLOSED — re-audited 2026-07-31.** `#print axioms` run on all 19 Y1 declarations:
**every one returns exactly `[propext, Classical.choice, Quot.sound]`**, so Y1 consumes **no
Group-MA axiom of any kind**. Build green (8714 jobs), no `sorry` in `LeanCode/`. Two deliberate
non-goals, recorded so they are not mistaken for gaps: **[LN] Eq. 14**'s closed-form `W/det` inverse
(Y1.1 — `adj/det` is what everything uses, and the concrete N=2 entry comes from MML.1's
`Q0inv_zero_one`) and **[LN] Eq. 67**'s contour-closure formula (Y1.4 — bypassed by Y1.3's
real-space re-route; formalizing it would re-import the very machinery Y1.3 avoided).

| Task | Title ([LN] ref) | Status | Lean file |
|------|-------|--------|-----------|
| Y1.1 | Complex Baxter matrix `Q̂₀(s):ℂ→Matrix` (Eq. 10–13) + inverse-as-`adj/det` (Eq. 14) | ✓ DONE (`q0_entry_c`, `Q0_mat_c`, `q0_entry_c_real`, `inv_apply_eq_adj_div_det`); closed-form Eq. 14 deferred — **and staying deferred** (no consumer; concrete N=2 entry served by MML.1 `Q0_det_fin_two`/`Q0inv_zero_one`) | `HSMixture/Q0Complex.lean` |
| Y1.2 | Outer MSA DCF Laplace transform `U₁(k)_{ij}=K_{ij}e^{−ikR_{ij}}/(ik+z_{ij})` (Eq. 34/46) + Yukawa pole at `k=iz_{ij}` | ✓ DONE (`integral_Ioi_cexp`, `outerDCF_transform`) | `YukawaOZ/OuterDCF.lean` |
| Y1.3 | Wiener–Hopf one-sided projection isolating `B₁={T_U}^{[R,∞)}` (Eq. 55–66) — **algebraic split + support + residues, NOT Hilbert transform** (Mathlib lacks it; mirror `Splitting.lean`/Group BAXTER). Split a/b/c | ✓ **DONE (a/b/c, axiom-clean, real-space route)**. a: `q0_poly_support_subset`, `q0MixEntry`+`_support_subset`, `integral_/fourier_{Iic,Ici}_eq_of_support/_eq_full`. b: `causal_projection_real`/`_fourier` (support-orthogonality = `1_{[R,∞)}·`, no FT injectivity). c: `matrix_conj_residue_analytic`, `outer_residue`, `outer_residue_eq_spectralAmp_residue` (`Res T_U = Res b_{ij}`). Remaining = physics inputs (OZ eqn, concrete supports), not logic gaps | `WHSupports.lean` (a); `YukawaCausalProjection.lean` (b); `YukawaCausalResidue.lean`+`YukawaWienerHopf.lean` (c) |
| Y1.4 | Residue evaluation of the WH projection → `B₁(k)` (§6.4.1, Eq. 63–67) | ✓ DONE — residue-theorem step (`matrix_conj_residue`, `triple_apply`) + the Eq. 63 integrand now supplied by Y1.3c (`matrix_conj_residue_analytic`, `outer_residue`). **Eq. 67's literal contour formula is deliberately NOT formalized** — it is [LN]'s k-space intermediate, bypassed by Y1.3's real-space projection, and `B₁` is already closed-form via Y1.5's `bMulti`. ⚠ *The proof note carried `◑` plus "Y1.3 is deferred" until 2026-07-31; Y1.3 had landed the same day (`feedback_stale_blockers`) — the two files had disagreed since.* | `YukawaOZMix/YukawaWienerHopf.lean`, `YukawaCausalResidue.lean` |
| Y1.5 | Spectral amplitude `b_{ij}(s)` four-term/multi-tail (Eq. 73) + tie `A` to `Q̂₀(s)⁻¹` | ✓ **DONE** — single-tail (`spectralAmp_residue`/`_n1`), collapse (`bMulti_single_eq`/`_residue`), **general distinct-`z`** (`simplePole_offResidue`, `bMulti_residue` = per-pole term matching, `bMulti_residue_Qinv` = tie to `Q̂₀⁻¹` via `one_add_sub_one`) | `YukawaOZMix/SpectralAmplitude.lean` |
| Y1.6 | First-order RDF `Ĥ₁=[Q̂₀ᵀ]⁻¹B₁[Q̂₀]⁻¹` (Eq. 68) + the exact C.5 residue corollary | ✓ DONE (`Hhat1`, `Hhat1_spec`, `Hhat1_residue`) | `YukawaOZMix/YukawaWienerHopf.lean` |
| Y1.7 | inner-core `S₁(k)` (§9, Eq. 45) + origin constraint (Eq. 76) + contact matching (§7) — Group Y1 capstone | ✓ DONE (axiom-clean): `innerS1`/`innerS1_support_subset_Iio` (Eq. 45, Proof 2 anti-causal), `b1_causal_eq_U1_fourier` (§9.3, instantiates Y1.3b for concrete `S₁`), `origin_constraint_eq76` (Eq. 76, reuses P.2 + `eij_at_origin`), `innerS1_contact_value` (§7, reuses Group 5.1) | `YukawaOZ/YukawaInnerCore.lean` |


### Group MML — N=2 Mixture Mittag-Leffler Inner-Core *(mixture_rdf)*

For N=2, Q̂₀(z) and its 2×2 inverse (adj/det) are fully algebraic (Y1.1 DONE), so the HS-pole
residue `B_k = −Q̂₀₀₁(s_k)/det′(Q̂₀)(s_k)` is provable without Y1.3 via `residue_of_simple_pole`.
This resolves the "no closed form for N=2" claim: the exact inner DCF **is** a convergent
Mittag-Leffler series; the only transcendental obstacle is the pole *locations* `s_k` (roots of
`det(Q̂₀)=0` — see **Group MZERO**), not the residues. Full assembly (MML.8) needs Y1.3 (done).
See [proof_notes_mixture_rdf.md](proof_notes_mixture_rdf.md) Group MML.

**MML.3 retired (2026-07-16):** it bundled ~5 independent sub-results and its genuine difficulty
(the series-equals-true-DCF *collapse*) is the N=2 matrix analog of the still-open scalar
`hcollapse` (OZFIX.6/9/10). **Superseded by the five topic-scoped tasks MML.4–MML.8** below
(Lean identifiers in `MixtureMLSeries.lean`/`MixtureInnerDCF.lean` are task-ID-free). MML.4–MML.7
✓ DONE (axiom-clean); MML.8 is the hard collapse (scoped only); **MML.9 + MML.10 ✓ DONE
(2026-07-24)** — the structure-factor form `Ĥ₁ = S₀·Ĉ₁·S₀`, which localizes every RDF HS pole in
`S₀ = (I−Ĉ₀)⁻¹` and so narrows MML.8's residual input to the **hard-sphere** side, plus the
residue-matrix factorization of term (II) and the mixture collapse factor `R_k = R_k·Ĉ₀(s_k)`.

**Group status (2026-07-24).** Every MML task is closed **except MML.8's collapse**: MML.1, MML.2,
MML.4, MML.5 (closed this session — no bookkeeping remainder), MML.6, MML.7, MML.9, MML.10, MML.11,
MML.12, MML.13 all ✓ axiom-clean; MML.3 retired. **MML.14 ◑ IN PROGRESS** — the matrix real-space
infrastructure MML.8's collapse needs, first rung (antiderivative tower) done. MML.8 retains the pole
set, pole order, both Laurent coefficients, the HS × Yukawa factorization, the per-pole collapse
factor, (MML.11) a **concrete** pole family carrying all of term (II)'s data, and (MML.12) a
correctly-scoped target region; what is missing is the **real-space** identification of the summed
series with the inner-core value (matrix `OZFIX.12`/`OZFIX.22`), now under construction as MML.14 —
and by MML.12 that identification can only ever be established on `(λ₀₁, R₀₁)`, never on the inner
piece `(0, λ₀₁)`.

| Task | Title | Status | Route / Lean file |
|------|-------|--------|-----------|
| MML.1 | Explicit 2×2 adjugate/det formulas for Q̂₀: `adj(Q̂₀)₀₁=−Q̂₀₀₁`, `[Q̂₀⁻¹]₀₁=−Q̂₀₀₁/det(Q̂₀)` | ✓ DONE (axiom-clean): `adjugate_fin_two_zero_one`, `inv_zero_one_eq`, `Q0_det_fin_two`, `Q0inv_zero_one` | `HSMixture/MixtureHSPoles.lean` |
| MML.2 | B_k residue formula: `Res_{s=s_k}[Q̂₀⁻¹]₀₁ = −Q̂₀₀₁(s_k)/det′(Q̂₀)(s_k)` from `residue_of_simple_pole` + MML.1 | ✓ DONE (axiom-clean): `b_k_residue` (simple-zero data as hyps, matching `residue_of_simple_pole`) | `HSMixture/MixtureHSPoles.lean` |
| ~~MML.3~~ | ~~Full inner-DCF Mittag-Leffler assembly~~ — **RETIRED, superseded by MML.4–MML.8** (2026-07-16) | ✗ retired | — |
| MML.4 | HS-pole Mittag-Leffler *term* (II): `mixHSterm B_k s_k r = B_k·e^{−s_k r}`, `mixHS_series` (`2·Re`-of-tsum), Yukawa coupling `Σ_t K_t/(z_t²−s²)`, and the **Yukawa-coupled residue** tying a given `B_k` to MML.2 (`coupling(s_k)·(−Q̂₀₀₁/det′)`) | ✓ DONE (axiom-clean): `mixHSterm`, `mixHS_series`, `yukawaCoupling`, `yukawaCoupling_continuousAt`, `b_k_residue_coupled` | `YukawaOZMix/MixtureMLSeries.lean` |
| MML.5 | *(**RDF-only** — not on the DCF path, see Group MRS)* Convergence of the HS-pole sum `Σ_k 2·Re[B_k e^{−s_k r}]` | ✓ **CLOSED 2026-07-24, axiom-clean, no remainder.** abstract + reduction + growth-witness DONE (axiom-clean, 2026-07-16): `mixHS_summable`; `mixHS_summable_of_growth` (`MixtureMLSeries.lean`); `exists_zero_family_growth_of_chordPoleFamily` (`Analysis/BanachPoleFamily.lean`); `Q0_det_c_pole_family_growth` (`MixtureHSZeros.lean`). **✅ 2026-07-17 — the CONCRETE GATE is now PROVED** (axiom-clean, `HSMixture/MixtureChordFamily.lean`): `detF_family_magnitude_bound` gives `∃ g C p, p < −1 ∧ …` with `‖q01(g n)‖·e^{rdist·Re(g n)}/‖det′(g n)‖ ≤ C‖g n‖^p`, `p = max((σ₀−σ₁−2r)/σ₁, (−σ₀−2r)/σ₁)`, under **`rdist > max(σ₀/2, (σ₁−σ₀)/2)`** — plus `detF_zero_family_growth` (linear growth, disk-membership-aware). **⚠ Two caveats:** (1) the threshold's second branch is genuine (the `−σ₀/s` term of `q01`; binds iff `2σ₀<σ₁`) — numerically re-confirmed at σ=[0.8,2.3] where the naive `σ₀/2` threshold is FALSE; (2) the bound is stated for the **reflected** family (`s_k := −(g n)`, `Re s_k > 0`). **2026-07-17: `Summable` WIRED end-to-end** — `detF_mixHS_summable` (`HSMixture/MixtureMLBound.lean`, axiom-clean) gives `Summable (mixHSterm (fun n => −q01(g n)/derivF(g n)) (fun n => −g n) rdist)` via `mixHS_summable_of_growth` (free `Bcoef`/`sfam`, no `b_k_residue` hyps). **✅ 2026-07-24 — the last item (`Bcoef = b_k_residue`) CLOSED, axiom-clean:** `detF_family_magnitude_bound` strengthened with `∀ n, g n ≠ 0 ∧ det′(g n) ≠ 0` (no new analysis — both are strict consequences of the `disk_facts` it already used; the fix was *hoisting* those facts out of the per-`n` bullet so two conclusions can read them), and `detF_Bcoef_eq_b_k_residue` + `detF_eq_det_Q0` (`rfl`) prove `−q01(s_k)/det′(s_k) = Res_{s_k}[Q̂₀⁻¹]₀₁` via `b_k_residue`/`detF_hasDerivAt`/`q0_entry_c_differentiableAt`; `detF_mixHS_summable` now carries the identification as a third clause ⇒ one theorem gives family + residues + `Summable`. ⚠ lesson: the missing fact was already inside the old proof and merely unexported. Peer obligation MZERO.5: ✅ CLOSED 2026-07-17. See [proof_notes_mixture_rdf.md](proof_notes_mixture_rdf.md) MML.5 | `HSMixture/MixtureChordFamily.lean`, `MixtureMLSeries.lean`, `Analysis/BanachPoleFamily.lean` |
| MML.6 | Yukawa doubly-propagated base (I): `yukawaBaseAmp = [Q̂₀⁻¹K Q̂₀⁻ᵀ]₀₁`, real-space `yukawaBaseTerm = Σ_t amp_t·e^{z_t(R−r)}`, and its certification as the exact Y1.5 residue | ✓ DONE (axiom-clean): `yukawaBaseAmp`, `yukawaBaseTerm`, `yukawaBaseAmp_eq_spectralAmp_residue` (reuses `spectralAmp_residue`). Real-space `e^{z_t(R−r)}` envelope derivation = MML.8 | `YukawaOZMix/MixtureInnerDCF.lean` |
| MML.7 | Origin constant `p₀ = −(base+HS-sum)\|_{r→0}` | ✓ DONE (axiom-clean): `origin_constant_mix` (generic, via P.2 `origin_necessity` with `E:=base+hsum`), `origin_constant_eij_mix` (Yukawa specialization, `base:=eij`, continuity via `fun_prop`; gives `_precompute_p0`'s `−(E_ij(0)+Σ_k 2Re B_k)` form) | `YukawaOZMix/MixtureInnerDCF.lean` |
| MML.8 | **RDF-only** assembly/collapse (I)+(II)+(III). ⚠ **DCF premise REFUTED (2026-07-16)** — (★) `Ĉ₁=Q̂₀(−k)·B₁·Q̂₀ᵀ(−k)` carries no `Q̂₀⁻¹` ⇒ the inner **DCF** has **no HS poles**, so term (II) does not exist for it → **superseded by Group MRS**. Survives only as the **RDF `h₁`** assembly (`Ĥ₁=[Q̂₀ᵀ]⁻¹B₁[Q̂₀]⁻¹` *does* carry the inverses) | ◑ **RDF-scoped** (DCF part → MRS). **Crux #1 (pole order) DONE** (an RDF fact), collapse open — VERY HARD (N=2 matrix analog of scalar `hcollapse`). Pole-order lemmas (axiom-clean): `doubly_prop_entry_eq`, `double_pole_leading_coeff`(`_ne_zero`) ⇒ doubly-propagated = **double** poles (`r·e^{−s_k r}`); stated simple-exp form (II) ⟺ **singly**-propagated. **Term (II) doubly form + collapse reduction target now landed (2026-07-17, axiom-clean, build green):** `mixHSterm2`/`mixHS_series2` (the RDF `(α_k+β_k·r)e^{−s_k r}` form; `mixHSterm2_eq`/`mixHS_series2_eq` = singly `mixHSterm` at `r`-absorbed coeff ⇒ MML.4/5 API transfers), `mixHS_series2_summable` (non-vacuity from MML.5-concrete growth), `mixRDFInnerAssembly` + `MixRDFInnerCollapse` (the `Prop` reduction target = matrix analog of `CoreSeriesClosure`, deliberately NOT an axiom). **Both double-pole Laurent coefficients now certified (2026-07-18, axiom-clean):** order-2 `β_k` (`double_pole_leading_coeff`, `r`-prefactor) + order-1 `α_k = N′/E(s_k)²−2N(s_k)E′/E(s_k)³` (`double_pole_reg_hasDerivAt`/`double_pole_reg_eventuallyEq`/`double_pole_second_coeff`, via simple-zero factorization `D=(·−s_k)·E`; = `reg′(s_k)`, avoids ℂ-absent L'Hôpital) ⇒ real-space term (II) fully pinned by pole data. Only the bridge `assembly = r·h₁` remains. **Corrected: recommend fully-SINGLY for the DCF** (was fully-doubly); doubly = the RDF `h₁`, a different object with a different term-(II) form. **Collapse route (2026-07-17):** OZFIX Route B is CIRCULAR (`OZFIX.14`, value-neutral contour); realistic path = matrix real-space Baxter/Wertheim–Thiele — the scalar analytic core (`OZFIX.15–20`) is done but hits the pole-LHP⇔decay obstacle (`OZFIX.17`), whose matrix analog this collapse inherits. **`OZFIX.22` (2026-07-17) executed the scalar reverse-assembly** (retired `oz_core_closure`+`oz_h_exterior_regularity` to theorems via `oz_h=baxterPsi/·` + the isolated decay axiom `baxter_exterior_regularity`) = the exact matrix recipe MML.8 follows: construct → bounded exterior-cts matrix OZ fixed pt → matrix-uniqueness bridge → collapse from matrix OZ★; axiom-swap on the decay is now a blessed pattern (not fallback), and no decay-free route exists. Still needs **matrix `oz_fixed_pt_unique` + matrix OZ★** (Group MRS infra), then mechanical. **⇒ NARROWED 2026-07-24 by MML.9:** `Ĥ₁=S₀·Ĉ₁·S₀` with `Ĉ₁` HS-pole-free (MRS.3) and finite (MRS.5) ⇒ every RDF HS pole is contributed by the two `S₀=(I−Ĉ₀)⁻¹` factors, which carry **no Yukawa data** — so the matrix OZ★ still needed is a **hard-sphere** statement, and the Yukawa half is already closed by Group MRS. **Deps ALL DONE:** Y1.3 ✓, MML.4–7 ✓, MZERO.1 ✓ (`detC_zeros_infinite_unconditional`), MML.5-concrete ✓ (`detF_mixHS_summable`) ⇒ only the collapse itself remains. See [proof_notes_mixture_rdf.md](proof_notes_mixture_rdf.md) MML.8 | `YukawaOZMix/MixtureInnerDCF.lean` |
| MML.9 | **Structure-factor form of the first-order RDF**: `Ĥ₁ = S₀·Ĉ₁·S₀` (the RDF dual of MRS.2's `oz1_C1_eq`), HS-pole localization, and both term-(II) Laurent coefficients in terms of `det′`/`det″` alone | ✓ **DONE (2026-07-24, axiom-clean, full build green)** — new `YukawaOZMix/MixtureRDFStructureFactor.lean` (ns `FMSA.MixtureRDF`). `oz1_H1_eq` (from `hoz`+`hTS` only — *strictly weaker* hypotheses than `star_of_first_order_oz`: no MRS.1 factorization, no MRS.7 symmetry); `structureFactor_eq_inv` (`T₀⁻¹=S₀`, no invertibility hyp — `Matrix.inv_eq_right_inv`); `inv_conj_entry_eq` (transpose-free sibling of `doubly_prop_entry_eq`) ⇒ **`rdf_entry_eq_num_div_det_sq` = a second, independent proof of Crux #1** straight from the OZ equation, never mentioning `B₁`; `det_eq_of_wienerHopf_factorization` (`det T₀ = det Q̂₀(k)·det Q̂₀(−k)` ⇒ pole set is **reflection-symmetric**, matching `detF_mixHS_summable`'s `s_k := −(g n)` convention); `rdf_entry_star_eq` (capstone); `rdf_entry_differentiableAt` (`Fin 2`) = MRS.3's `star_entry_differentiableAt` **plus the single hypothesis `det T₀(k)≠0`** — that hypothesis *is* the DCF/RDF dividing line; `rdf_entry_double_pole` (order-2 pole at a simple `det T₀` zero, for the physical OZ pair). **Four-term dressing** (`S₀=I+Ĥ₀`): `structureFactorDressing`/`rdf_four_term_dressing` give `Ĥ₁ = Ĉ₁ + Ĥ₀Ĉ₁ + Ĉ₁Ĥ₀ + Ĥ₀Ĉ₁Ĥ₀`, and `rdf_sub_dcf_is_dressed` (`Ĥ₁−Ĉ₁ = the three Ĥ₀ terms`) locates **every** RDF HS pole in the three `Ĥ₀`-dressed terms (`Ĉ₁` HS-pole-free by MRS.3) ⇒ MML.8's collapse is term-by-term over `h₀`-dressed convolutions, `h₀` the only object still owing an ML collapse. **Cofactor-free Laurent coefficients:** `double_pole_second_coeff_deriv_form` gives `α_k = N′/D′²−N·D″/D′³` (and `β_k=N/D′²`) with the auxiliary `E` eliminated via `D′(s_k)=E(s_k)`, `D″(s_k)=2E′(s_k)` — ⚠ the `D″` step needs only **`ContinuousAt E′`**, not a 2nd derivative of `E`, because `(·−s_k)·g` has derivative `g(s_k)` for merely continuous `g` (`hasDerivAt_sub_mul_of_continuousAt`). Pitfalls: `fin_cases` leaves `⟨1,⋯⟩` ≠ numeral `1` for `rw` (use `Fin.forall_fin_two.2`); `field_simp` on the slope goal needs a trailing `ring`. See [proof_notes_mixture_rdf.md](proof_notes_mixture_rdf.md) MML.9 | `YukawaOZMix/MixtureRDFStructureFactor.lean` |
| MML.10 | Term (II)'s coefficient factored as **HS residue × (finite Yukawa DCF) × HS residue**, + the **mixture collapse factor** (matrix analog of the scalar `ρĈ(kₙ)=1`) | ✓ **DONE (2026-07-24, axiom-clean, full build green)**. (a) `hsResidueMatrix T₀ D′ := D′⁻¹ • adj T₀` is certified to be `Res_{s_k} S₀` entrywise (`structureFactor_entry_residue`, via `inv_apply_eq_adj_div_det` + `residue_of_simple_pole`) ⇒ **one `S₀` ⇒ one simple pole**, two ⇒ MML.9's order-2 pole; `num_div_det_sq_eq_sandwich` then gives `β_k = (R_k·Ĉ₁(s_k)·R_k)ᵢⱼ` (`rdf_double_pole_coeff_sandwich`), and with (★) `β_k = (R_k·Q̂₀(−s_k)·B₁(s_k)·Q̂₀ᵀ(−s_k)·R_k)ᵢⱼ` (`rdf_double_pole_coeff_star`) — HS spectral data outside, closed-form Yukawa data inside, so MML.9's narrowing is now an identity between numbers, not a reading. (b) **MML.8 Crux #2's "first concrete sub-piece" closed**: `hsResidue_annihilates` (`R_k·T₀(s_k)=T₀(s_k)·R_k=0`, from Mathlib `adjugate_mul`/`mul_adjugate` at `det=0`) ⇒ `hsResidue_eq_mul_cmix`: **`R_k = R_k·Ĉ₀(s_k) = Ĉ₀(s_k)·R_k`**, the literal matrix transcription of `ρĈ(kₙ)=1` (`N=1`: `R_k` cancels ⇒ `Ĉ₀(s_k)=1`). ⚠ the 2026-07-17 scoping called this expensive ("matrix WH factorization ABSENT from Lean") — MRS.6/MRS.7 landed it since, and defining `R_k` *through the adjugate* makes the collapse factor a one-liner. Pitfall: `rw [hT] at hL hR` also rewrites `T0` inside `hsResidueMatrix T0 D′` (desync; `linear_combination (norm := module)` then reports `⊢ 2 = 0`) — build `R*T₀ = R − R*C₀` as its own `have` and finish with `sub_eq_zero.mp`. See [proof_notes_mixture_rdf.md](proof_notes_mixture_rdf.md) MML.10 | `YukawaOZMix/MixtureRDFStructureFactor.lean` |
| MML.11 | **Concrete term-(II) data**: one explicit pole family carrying pole locations, the order-2 coefficient, and MML.5's summability | ✓ **DONE (2026-07-24, axiom-clean, full build green)** — new `YukawaOZMix/MixtureRDFPoleData.lean`. ⚠ **Instantiate the Y1.6 form, not MML.9's**: `Ĥ₁=S₀Ĉ₁S₀` has denominator `det T₀ = det Q̂₀(k)·det Q̂₀(−k)`, so concreteness there needs `det Q̂₀(−s_k)≠0` (a reflection-asymmetry fact nobody has proved and MZERO does not give); `Ĥ₁=[Q̂₀ᵀ]⁻¹B₁[Q̂₀]⁻¹` has denominator `det Q̂₀(k)²`, poles = the `det Q̂₀` zeros directly, simplicity = `det′≠0` — exactly what MML.5's strengthened family exports (**so MML.5's non-vanishing clause is MML.11's enabling hypothesis, not a tidy-up**). Chain: `hhat1_entry_eq_num_div_det_sq` (`Ĥ₁ = (adj Q̂₀)ᵀB₁ adj Q̂₀/(det Q̂₀)²`, via `Matrix.adjugate_transpose`) → `hhat1_double_pole` (order-2 pole, coefficient `β_k=(R̃ᵀB₁(s_k)R̃)ᵢⱼ`, `R̃=det′(s_k)⁻¹•adj Q̂₀(s_k)`) → **`q0Residue_zero_one`: `R̃ 0 1 = −q01(s_k)/det′(s_k)`** ⇒ MML.10's matrix residue and MML.2/MML.5's scalar residue are **one object** → `detF_rdf_pole_family` (one injective `g` with simple-zero triple + concrete `rdfBeta` at each pole + `Summable`). `detF_mixHS_summable` upgraded to export the simple-zero triple. Non-vacuous (`B₁` continuity is satisfiable; the coefficient clause states the limit's *value*, nonzero-ness is `double_pole_leading_coeff_ne_zero` separately). Pitfalls: Mathlib has **no** `continuousAt_finset_sum`/`ContinuousAt.fun_sum` — use `tendsto_finsetSum` (`_finset_sum` deprecated), which applies since `ContinuousAt` *is* the right `Tendsto`; after `Summable.congr` beta redexes `(fun n ↦ …) n` block `rw`, use `simp only [mixHSterm, q0Residue_zero_one]`. See [proof_notes_mixture_rdf.md](proof_notes_mixture_rdf.md) MML.11 | `YukawaOZMix/MixtureRDFPoleData.lean` |
| MML.12 | **The collapse REGION** — scoping correction + a permanent bound on the ML series' reach | ✓ **DONE (2026-07-24, axiom-clean, full build green)**. (1) `MixRDFInnerCollapse` quantifies `0 < r < R_ij`, but term (II) converges only for `r > max(σ₀/2,(σ₁−σ₀)/2)` (MML.5's gate). Below that, `tsum` of a non-summable family is Lean's junk `0` ⇒ the predicate degenerates to `base r + p0 = r·h₁(r)`, a different claim with no reason to hold. **This is the `Kterm` vacuity trap (MML.8 Crux #2 item 4) reaching MML.8 through the QUANTIFIER RANGE, not the summand** — which is why the "add antiderivatives until the exponent is `<−1`" check missed it. ⚠ not a disproof of the `(0,R)` form; its content below threshold is unsupported/junk-valued. Fix = `MixRDFInnerCollapseAnnulus`, whose region is **never empty** (`threshold_lt_contact`, both branches strict from `0<σ₁`/`0<σ₀`; witness `exists_mem_collapse_region`). (2) **Structural:** the threshold's second branch **is** the inner-core knot, `(σ₁−σ₀)/2 = Mix.lam 0 1 = λ₀₁`, so `λ₀₁ ≤ threshold` (`lam01_le_threshold`) and the collapse region sits inside the **outer** piece `(λ₀₁,R₀₁)` of the IB.4/MPOLY.5 split ⇒ **the HS-pole ML series can never certify the inner piece `(0,λ₀₁)`**, whatever happens to the real-space step; that piece is Group MRS's finite closed form. Consistency: `σ₀/2 ≥ λ₀₁ ⟺ 2σ₀ ≥ σ₁`, exactly MML.5's "second branch binds only when `2σ₀ < σ₁`". See [proof_notes_mixture_rdf.md](proof_notes_mixture_rdf.md) MML.12 | `YukawaOZMix/MixtureRDFPoleData.lean` |
| MML.13 | **`n=1` soundness bridge** for MML.9–MML.12 (the only mechanical test a consumer-less body of statements admits; MRS.0b's precedent) | ✓ **DONE (2026-07-24, axiom-clean, full build green) — all three checks PASS, no statement bug.** (1) collapse factor: MML.10's `R_k=R_k·Ĉ₀(s_k)` at `Fin 1` gives `Ĉ₀(s_k)=1` = the scalar `ρĈ(kₙ)=1` (`cmix_eq_one_fin_one`, via `hsResidueMatrix_fin_one`: `adj=1` ⇒ `R=det′⁻¹•I`). ⚠ **consistency check, NOT independent confirmation** — at `Fin 1` the conclusion is already forced by `det T₀(s_k)=0` alone (`cmix_eq_one_fin_one_of_det`); what it rules out is a sign/transposition/`det′`-power error, which would make the two routes disagree. (2) pole order: `hhat1_fin_one_entry` = scalar `B₁/(det Q̂₀)²`, a genuine double pole, matching Crux #1's `Q̂₀⁻¹`-count. (3) coefficient: `hhat1_double_pole_fin_one` — MML.11's sandwich `(R̃ᵀB₁R̃)ᵢⱼ` degenerates to the elementary `B₁(s_k)/det′(s_k)²`, **no matrix artifact survives**. **Non-vacuity certificate** recorded as a Lean `example` (`Q̂₀(z)=!![z−1]`, `s_k=1`, `det′=1≠0`, constant `B₁`) — this repo has shipped true-but-vacuous statements before (`b4_origin_bc_abstract`, `b9_d_ij_nonzero_example`), so the degenerate case is proved non-empty rather than assumed. Unlike MA.5/MA.2/clause-6a this bridge found **no** defect; recorded so it is not re-run. Does not advance the collapse. See [proof_notes_mixture_rdf.md](proof_notes_mixture_rdf.md) MML.13 | `YukawaOZMix/MixtureRDFPoleData.lean` |
| MML.14 | **Matrix real-space infrastructure I** — the antiderivative tower of term (II) (the first genuine building block of MML.8's collapse, mixture analog of `residue_term→Hterm→Kterm`) | ◑ **First rung DONE (2026-07-24, axiom-clean, full build green).** Key fact: the family `{(a+b·r)e^{−s·r}}` (`expLinTerm`) is **closed under antidifferentiation**, coeff map `antiCoeff s (a,b)=(−a/s−b/s², −b/s)` (`expLinTerm_antideriv_hasDerivAt`, `s≠0`) — no new shapes, so the whole tower stays inside `expLinTerm`. Two concrete rungs `mixHSAntideriv1`/`2` (matrix `Hterm`/`Kterm`) with the `HasDerivAt` chain `mixHSAntideriv2→mixHSAntideriv1→mixHSterm2`. Decay mechanism made precise: `expLinTerm_norm` (`‖(a+b·r)e^{−s·r}‖=‖a+b·r‖e^{−r·Re s}`) + reflected-family `≤1` bound + `antiCoeff` per-rung `1/‖s‖` gain (`antiCoeff_snd_norm`, `antiCoeff_fst_norm_le`) ⇒ two rungs = two spare `1/‖s‖` powers ⇒ should push summability exponent `<−1` **threshold-free** (the `Kterm` all-`u>0` analog, reaching the inner core, vs MML.5's `rdist>max(σ₀/2,λ₀₁)`). Summability reduced to one coeff bound: `mixHSAntideriv2_eq_mixHSterm` + `mixHSAntideriv2_summable_of_growth` (`Summable` from growth + `hbound` = `‖s_n‖^p`, `p<−1`). **✅ Rung 1 DONE (2026-07-24):** `mixHSAntideriv2_summable_of_coeff_bounds` — from a common `‖s_n‖^q` bound (`q<1`) on `α_n,β_n` + reflected `Re s_n≥0` + `‖s_n‖≥1` + growth, the `Kterm`-rung series is `Summable` at **every** `r≥0` (exponent `p=q−2<−1`, the two `antiCoeff` powers), **threshold-free** (no `rdist>max(σ₀/2,λ₀₁)`) — the mixture `Kterm_summable_of_pole_family`. Arithmetic done once: `mixHSAntideriv2Coeff_eq` (`α/s²+2β/s³+(β/s²)r`) + `mixHSAntideriv2Coeff_norm_le` (`≤(‖α‖+(2+r)‖β‖)/‖s‖²`, folding `1/‖s‖³→1/‖s‖²` via `‖s‖≥1`) + reflected `e^{−r Re s}≤1` + `Real.rpow_sub`. **Reduced to component bounds + crux validated (2026-07-24):** `coeff_div_sq_norm_le`/`coeff_alpha_norm_le` reduce α,β to `‖N‖,‖N′‖,‖N·det″‖ ≤ C‖s‖^q` (+ det′ const lower bound) via `mixHSAntideriv2_summable_of_component_bounds`; **`q<1` confirmed** — the live worry (B₁ carrying `e^{−ikR}` ⇒ growth ⇒ `q≥1` ⇒ MML.5 threshold reinstated) is **refuted**: the WH-projected B₁ = `bMulti = Σ c/(s+z)` is a pure-pole sum, no exponential (`e^{−ikR}` is in the *outer* `U₁`, absorbed by the causal projection), and **`bMulti_norm_le` (`SpectralAmplitude.lean`, axiom-clean) proves `‖B₁(s)‖ ≤ C·N²/‖s‖` — B₁ DECAYS**; with adj bounded + `det Q̂₀→1` (det′,det″ bounded) all three component bounds hold `q<1` ⇒ **tower genuinely threshold-free, approach validated**. **Numerator assembly DONE:** `triple_transpose_entry_norm_le` (`‖(Aᵀ·B·A)ᵢⱼ‖≤NN²Ca²Cb` from entry bounds, via `triple_entry_transpose_eq`) + `tower_summable_of_matrix_bounds` — assembles `‖N‖,‖N·det″‖≤C‖s‖^q` from adj entry bound + B₁ entry bound (`bMulti_norm_le`, q=−1) + det″ bound + det′ lower, concludes summability; only `N′` is an input. **Left (per-entry grind, no new structure):** the `N′` assembly (same triple-product pattern, needs adj′/`bMulti′` derivative bounds) + the standard `adj`/`det″` per-entry bounds on the explicit Baxter matrix (= mixture `residue_term_norm_bound`; `adj` is `q01_norm_le`-style, `det″` bounded from `det→1`) + cofinite→∀n wiring. **✅ Rung 3 DONE (2026-07-24):** the per-pole IBP/FTC toolkit on the proved `hasDerivAt` chain — `mixHSterm2_integral`/`mixHSAntideriv1_integral` (per-pole FTC on the two rungs, `∫mixHSterm2=[mixHSAntideriv1]`, `∫mixHSAntideriv1=[mixHSAntideriv2]`), `mixHSterm2_weighted_ibp` (`∫w·mixHSterm2=[w·A1]−∫w′·A1`, the derivative-off-term step), `mixHSterm2_integral_tsum` (termwise `∫∑=∑[A1]` via `integral_tsum`, `L¹`-hyp holds on the annulus by MML.5). This is the reduction's plumbing; the FULL matrix `oz_collapse_of_two_sigma_le` still needs the matrix OZ★ real-space convolution (unbuilt), which Rung 3 *consumes*. **✅ Rung 2 DONE (2026-07-24):** `MixCoreSeriesClosure alpha beta sfam anchor poly` — the `Kterm`-form first-order-remainder series `∑ₙ[mixHSAntideriv2(r)−mixHSAntideriv2(anchor)−(r−anchor)·mixHSAntideriv1(anchor)]=poly r` on `(0,anchor]` (the Taylor remainder of `mixHSAntideriv2` at anchor, `mixHSAntideriv2′=mixHSAntideriv1`); ⚠ `Kterm`-form not `Hterm` = MML.12 vacuity lesson at the series level; held as `Prop` not axiom = sub-family/pole-exhaustion trap. **Non-vacuity** `mixCoreSeriesClosure_summand_summable` (summand `Summable` from the three Rung-1 summabilities) + `mixHSAntideriv1_summable_of_coeff_bounds` (Hterm-rung, exponent `q−1`, needs `q<0`, RDF's `q≈−1` clears it) discharges the `mixHSAntideriv1` hyp. `poly` general `ℝ→ℂ`, concrete value from matrix OZ★. **◑ Matrix OZ★ STARTED (MML.15, 2026-07-24):** `HSMixture/MixtureOzStar.lean` — `matRadialConv` (entrywise `(A⋆B)ᵢⱼ=∑ₖ radial3d_conv(Aᵢₖ,Bₖⱼ)`, reduces matrix OZ conv to scalar `radial3d_conv` per entry w/ species coupling), `MatOZStar Ψ Φ ρ` predicate (`Ψᵢⱼ=r·Φᵢⱼ+ρ·r·(matRadialConv Φ (Ψ/·))ᵢⱼ`, `Prop`), `matOZStar_entry` (coupled `∑ₖ` entry eqn), **`matOZStar_fin_one_of_scalar` = n=1 bridge: `MatOZStar` at Fin 1 IS the proved scalar `baxterPsi_ozstar`** (validates target shape + non-vacuity witness). **◑ General-N construction STARTED (2026-07-24) — 2 foundational layers:** (1) OZ★ target shape above; (2) **real-space factorization** `matSelfConv` (`(Q⋆Qᵀ)ᵢⱼ=∑ₖ∫q0ᵢₖ(t)q0ⱼₖ(t−v)dt`, image of `Q̂₀(k)Q̂₀ᵀ(−k)`) + `MatBaxterFactorization` (matrix real-space KDEF `ρKᵢⱼ=qᵢⱼ−(matSelfConv q)ᵢⱼ`) + **`matBaxterFactorization_fin_one_of_scalar` = n=1 bridge to the proved scalar KDEF `rho_baxterK_eq_q0_self_conv`** (OZFIX.18). **Layer 3 (2026-07-24): shell-conv = K-conv bridge (OZFIX.19 analog)** — `matShellConv` (1D Baxter-kernel form `∑ₖ∫₀^σ Kᵢₖ(u)(oddExt Gₖⱼ(r−u)+oddExt Gₖⱼ(r+u))du`) + `matRadialConv_eq_matShellConv` (`r·matRadialConv=matShellConv` from per-(i,k) scalar identity `hentry`, pure `Finset.mul_sum` assembly — matrix version is scalar summed over species `k`, no new analysis) + **`matShellBridge_fin_one_of_scalar` = n=1 bridge to scalar `radial3d_conv_eq_baxterK_shell`**; connects layer 1 (OZ★ shape) to layer 2 (factorization). **general-`c` shell identity DONE (2026-07-24, `HardSphere/ShellKernel.lean`):** `shellKernel c σ v := 2π∫_|v|^σ s·c(s)ds` + `radial3d_conv_eq_shellKernel` (`r·radial3d_conv(c,g)=∫₀^σ shellKernel(c)(u)(oddExt g(r−u)+oddExt g(r+u))du` for **any** `c` supported in `[0,σ]`; transcription of scalar `radial3d_conv_eq_baxterK_shell` with `c_HS→c`/`baxterK→shellKernel`/`c_HS_outer→hc_outer`; `shellKernel_c_HS` = reduces to `baxterK`) — consumed by `matRadialConv_eq_matShellConv_of_shellKernel` (kernel built from entries `Kᵢₖ=shellKernel(C₀ᵢₖ)`, `hentry` discharged per entry) ⇒ **shell-bridge layer complete for arbitrary mixture entries**. **Layer 4 (2026-07-24): matrix `baxterPsi` construction shape** — `matBaxterPsi Ψouter Ψcore σ` (3-branch glued: outer/reflect/definitional-core) + `matBaxterPsi_core`/`_outer`/`_reflect` + `MatRenewalEq` (coupled Volterra `Ψouterᵢⱼ=Fᵢⱼ+∑ₖ∫_σ^r Qᵢₖ(r−t)Ψouterₖⱼ(t)dt`) + n=1 bridges `matBaxterPsi_fin_one_of_scalar`/`matRenewalEq_fin_one_of_scalar` (→ scalar `baxterPsi_core`/`baxterPsiOuter_spec`); `Ψouter` a parameter. **✅ Ψouter CONSTRUCTED (2026-07-24):** the mixture renewal `Ψ(r)=F(r)+∫_σ^r Q(r−t)·Ψ(t)dt` is a **matrix product** (`∑ₖ Qᵢₖ·Ψₖⱼ = (Q·Ψ)ᵢⱼ`), hence a Volterra eq on the complete normed ring `Matrix ℝ`. **`Analysis/VolterraBanach.lean` = Banach generalization of MA.10** (`volterra_convolution_existsUniqueE`: for any complete normed ring `E`, `u(r)=g(r)+∫_a^r q(r−t)·u(t)dt` has a unique continuous `E`-valued solution — identical iterate-contraction proof, only `|K·u|≤M|u|` → `‖q·u‖≤‖q‖‖u‖` via `norm_mul_le`; axiom-clean, mechanical); `matVolterra_convolution_existsUnique` instantiates it at `E=Matrix ℝ` (`Matrix.linftyOp` norm) ⇒ **unique continuous matrix `Ψouter`**. **✅ Entry-extraction DONE — matrix-norm plumbing SOLVED (2026-07-24):** the diamond (`ENormedAddMonoid (Matrix ℝ)` not synthesized under `letI`'d `Matrix.linftyOp` norm — default Matrix product topology competes with the norm's) is fixed by **one `letI : ENormedAddCommMonoid (Matrix ℝ) := NormedAddCommGroup.toENormedAddCommMonoid`** (pins the norm-induced topology). Three lemmas close it: `matrix_intervalIntegral_apply` (`(∫f)ᵢⱼ=∫fᵢⱼ` via entry CLM), `matrix_mul_intervalIntegral_entry` (`(∫Q·U)ᵢⱼ=∑ₖ∫Qᵢₖ·Uₖⱼ`, +`Matrix.mul_apply`+`integral_finsetSum`), **`matRenewalEq_of_matrixProduct` = end-to-end: matrix-product renewal IS `MatRenewalEq`** ⇒ `Ψouter` constructed in the exact ∑ₖ form. **✅ Matrix `F[K]=Ĉ` DONE (2026-07-24, OZFIX.18 F-part):** general-`c` version in `HardSphere/ShellKernel.lean` — `shellKernel_hasDerivAt` (`shellKernel(c)′=−2π·v·c(v)`, FTC-1, general-c `hasDerivAt_baxterK`) + `shellKernel_apply_sigma`/`_continuousOn` + `radial_fourier_eq_intervalIntegral_of_support` + `shellKernel_cos_eq_radial_fourier` (`2∫₀^σ shellKernel(c)·cos=radial_fourier(c)`, one IBP, boundary killed by `K(σ)=0`); `MixtureOzStar` `matShellKernel_cos_eq_radial_fourier` (per entry) + n=1 bridge to scalar `baxterK_cos_eq_radial_fourier`. **✅ Matrix 2D-reindex DONE (2026-07-27, OZFIX.20):** scalar `dbl_conv_reindex` uses same `q0` both factors; matrix `matSelfConv=∑ₖ Qᵢₖ⋆Qⱼₖ` couples DIFFERENT factors ⇒ genuine content = **two-function** reindex `dbl_conv_reindex_two` (`BaxterRenewal`, `∫q0a∫q0b·ψ(r+t−s)=∫_u[(∫_u^σ q0a·q0b(·−u))ψ(r+u)+(∫_u^σ q0b·q0a(·−u))ψ(r−u)]`, asymmetric triangle-split since factors don't commute across diagonal, 9 integrability hyps, collapses to scalar at q0a=q0b) + `matDblConv_reindex` (`MixtureOzStar`, sum over species k via `integral_finsetSum`+`Finset.sum_mul`, kernels `matSelfConv i j`/`j i` transpose) + n=1 bridge. **✅ Integrability side-conditions DONE (2026-07-27):** `MixtureOzStarIntegrable.lean` discharges all 9 per-species hyps of `matDblConv_reindex` from natural regularity (Q entries continuous + psi measurable+uIcc-bddOn = exactly `q0_poly`/`baxterPsi` regularity). Generic engine: `bddOn_uIcc_of_continuous`/`psiComp_bddOn` (compact bounds), `swap_indicator_integrable` (shared Fubini `(Ioi·).indicator` engine → `swapPM_integrable`/`swapA_integrable`), `constMul_psi_intervalIntegrable` (slices), `paramLo/Hi/paramKernel_mul_intervalIntegrable` (parametric inner integrals via `measurable_intervalIntegral_param`+`norm_integral_le_of_norm_le_const`); capstone `matDblConv_reindex_of_regular` = reindex conditional only on continuity+measurability/bddOn (matrix analog of unconditional `baxterPsi_ozstar`). **✅ Matrix OZ★ ASSEMBLED (2026-07-28, OZFIX.15/19/20, axiom-clean, NO decay axiom):** `matOzStar_of_shellClaims` derives `MatOZStar Ψ Φ ρ` from `hfact` (KDEF `MatBaxterFactorization`) + `hbridge` (`matRadialConv_eq_matShellConv`, OZFIX.19) + `hQint`/`hSint` (integrability, dischargeable) + `hclaimA` (matrix renewal in shell form, OZFIX.15 analog of `baxter_psi_conv_eq_phi`). Load-bearing algebra `matShellConv_kdef_split` (KDEF a.e. splits `ρ·matShellConv` → shell-Q − self-conv-shell); claimA gives `Ψ−r·Φ` = same shell expr ⇒ MatOZStar. n=1 = scalar `baxterPsi_ozstar` via existing `matOZStar_fin_one_of_scalar`. **Structural finding:** `matSelfConv(i,k)≠(k,i)`; reindex outputs asymmetric pairing, symmetric `matShellConv` aligns only at N=1/symmetric-Q; assembly sidesteps by taking claimA directly in shell form. **Remaining = produce concrete (Q,Kmat,Φ,Ψ) with hfact+hbridge+hclaimA + DECAY** = matrix Wiener-Hopf factorization of I−ρĈ=Q̂Q̂ᵀ with det Q̂ pole-free in LHP (OZFIX.17 pole↔decay obstacle, matrix analog of `baxter_no_open_lhp_pole_core`); isolable as 1 matrix axiom per OZFIX.22 if matrix arg-principle/Hurwitz out of reach. **✅ Matrix pole-freeness needs NO new axiom (2026-07-28):** `zeroFree_lowerHalfPlane_of_homotopy` is stated for ARBITRARY entire family H:ℝ→ℂ→ℂ ⇒ matrix `det Q̂≠0` LHP = same axiom at `H t z=(M t z).det`. `MatrixDetPoleFree.lean`: `differentiable_matrix_det`/`continuousOn_matrix_det` (entrywise reg ⇒ det reg via `det_apply'` Leibniz, no matrix normed-space) + `matDet_zeroFree_lowerHalfPlane_of_homotopy` (reduction, `#print axioms` = only the existing homotopy axiom). **Remaining = discharge geometric hyps for concrete `Q0_mat_c_phys`:** hreal=`pyhs_mixture_no_spinodal` (have it), hbase=dilute det→1, hbound=bounded LHP zeros; + the irreducible core = EXISTENCE of matrix WH factorization I−ρĈ=Q̂Q̂ᵀ. **✅ hbound ESCAPE MECHANISM DONE (2026-07-28, `MixtureDetEscape.lean`, no new axiom):** open-LHP-k ⟺ right-half-s (Re s≥0); via MML monomial bridge `s⁶detF=Mc+M₀e^{−sσ₀}+M₁e^{−sσ₁}+M₀₁e^{−s(σ₀+σ₁)}` (`detC_monomial_eq`): `exp_neg_mul_norm_le_one` (Re s≥0⇒‖e^{−sσ}‖≤1) + `Wfun_ne_zero_of_dominant`/`detF_ne_zero_of_dominant` (deg-6 Mc dominates M₀+M₁+M₀₁ ⇒ detF≠0). Matrix analog of scalar `exists_uniform_escape_radius`. **✅ hbound FULLY CLOSED (2026-07-28, `exists_escape_radius`, axiom-clean):** existing `MixtureChordFamily` bounds at M:=‖s‖ ⇒ `‖Mc‖≥‖s‖⁶−KMc‖s‖⁵` (McNum_sub_norm_le+reverse triangle) vs `‖M₀‖+‖M₁‖+‖M₀₁‖≤(K₀+μ+K₁+K₀₁)‖s‖⁴` ⇒ dominance for ‖s‖≥R:=max(1,KMc+K₀+μ+K₁+K₀₁+1) (nlinarith) ⇒ `∃R>0,∀s,Re s≥0→R≤‖s‖→detF s≠0`. **✅ DENSITY-HOMOTOPY SCAFFOLD BUILT (2026-07-28, `MixtureDetHomotopy.lean`, axiom-clean):** s=0 pole handled by running homotopy on ENTIRE `W=s⁶·detF` (`detC_monomial_eq`, W=0⇔detF=0 for s≠0) + scalar `zeroFree_lowerHalfPlane_of_homotopy` at `H t z=W(Pscale t P,I·z)`; convention s=I·z ⇒ Re s=−Im z. Bricks: `Pscale`(rr↦t·rr)+`Pscale_Phys`, `Wfun_Pscale_zero`(dilute W=s⁶)+`Wfun_dilute_ne_zero`(hbase =−z⁶≠0), `Wfun_ne_zero_of_norm_ge`(hbound z-form), `Wfun_comp_I_differentiable`(hholo via `Wfun_hasDerivAt`), `Wfun_Pscale_comp_I_continuous`(hcont via fun_prop). **✅ ASSEMBLY DONE (2026-07-28, conditional, no new axiom):** `Wfun_zeroFree_of_hreal`/`detF_zeroFree_of_hreal` wire hcont/hholo/hbound/hbase into scalar `zeroFree_lowerHalfPlane_of_homotopy` at `H t z=W(Pscale t P,I·z)` ⇒ `detF(I·z)≠0` for Im z<0, conditional on hRunif(uniform R)+hreal; `#print axioms`=std+existing homotopy axiom. **STRUCTURAL FINDING:** Pscale (linear rr) is WRONG family for hreal — `pyhs_mixture_no_spinodal` holds for any physical density (etaMix<1) ⇒ discharges hreal along PHYSICAL density path (ρ↦t·ρ, etaMix(t·ρ)=t·etaMix(ρ)<1) but Pscale scales rr linearly (not a physical (σ,ρ') point, PY ties rr/Qp/Qpp nonlinearly). **✅ PHYSICAL DENSITY PATH BUILT (2026-07-28, `MixtureDetHomotopyPhys.lean`, hreal k≠0 done, no new axiom):** `Pdens σ ρ t`=MixParams at density t·ρ (rhoGeoPhys(t·ρ) linear in t, Q0phys/Qppphys vary via vac ⇒ genuine physical state unlike Pscale); `detF_Pdens_eq`(bridge detF(Pdens)=det Q̂₀-phys(σ,t·ρ), axiom-clean) + `etaMix_smul`(η(t·ρ)=t·η(ρ)) + `detF_Pdens_ne_zero`(**hreal k≠0** from existing `pyhs_mixture_no_spinodal`, HS spinodal-free ∀density). **REMAINING SUBTLETY:** run homotopy on ENTIRE detF not W (W(P₀,0)=0 origin zero breaks hreal@z=0; scalar used 1−Q̂ entire/nonzero@origin); detF entire = q0_entry_c removable singularity@0. **✅ detF ENTIRE DONE (2026-07-28, `MixtureDetEntire.lean`, axiom-clean):** Lean detF 0/0-junk@0 but analytic away (`Q0_det_c_differentiableAt`) + finite removable limit (`detC_tendsto` from existing `q0_entry_c_tendsto` φ₁(0)=−σ²/2 φ₂(0)=σ³/6); `detF_update_differentiable`=`update detF 0 c` ENTIRE via Riemann `analyticAt_of_differentiable_on_punctured_nhds_of_continuousAt` (punctured 𝓝 diff since update=detF off 0; ContinuousAt@0 via `nhdsNE_sup_pure`+limit) + `detF_ext_differentiable` packages. Matrix analog of scalar `1−Q̂`. **✅ DILUTE BASE DONE (2026-07-28):** `Pdens_zero_rr`(rr(Pdens 0)=0 since rhoGeoPhys(0·ρ)=√0=0)+`Q0_mat_c_rho_geo_zero`(Q0_mat_c@0-coupling=I)⇒`detF_Pdens_zero`(detF(Pdens 0)≡1)+`detF_ext_Pdens_zero`(hbase: update ≡1). **PRECISE REMAINING for concrete pole-freeness (no new axiom):** hholo=`detF_ext_differentiable`✓, hbase=`detF_ext_Pdens_zero`✓, hreal(k≠0)=`detF_Pdens_ne_zero`✓, hbound=lift `exists_escape_radius` to detF_ext✓(mechanical); **✅ hreal@z=0 origin value c_t≠0 CLOSED (2026-07-28, AXIOM-CLEAN, no physics axiom!):** `Q0MomentGeOne.lean`+`MixtureDetOrigin.lean`. Route 1: `moment_key` gives `ad−bc≥a+d` ⇒ `det=1−(a+d)+(ad−bc)≥1` (`Q0_moment_det_ge_one`/`Q0_mat_phys_det_ge_one`, strict-with-margin) — survives z→0 limit: `detF_Pdens_ofReal`(detF(Pdens t,z)=det Q0_mat_phys(z,t·ρ) at real z) + det≥1 ⇒ `Re(c)=lim≥1>0` (`ge_of_tendsto`) ⇒ **c≠0** (`detF_Pdens_origin_ne_zero`; hreal@z=0=`detF_ext_Pdens_origin_ne_zero`). `#print axioms`=std 3 — k=0 compressibility positivity PROVED (moment_key algebra), NOT a companion axiom, doesn't even use `pyhs_mixture_no_spinodal`. Bonus: real removable p1→−σ²/2, p2→σ³/6. **✅ CONCRETE POLE-FREENESS THEOREM ASSEMBLED (2026-07-28, `MixtureDetOrigin.lean`, no new axiom):** `mixtureDet_pole_free_of_regular` = `detF(Pdens σ ρ 1, I·z)≠0` on open LHP (Im z<0) for physical mixture; homotopy on entire detF_ext via `zeroFree_lowerHalfPlane_of_homotopy`, ALL pointwise inputs discharged (hholo=`detF_update_differentiable`, hbase=dilute≡1, hreal k≠0=`Pdens_detF_imag_ne_zero` no-spinodal, hreal k=0=`cf_ne_zero` axiom-clean compressibility, hbound=uniform escape); `#print axioms`=ONLY existing `zeroFree_lowerHalfPlane_of_homotopy`+`pyhs_mixture_no_spinodal`, NO new axiom. Conditional only on 2 FAMILY-REGULARITY hyps: hcont + hRunif. **PROGRESS 2026-07-28:** `detF_ne_zero_of_Kbound` (`MixtureDetEscape`, axiom-clean) = uniform escape mechanism for hRunif (K_Mc,K₀,μ,K₁,K₀₁≤B ⇒ detF≠0 @ ‖s‖≥max(1,5B+1)); `vacMix`/`xi2` continuous on [0,1] + vac>0. **✅ hRunif DONE (2026-07-29, `MixtureDetUniform.lean`, axiom-clean):** `hRunif_Pdens` = single R=max(1,5B+1) ∀t∈[0,1]; 5 K-constants continuous in t (`Ksum_cont`: vacMix/xi2 fun_prop, vac(t)>0 ⇒ Q0phys/Qppphys=…/vac ContinuousOn.div, K=poly in fields) ⇒ compact-bounded B (`isCompact_Icc.exists_bound_of_continuousOn`); `Pdens_Phys`(t>0 via Q0phys_pos/Qppphys_pos/rhoGeoPhys_pos)+`detF_ne_zero_of_Kbound`; t=0 dilute≡1. **✅ hcont DONE ⇒ UNCONDITIONAL (2026-07-29, `MixtureDetHcont.lean`):** 0/0-junk only from φ₁,φ₂=num/sⁿ (depend on s/σ NOT t) ⇒ continuous removable ext `psi1`/`psi2` (via `phi{1,2}_tendsto`+`nhdsNE_sup_pure`) ⇒ ψ-form det `detFpsi` MANIFESTLY jointly continuous (`detFpsi_continuous`) + `=detF` off 0 (`detFpsi_eq_detF`); `cf t=detFpsi(Pdens t) 0` (`cf_eq_detFpsi_zero`) ⇒ detF_ext=detFpsi ⇒ hcont by `ContinuousOn.congr` (`hcont_Pdens`); t-independence of φ sidesteps parametrised-removable-singularity. **`mixtureDet_pole_free` = detF(Pdens 1,I·z)≠0 open LHP physical N=2 mixture, FULLY UNCONDITIONAL**; `#print axioms`=ONLY existing 2 (`zeroFree_lowerHalfPlane_of_homotopy`+`pyhs_mixture_no_spinodal`). **OZFIX.17 matrix pole↔decay RESOLVED for N=2, no new axiom.** ANSWER "need new MATH axiom?": NO** — pole-freeness reuses `zeroFree_lowerHalfPlane_of_homotopy`, hreal=`pyhs_mixture_no_spinodal`, hbase=`Q0_mat_c_at_zero` det=1, hbound mechanism proved, factorization explicit `Q0_mat_phys_eq_one_sub_mul`. Remaining = poly asymptotics (‖Mc‖≥c‖s‖⁶, ‖M₀/M₁/M₀₁‖≤C‖s‖⁵) + homotopy scaffold + s=0 pole = TRANSCRIPTION not axioms. **Remaining overall:** Rung 1 per-entry grind (N′ + std adj/det″); matrix OZ★ general-N core (the last unbuilt piece). Pitfalls: `once-/twice` closes a `/-! -/` docstring early (`-/`) — write `once- or twice-`; `∀ n, …(n:ℝ)…sfam n` infers `n:ℝ` — annotate `∀ n:ℕ`; `Complex.norm_real` gives `‖r‖` real not `|r|` (+`Real.norm_eq_abs`); **enorm `‖·‖ₑ` needs `open ENNReal`**; docstring line starting `axiom` trips the `^axiom` ledger grep. See [proof_notes_mixture_rdf.md](proof_notes_mixture_rdf.md) MML.14, MML.15 | `YukawaOZMix/MixtureRDFAntideriv.lean`, `HSMixture/MixtureOzStar.lean` |
| MML.16 | **RDF parity partner annihilated exactly** — the one-sided-vs-sine ("even pseudo-trace") question | ✓ DONE (2026-08-03, axiom-clean): Tang & Lu 1995 pp. 91–94 **checked against the original** — they keep `H̃=[Ĥ(−k)−Ĥ(k)]/(ik)` and write the partners `Q̂₀ᵀ(k)Ĥ₁(−k)Q̂₀(k)`, `−[Q̂₀(−k)]⁻¹Ĉ₁(−k)[Q̂₀ᵀ(−k)]⁻¹` explicitly in Eq. (10); support analysis kills them **exactly** (not asymptotically). Lean: `reflected_support_Iic`, `reflected_rdf_conj_support`, `antiCausal_triple_support`, `parity_partners_causal_projection_zero`, `causal_projection_with_reflection`(+`_open`), non-vacuity `example` with `Wrefl ≠ 0`. ⚠ **[LN] deviates here** (`H̃₁=Ĥ₁` is false in its own normalization; `eq:rearranged` lost the partners) — use the original | `YukawaOZMix/ReflectedTermSupports.lean` |

### Group MRS — Mixture Real-Space Baxter Route to the Inner DCF *(mixture_dcf)*

**The DCF route**, replacing MML.8's refuted premise. Pivot: **(★)** `Ĉ₁(k) = Q̂₀(−k)·B₁(k)·Q̂₀ᵀ(−k)`
— an algebraic consequence of [LN] eq:OZ1_Baxter (a) + `Hhat1_spec` (b) that **contains no `Q̂₀⁻¹`**.
`Q̂₀` entire ⇒ `Ĉ₁` has **only Yukawa poles** ⇒ the `det Q̂₀` zeros (Group MZERO) **never enter the
DCF**, and the inner DCF is a **finite closed form** (polynomial + finitely many exponentials),
piecewise at the support-overlap knots (`λ_ij` first — exactly Group IB's set). Numerics: (★) to
**4.4×10⁻¹³**; the finite-basis fit split at `λ_01` drops the residual ~1000× (1.9×10⁻³ → 1.3×10⁻⁶,
then at the quadrature noise floor). **MZERO / MML.5-concrete are NOT needed here** — they stay
load-bearing for the RDF `h₁` only. **The inverses are the DCF/RDF dividing line.**
Source: `todo/to_Lean.md` §1–§2 (2026-07-16 numerical session). See
[proof_notes_mixture_dcf.md](proof_notes_mixture_dcf.md) Group MRS.

| Task | Title | Status | Lean file |
|------|-------|--------|-----------|
| ~~MRS.1~~ | ~~Matrix WH factorization `Q̂₀(k)·Q̂₀ᵀ(−k) = I − Ĉ₀(k)`~~ — **DECOMPOSED (2026-07-17) into MRS.6/MRS.7/MRS.8** (pure-numeric) | ✗ retired → MRS.6–8 | — |
| MRS.6 | *(ex-MRS.1a)* WH factorization **frame**: define `Ĉ₀ := I − Q̂₀(k)·Q̂₀ᵀ(−k)`; discharge MRS.2's `hfact` for free; **reduce** `hT0symm` to the swap identity `Q̂₀(−k)Q̂₀ᵀ(k) = Q̂₀(k)Q̂₀ᵀ(−k)` | ✓ **DONE (2026-07-17, axiom-clean)** — `MixtureRealSpace.lean` (ns `FMSA.MRS`): `Cmix0`, `Cmix0_factorization` (hfact), `Cmix0_transpose`, `Cmix0_isSymm_iff` (symmetry ⟺ swap), `T0_isSymm_of_swap` (⇒ `hT0symm`). | `YukawaOZMix/MixtureRealSpace.lean` + `fin2_transpose_eq_iff_offdiag`/`T0_isSymm_iff_offdiag` (N=2 symmetry ⟺ single off-diagonal scalar; diagonal automatic) ⇒ **`hT0symm` fully reduced to one scalar identity = MRS.7**. | `YukawaOZMix/MixtureRealSpace.lean` |
| MRS.7 | *(ex-MRS.1b)* the **swap identity** `Q̂₀(−k)Q̂₀ᵀ(k) = Q̂₀(k)Q̂₀ᵀ(−k)` for the physical `Q0_mat_c` — discharges `hT0symm` | ✓ **DONE (2026-07-17, axiom-clean)** — `MixtureRealSpace.lean`. **`Qphys_T0_isSymm`**: `T₀=Q̂₀(k)Q̂₀ᵀ(−k)` symmetric for the physical matrix. Chain: KEY 1/2 (`Q0phys_key_relation`/`Qppphys_key_relation`) + **`swap_offdiag_of_keys`** (coefficient-free structural swap under 4 minimal hyps, proved via `E₀=e^{σ₀k/2}`/`E₁=e^{σ₁k/2}` atoms + `field_simp`+`ring`) + physical instantiation (discharge 4 hyps via casts + `√(ρᵢρᵢ)=ρᵢ`). **hT0symm fully closed.** | `YukawaOZMix/MixtureRealSpace.lean` |
| MRS.8 | *(ex-MRS.1c)* validate `Ĉ₀ = I−Q̂₀(k)Q̂₀ᵀ(−k)` **is** the physical HS DCF transform (behind MRS.2's OZ hyps) | ☐ **deferred + deprioritized (2026-07-17)**. **Search: a mixture PY DCF `Ĉ₀` does NOT exist in Lean** (only scalar `c_HS`+transforms; `cHS_FMT` is the FMT DCF; rank-2 `Q0_mat_phys=I−UV` is a factorization of Q̂₀ itself, not the WH product). **NOT on the critical path**: the finite-form payoff needs only (★)+`Q̂₀` entire, not `Ĉ₀`; `hTS` is definitional, `hoz` is the OZ axiom. Build only for an unconditional statement. **⚠ Status changed 2026-07-31 — WORK IN PROGRESS, do not re-audit as dormant.** New `YukawaOZMix/MixtureHSDCF.lean` (untracked, wired into `LeanCode.lean:98`, builds clean, axiom-clean) supplies the zeroth-order **support backbone** out of the MRS machinery: `qFwd` (forward Baxter window — the genuinely new piece), `qpConv = qFwd ⋆ pMixEntry`, `cHSmixRaw = qFwd + pMixEntry − Σ_l qpConv` with support `⊆ [−R_ij, R_ij]` (intermediate species cancels, as in `bConvP`). **The transform identity `Cmix0 = I − Q̂₀(k)Q̂₀ᵀ(−k)` ⟺ physical HS DCF — MRS.8's actual statement — is still not proved**, so the 2026-07-17 "does not exist in Lean" verdict holds for the identity while being obsolete as a description of the effort. Sibling starts the same week: `HSMixture/MixtureBaxterSeed.lean` (matrix `Ψ⋆Q₊`, OZ★/RDF track), `Analysis/MatrixWienerHopf.lean`. | `YukawaOZMix/MixtureHSDCF.lean` (new) |
| MRS.2 | [LN] eq:OZ1_Baxter (a) `Q̂₀ᵀ(k)Ĥ₁Q̂₀(k) = [Q̂₀(−k)]⁻¹Ĉ₁[Q̂₀ᵀ(−k)]⁻¹`; **physical route:** derive (★) from first-order OZ + MRS.1 | ◑ **backbone DONE (2026-07-17, axiom-clean)** — `MixtureRealSpace.lean`: `oz1_C1_eq` (`Ĥ₁T₀=S₀Ĉ₁`+`T₀S₀=I` ⇒ `Ĉ₁=T₀Ĥ₁T₀`) + **`star_of_first_order_oz`** ((★) from 5 named matrix identities: OZ `hoz`/`hTS`, MRS.1 `hfact`, `Ĉ₀`-symmetry `hT0symm`, `Hhat1_spec` `hB1`). **Pins the exact conventions** the algebra needs (OZ form `Ĥ₁T₀=S₀Ĉ₁`, factor order `Q̂₀(k)Q̂₀ᵀ(−k)`, `Ĉ₀` symmetric). ⚠ **"Remaining = the concrete factorization = MRS.1" is stale (fixed 2026-07-31)** — MRS.1 was retired into MRS.6/7/8, and MRS.6/MRS.7 discharged `hfact` and `hT0symm` the same day. Of the five hypotheses, **four are now discharged or proved** (`hfact` MRS.6, `hT0symm` MRS.7, `hB1` = Y1.6 `Hhat1_spec`, `hTS` definitional); the one genuinely left is **`hoz`, the first-order OZ equation itself** — [LN]'s physical starting point, whose identification with the real HS DCF is MRS.8 (off the critical path). | `YukawaOZMix/MixtureRealSpace.lean` |
| MRS.3 | **(★)** `Ĉ₁ = Q̂₀(−k)·B₁·Q̂₀ᵀ(−k)` from (a)+(b) — pure algebra; **corollary: only Yukawa poles, no HS poles** | ✓ **DONE (2026-07-17, axiom-clean)** — `YukawaOZMix/MixtureRealSpace.lean` (ns `FMSA.MRS`): `star_of_oz1_baxter` (the algebra: `Qm⁻¹C₁(Qmᵀ)⁻¹ = B₁` ⇒ `C₁ = Qm·B₁·Qmᵀ`), `star_of_oz1_baxter_hhat1` (equates (a) with **(b) = Y1.6 `Hhat1_spec`**, so (b) is a *proved* input, not an assumption), `star_entry_eq` + **`star_entry_differentiableAt`** (the load-bearing corollary: every `Ĉ₁` entry is differentiable wherever `Q̂₀(−·)`,`B₁` are — **with no hypothesis on `det Q̂₀`**, since (★) has no inverse ⇒ MZERO's zeros impose no singularity on the DCF). **Conditional only on (a) = MRS.2**, taken as a hypothesis. **+ concrete corollary `starMix_entry_differentiableAt` (2026-07-17)**: the no-HS-poles fact for the *actual* `Q0_mat_c` mixture matrix (differentiability of `Ĉ₁` entries off `k=0`, no `det Q̂₀` hyp; via `q0_entry_c_differentiableAt`∘neg). |
| MRS.4 | Real-space `q_ij` quadratic + the `λ_ij` delta in `q'_ij` (amplitude `−σᵢσⱼ/(2Δ)`, ξ₂ cancels) + its symmetry ⇒ delta-cancellation | ◑ **jump-value core DONE (2026-07-17, axiom-clean, general `N`)** — `q0Mix_jump_amplitude` (`=−πσᵢσⱼ/vac`=`−σᵢσⱼ/(2Δ)`, **ξ₂ cancels**), `q0Mix_jump_amplitude_symm` (i↔j-symmetric ⇒ cancellation), `lam_sub_R_eq`, `q0Mix_quad_at_R` (delta is one-sided). The δ **distribution** reframing is interpretation (needs distribution theory Mathlib lacks); the jump-value content — what the cancellation actually needs — is proved, sidestepping distributions. **⚠ audit 2026-07-31: `◑` is BY CHOICE, not blockage** — the delta is a hazard of Baxter's *differentiated* relation, and **MRS.5 never forms `q'`** (it convolves the undifferentiated `𝒬⁻ = δ − q⁻`, so the only kernels are an `Icc`-windowed quadratic and an `Ici`-windowed exp). The section header had said "☐ not started", contradicting its own Lean block. | `YukawaOZMix/MixtureRealSpace.lean` |
| MRS.5 | Convolution `𝒞 = 𝒬⁻ ⋆ ℬ ⋆ (𝒬⁻)ᵀ` ⇒ **finite closed form**, piecewise at the knots | ✅ **CLOSED, EXPLICIT, GENERAL `N`, AXIOM-FREE (audited 2026-07-31).** Three layers: *atoms* `MixtureRealSpace.lean` (`integral_poly_exp_conv`/`_linear_`/**`_quadratic_`**, FTC poly⋆exp=poly+exp); *engine* `MixtureClosedForm.lean` (the convolution ⇄ interval-integral bridges `indicator_ici/icc_conv_indicator_icc/ici` + `indicator_icc_conv_general`; sign-paired atoms; `bConvP_closed_form`(+`_outer`), `pConvB_closed_form`, `pbpConv_eq_intervalIntegral`, `intervalIntegral_P_expQuadClosed`, `integral_quad_quadReflected`, `oddPart_reduction`); *general-`N` assembly* **`MixtureDCFSmooth.lean`** — **`pbpConvG_eq` + `outerHalfG_eq`/`alignedHalfG_eq`/`midHalfG_eq`/`neg_halfG_eq` (all four regions of `P⋆ℬ⋆P` at `(i m n j : Fin N)`, each `= Σ_q outerPerPoleVal`/`alignedPerPoleVal`, both explicit poly·exp with NO residual integral) + `Wmix` (the 4-term double sum over species) + `dcfOdd`**. Integrability gate discharged piecewise (`pbpIntegrand_intervalIntegrable`), not assumed. Capstones = IB.9's `dcfOdd_contDiffOn_{upper,lower,like}`. **⚠ The (i)–(iv) "remaining" list in the proof note was closed 2026-07-19 under the IB.9 banner and nobody updated the MRS record — see the audit box in [mixture_dcf](proof_notes_mixture_dcf.md).** ⚠ **Axiom attribution was also wrong**: the *only* MRS declarations touching an axiom are `fourier_kernel_finite_poles`/`innerDCF_finite_closed_form` on the **abandoned k-space route** (and the axiom is **MA.1**'s `halfDiskBoundary_eq_sum_of_small_circles`, not "MA.3's"). Python oracle `fmsa_double_prop.py` `_build_closed_form` stays the independent per-piece checker (outer=MSA tail 1e-14, N=1 inner=Tang 1e-9, quad atom=scipy.quad 1e-15). **Not** produced: a single displayed `r·c^{(1)}_ij(r) = …` identity (the values live per-region; contraction is transcription, and `MixtureDCFSmooth` consumes the pieces directly). | `MixtureRealSpace.lean`, `MixtureConvolution.lean`, `MixtureClosedForm.lean`, **`MixtureDCFSmooth.lean`** |
| MRS.5 note | *(historical — the 2026-07-17 audit called MRS.5 "the single blocker for a concrete `c1_inner`".)* The object is `c1_inner_ij(r) = [𝒲(r)−𝒲(−r)]/(2π√(ρᵢρⱼ)r)`, `𝒲 = 𝒬⁻ ⋆ ℬ ⋆ (𝒬⁻)ᵀ`, with 𝒬⁻ entry `q0MixEntry` (`WHSupports.lean`), knots `Mix.lam`/`Mix.R`, and the `2π√(ρᵢρⱼ)r` prefactor literal. Sub-`def`s landed 2026-07-17 (`MixtureConvolution.lean`: `bMixEntry`, `pMixEntry`, `bConvP` + support lemmas); the **4-term `𝒲` and its odd part** landed 2026-07-19 as `Wmix`/`dcfOdd` (`MixtureDCFSmooth.lean`). **Blocker retired.** | ✅ **superseded by the MRS.5 row above** | → [mixture_dcf](proof_notes_mixture_dcf.md) Group MRS · `YukawaOZMix/MixtureConvolution.lean`, `MixtureDCFSmooth.lean` |
| MRS.9 | **`HSMixture/` library reorganization** (not DCF math — the layered-directory work that placing the `MRS.0` axiom triggered; consolidates ex-`MRS.0c/0d/0e/0f`). `9a` create `HSMixture/` layer + layering invariant; `9b` split straddling files → `Analysis/` (5 new abstract files); `9c` species binder → `N`; `9d` split `MixturePolyCoeffs` + move `MixtureLaurent` + rename `mixHS*`. | ✓ **DONE — 9a–9c 2026-07-19, 9d 2026-07-27** (build 8699, layering re-verified, axiom ledger byte-identical). `9d`: extracted `p1_limit`/`p2_limit`/`p2_quartic_coeff` → `Analysis/Taylor4Calculus`, moved `MixtureLaurent` → `HSMixture/`, renamed the 4 generic `mixHS*` in `Analysis/PoleSeriesSummable` → `poleExp*` (derived `mixHSterm2`/`detF_mixHS_summable` kept). The 2026-07-19 six-lemma back-edge list was stale — only `p1_limit`/`p2_limit` were actually needed. | → [mixture_rdf](proof_notes_mixture_rdf.md) `### Task MRS.9` (record co-located with its history, not in `mixture_dcf`) · `LeanCode/HSMixture/`, `LeanCode/Analysis/` |

### Group MZERO — Mixture `det(Q̂₀)` Zero Family (HS-pole existence) *(mixture_rdf)*

**⚠ Scope (2026-07-16): RDF-only.** (★) (Group MRS) shows the inner **DCF** has no HS poles, so this
group is **not** on the DCF path. It stays load-bearing for the **RDF `h₁`**, whose
`Ĥ₁=[Q̂₀ᵀ]⁻¹·B₁·[Q̂₀]⁻¹` *does* carry the inverses ⇒ genuine HS poles.

**MZERO.2–MZERO.11 decompose MZERO.1** (`det(Q̂₀)` has ∞ many complex zeros; foundation done,
`MixtureHSZeros.lean`) into two independent routes — **Route A** Banach (MZERO.2–MZERO.7) and
**Route B** Rouché/Jensen (MZERO.8–MZERO.11); either route alone closes MZERO.1. The N=2
`det(Q̂₀)` is an exponential polynomial in `s` (quasi-polynomial) — the analog of `Qhat_complex`
for N=1 (POLE.3 `Qhat_complex_zeros_infinite`). See [proof_notes_mixture_rdf.md](proof_notes_mixture_rdf.md) Group MZERO.

**✅ GROUP CLOSED — audited 2026-07-24.** All of MZERO.1–MZERO.11 are ✓, and the audit was
mechanical, not a re-read of statuses: every named deliverable was `#print axioms`-checked and each
returns the standard three only. Checked constants — `detC_zeros_infinite_unconditional`,
`chordPoleFamily_detC_exists` (MZERO.1/5); `chord_zero_exists_of_bounds`, `chordPhi_fixedPt_iff`,
`chordPhi_lipschitzOnWith`, `mapsTo_closedBall_of_lipschitzOnWith_of_dist_le` (MZERO.3/4/6, all in
`FMSA.BanachPoleFamily`, **not** `FMSA.MixtureHSPoles` — the generic Banach layer was migrated to
`Analysis/`); `Q0_det_c_zeros_infinite`, `Q0_det_c_pole_family_growth` (MZERO.7);
`det_meromorphicOn` (MZERO.8); `det_divisor_nonneg`, `detC_jensen_log_bound` (MZERO.9);
`detBoundaryGrowth_of_linear` (`FMSA.BoundaryGrowth`), `detC_boundaryGrowth_iff_infinite_zeros`
(MZERO.10); `detC_zeros_infinite_of_boundaryGrowth` (MZERO.11). No open item remains in this group.

✅ **RE-AUDITED 2026-07-31 — holds; three findings.** All **20** MZERO constants re-measured,
`#print axioms` = standard three (nothing shifted in `be70ac4` "General N" or the MRS.9d shuffle).
(1) **Machinery vs headline split.** `detF_family_magnitude_bound` is **load-bearing** (consumed by
`MixtureMLBound`/MML.5, `MixtureInnerDCF`/MML.8, `MixtureRDFPoleData`/MML.11) and the generic
`ChordPoleFamily`/`zeros_infinite_of_chordPoleFamily` layer is **reused by the scalar track**
(`HardSphere/BaxterChordFamily.lean`, `BaxterPoles.lean`) — the `Analysis/` migration paid off beyond
the mixture. But the **`Set.Infinite` headlines of both routes have NO Lean consumer** (all hits are
docstrings): MRS.3 removed the DCF consumer (`det Q̂₀` zeros never enter `Ĉ₁`) and the only remaining
one, MML.8, is open. Proved-and-unconsumed is expected here, not a defect — but it is the hazard class
MRS.0b was invented for, so record it. (2) ⚠ **`MixParams.Phys` is a naming trap** — it means "ordered
positive diameters + entrywise positive matrices", **not** the Lebowitz PY coefficients;
`Q0phys`/`Qppphys`/`rhoGeoPhys` appear in **none** of the three MZERO files, so `hP : P.Phys` must not
be read as "instantiated at the physical mixture". (3) **The physical bridge is available and
favourable but unwritten** — `Q0phys`/`Qppphys` are manifestly entrywise positive for `vac>0, σ>0,
ρ≥0`; checked at the reference mixture (η=0.4549) `min Q0phys = 19.03`, `min Qppphys = 26.53`, and the
zeros were located at those **physical** coefficients (8 zeros, `|detC| ≤ 3.4e-15`, mean Im spacing
**3.098** vs predicted `2π/σ₁ = 3.1416`, reproducing MZERO.2's GO gate). ⇒ **MZERO.12 written, see
the row below.**

| Task | Title | Status | Route / Lean file |
|------|-------|--------|-----------|
| MZERO.1 | Infinitely many HS poles for N=2: `det(Q̂₀(s))=0` has infinitely many complex roots (analog of `POLE.3`) | ✓ **DONE (2026-07-17) — CLOSED via MZERO.5**: `detC_zeros_infinite_unconditional` (`HSMixture/MixtureChordFamily.lean`, axiom-clean, parameter-only hypotheses `0<σ₀<σ₁`, `rr/Qp/Qpp>0`). Foundation (earlier, axiom-clean): `Q0_det_c_tendsto_one`, `q0_diag_c_tendsto_one`, `q0_offdiag_prod_tendsto_zero`, `Q0_det_c_not_identically_zero`, holomorphy `Q0_det_c_differentiableAt` (s≠0). Route A (Banach/chord) was the genuine route; Route B (Jensen) is a reformulation (`detC_boundaryGrowth_iff_infinite_zeros`) | `HSMixture/MixtureChordFamily.lean`, `MixtureHSZeros.lean` |
| MZERO.2 | *(Python, POLE.2 analog — GO/NO-GO gate)* feasibility | ✓ **GO** (`verify_mixture_hs_poles.py`): quasi-periodic zero family (Δ Im≈π, 22 zeros to Im≈239), chord-Newton `g=s−F/F'(s1)` on `r=0.15` — both Banach conds hold uniformly, **K≈0.30–0.35 (does NOT drift to 1)**, self-map gap≪r(1−K). Banach path viable w/ chord-Newton | Banach; `verify_mixture_hs_poles.py` |
| MZERO.3 | generic chord-Newton Banach wrapper (Lipschitz self-map ⇒ ∃ zero); map-independent | ✓ DONE (axiom-clean): `chord_zero_exists_of_bounds` (via `ContractingWith.exists_fixedPoint'`) | Banach; **`Analysis/BanachPoleFamily.lean`** (ns `FMSA.BanachPoleFamily`; migrated out of `MixtureHSZeros.lean` by MRS.9b) |
| MZERO.4 | chord map `s↦s−F s/Fp1` + `fp ⟺ F=0` (unconditional; simpler than log-map, no branch-safety) | ✓ DONE (axiom-clean): `chordPhi`, `chordPhi_fixedPt_iff` | Banach; **`Analysis/BanachPoleFamily.lean`** (ns `FMSA.BanachPoleFamily`) |
| MZERO.5 | magnitude bounds: `‖1−det′(s)/det′(s₁)‖ ≤ K` (chord-Lipschitz) + self-map `hstep` (`‖det(s₁)/Fp1‖ ≤ r(1−K)`) | ✓ **DONE (2026-07-17) — CLOSED**, axiom-clean, no sorry, full build green: `chordPoleFamily_detC_exists` + **`detC_zeros_infinite_unconditional`** (`HSMixture/MixtureChordFamily.lean`, 3497 lines) — hypotheses = parameter positivity/ordering ONLY (`0<σ₀<σ₁`, `rr/Qp/Qpp > 0`) ⇒ **MZERO.1 CLOSED too**. 2-freq transposition of POLE.9: monomial polynomialisation `s⁶detC = McNum + M0Num e^{−sσ₀} + M1Num e^{−sσ₁} + M01Num e^{−s(σ₀+σ₁)}`, anchor `i·2πn/σ₁` with exact σ₁-phase kill, derived guess `sGuess = −(1/σ₁)log t + i·2πn/σ₁` (**negative** log-lift), `rc = 1/(20σ₁)`, `K = 3/4`. Retires the `hbound`/`hstep` hypotheses of `Q0_det_c_zeros_infinite`. See [proof_notes_mixture_rdf.md](proof_notes_mixture_rdf.md) MZERO.5. | `HSMixture/MixtureChordFamily.lean` |
| MZERO.6 | `LipschitzOnWith K (chordPhi F Fp1) ball` (from `HasDerivAt`+MZERO.5 bound) + `MapsTo` disk-into-itself | ✓ DONE (axiom-clean): `chordPhi_lipschitzOnWith` (`Convex.lipschitzOnWith_of_nnnorm_deriv_le`), `mapsTo_closedBall_of_lipschitzOnWith_of_dist_le` | Banach; **`Analysis/BanachPoleFamily.lean`** (ns `FMSA.BanachPoleFamily`) |
| MZERO.7 | single-`n` (`det_zero_exists_for_n`) → Im-spaced family (`Q0_det_c_pole_family_exists`) → distinctness ⇒ `{s:det=0}` infinite | ✓ DONE (axiom-clean): `Q0_det_c_zeros_infinite` (`Set.Infinite`, via `Set.infinite_of_injective_forall_mem`) — the ∀-hypothesis layer, formerly conditional on MZERO.5. **2026-07-17: MZERO.5 CLOSED**, and the unconditional conclusion is now available directly as `detC_zeros_infinite_unconditional` (`MixtureChordFamily.lean`, parameter-only hypotheses) | Banach; `MixtureHSZeros.lean`, `MixtureChordFamily.lean` |
| MZERO.8 | `det(Q̂₀)` meromorphic (for Jensen) | ✓ **DONE** (`det_meromorphicAt`/`det_meromorphicOn`): meromorphic algebra (`φ=entire/sⁿ` ratio) + `fun_prop` — **no continuation needed**, s=0 hard part dissolves | Rouché; `MixtureHSZeros.lean` |
| MZERO.9 | `divisor det ≥ 0` + Jensen-counting bound (`hJensen`) | ✓ **`divisor det U ≥ 0` now UNCONDITIONAL** — `det_divisor_nonneg` (axiom-clean). The "det has a limit at `0`" hyp of `det_divisor_nonneg_of_tendsto` is discharged by the Baxter removable values `φ₁(0)=−σ²/2`, `φ₂(0)=σ³/6` (`phi1_tendsto`/`phi2_tendsto`, from exp-Taylor `(e^w−1−w)/w²→½` `expTaylor2`, `(e^w−1−w−w²/2)/w³→⅙` `expTaylor3`) → `q0_entry_c_tendsto` → `detC_tendsto`. **These `s=0` Taylor coeffs are reusable for the inner-core poly / numerics (cf. MPOLY, GAP.9).** **`hJensen` NOW PROVED** (2026-07-16, axiom-clean): `detC_jensen_log_bound` cites abstract MA.5 `circleAverage_log_norm_le_of_finite_zeros` + the `detC` bridge (finite literal zeros ⇒ finite divisor support via analyticity off `s=0`). **⇒ MZERO.9 fully DONE** | Rouché; `MixtureHSCounting.lean` |
| MZERO.10 | boundary growth → hypothesis | ✓ `DetBoundaryGrowth` + `detBoundaryGrowth_of_linear` (a `≥c·R` estimate ⟹ it, via `isLittleO_log_id`). Axiom-clean. **⚠ EQUIVALENT to MZERO.1 (2026-07-16, PROVED `detC_boundaryGrowth_iff_infinite_zeros`, axiom-clean, `0<σᵢ`):** via Jensen `circleAverage(log‖detC‖) 0 R = ∫n(t)/t dt+const`, super-log growth ⟺ ∞-many zeros. `⟹` = `detC_boundaryGrowth_of_infinite_zeros` (Jensen lower bound + identity-theorem non-vanishing). **So Route B is a Jensen *reformulation* of MZERO.1, NOT an independent closure** — the `e^{−sσ}`-growth reading was imprecise (zero-free exp has avg 0; growth is all from zeros). Genuine route = Route A (MZERO.5). | Rouché; `HSMixture/MixtureHSCounting.lean` + **`Analysis/BoundaryGrowth.lean`** (`DetBoundaryGrowth`, `detBoundaryGrowth_of_linear`, ns `FMSA.BoundaryGrowth`) |
| MZERO.11 | Jensen capstone ⇒ ∞ many zeros | ✓ **structural capstone** `infinite_zeros_of_growth` + `detC_zeros_infinite_of_growth` (indep. Route-B proof of `Set.Infinite {detC=0}`, matches Route A). Axiom-clean; reuses `det_meromorphicOn` (MZERO.8). **With MZERO.9's `hJensen` now proved, `detC_zeros_infinite_of_boundaryGrowth` gives the infinitude modulo ONLY `DetBoundaryGrowth` (MZERO.10)** — the hJensen hypothesis is discharged | Rouché; `HSMixture/MixtureHSCounting.lean` + **`Analysis/BoundaryGrowth.lean`** (`infinite_zeros_of_growth`) |
| **MZERO.12** *(new 2026-07-31)* | **The PHYSICAL instantiation** — MZERO.1/5 at the Lebowitz PY coefficients, not merely at an entrywise-positive parameter pack | ✓ **DONE, axiom-clean, build green (8716)** — new **`HSMixture/MixtureZerosPhys.lean`** (a *new file*, so `MixtureChordFamily.lean` and the rest of the MML.8 working set are untouched). `Pdens_one` (`Pdens sigma rho 1` **is** the physical pack, `1•ρ=ρ`) → **`detC_zeros_infinite_phys`** (`0<σᵢ`, `σ₀<σ₁`, `0<ρᵢ`, `η<1` ⇒ `{s \| detC ![σ₀,σ₁] ↑rhoGeoPhys ↑Q0phys ↑Qppphys s = 0}.Infinite`) → **`Q0_mat_c_phys_det_zeros_infinite`** (same on `Q0_mat_c`, i.e. **definitionally MRS.7's `Qphys`** — the form a consumer wants), plus a **non-vacuity `example`** at the reference mixture `σ=[1,2]`, `ρ=[0.03475,0.10425]` (`η≈0.4549`; the `η<1` step needs only `π<4`). ⚠ **It proves nothing new, and that is the finding:** `Q0phys_pos`/`Qppphys_pos`/`rhoGeoPhys_pos` — and even the packaged **`Pdens_Phys`** — were **already theorems** in `MixtureDetUniform.lean`, built for the density homotopy of the physics-axiom-retirement track (consumer `hRunif_Pdens`) and **never pointed at `detF_zeros_infinite`**. The whole corollary is `Pdens_Phys … 1` + `one_smul`. A first draft re-proved them and the build caught it as a name clash (`environment already contains 'FMSA.MixtureHSPoles.Qppphys_pos'`) — `feedback_stale_blockers` in pure form: **two tracks had each done half and neither knew.** Layering keeps the statement at `Q0_mat_c` level (`HSMixture/` may not import `YukawaDCF/`); an `FMSA.MRS` restatement on the literal `Qphys` is a one-liner. | `HSMixture/MixtureZerosPhys.lean` |


### Group MPOLY — Mixture Inner-Core Polynomial Coefficients *(mixture_dcf)*

*(optional)* Faithful inner-core polynomial coefficients `D_ij = R'_ij(0)/6` (and A,B,C,E⁴) via the
inside-core Laplace remainder `R_ij(s) = s⁵[e^{sR}·S_ij(s) − Y_ij(s)]` ([LN] §9.4, Eqs 106–120) —
promotes **GAP.9 Option B** (cubic-coefficient *mechanism*, DONE) to the exact `R'_ij(0)/6` identity.
Decomposed into MPOLY.1–MPOLY.5; **MPOLY.1–MPOLY.3 DONE** (2026-07-16,
axiom-clean — the order-4 Taylor calculus + `Δ_Q` + inverse entry). **MPOLY.4–MPOLY.5 ❌ NOT COMPLETABLE —
falsified 2026-07-17:** [LN] Eq (101)'s single deg-≤4 `P_ij` holds only for **like** pairs; for **unlike**
pairs the inner core **splits at `λ_ij`** (`fmsa_double_prop` closed form: `(0,λ)` deg 1 / `(λ,R)` deg 4), so
the target unlike `D_01` does not exist (rigidity `not_single_poly_of_pieces_ne`, IB.4). A second, independent
contradiction: the shipped `Ĉ₁=Q̂₀(−k)B₁(k)Q̂₀ᵀ(−k)` has **no `Q̂₀⁻¹` and no HS poles**, against the MPOLY
umbrella's "`S_ij` involves `Q̂₀⁻¹`". **What survives** = the generic order-4 Taylor machinery (MPOLY.1–3) +
`taylor4_coeff_unique`, reusable off Eq (101). Correct path for the piecewise inner core = **Group MRS**
(real-space Baxter, `MRS.5`). See [proof_notes_mixture_dcf.md](proof_notes_mixture_dcf.md) Group MPOLY.

✅ **AUDITED 2026-07-31 — group CLOSED, three corrections.** (1) **Falsification re-verified exactly on
the current tree**, no fitting: reading the analytic pieces and forming `f = 𝒲(r)−𝒲(−r)` symbolically
gives `+1.1115·r` on `(0,λ₀₁)` (deg 1, zero constant — both `±r` land in the same `[−λ,+λ]` piece so
`f` is odd there) versus `−1.2813 + 1.1890r + 0.2029r² + 0.0447r⁴` on `(λ₀₁,R₀₁)` (deg 4, nonzero
constant, `r³`/`r⁵` absent); max coeff difference **1.2813** ⇒ no single `Q`. **Digit-for-digit
identical to the 2026-07-17 record**, so `b3896eb`/"General N" did not invalidate it; like pair `(0,0)`
(`λ=0`, one piece) confirms Eq (101) is correct there. ⚠ **Epistemic status:** the *rigidity* is a Lean
theorem, the *premise* `P₁≠P₂` is a machine computation from the validated closed form — a disproof by
counterexample, not a Lean disproof. (2) **All file paths in the MPOLY notes are stale — MRS.9d moved
this code 2026-07-27.** `HSMixture/MixtureLaurent.lean` **no longer exists**: the order-4 Taylor calculus
(incl. both survivors `taylor4_coeff_unique`/`poly4_eq_zero_of_littleO`) is now **`Analysis/Taylor4Calculus.lean`**
(ns **`FMSA.Taylor4`**), only `q0_entry_taylor4`/`taylor4_inv_entry` remain in **`HSMixture/MixtureLaurent.lean`**,
and `MixturePolyCoeffs.lean`'s namespace is **`FMSA.MixturePoly`**. **That move upgraded "what survives"
from mixture leftovers into a general-purpose `Analysis/` library.** (3) `#print axioms` on all 18
MPOLY-related declarations = **standard three**; GAP.8's vacuity re-confirmed by reading the proof term
(`⟨…, rfl×5⟩`, `hR : AnalyticAt` unused).

| Task | Title | Status | Route / Lean file |
|------|-------|--------|-----------|
| MPOLY.1 | *(MPOLY pipeline)* order-4 Taylor of `q0_entry` = `δ − ρ·Ep₄·Pp₄` (the 5-coeff `q_ij(s)=q^[0]+…+q^[4]s⁴` structure, [LN] Eq 134) | ✓ DONE (axiom-clean, re-verified 2026-07-31): `q0_entry_taylor4` + `p1_quartic_coeff`(`−σ⁶/720`), `p2_quartic_coeff`(`σ⁷/5040`), `exp_neg_quartic_rem`; extends GAP.9's `q0_entry_taylor3` by one order (`Real.exp_bound`) | `HSMixture/MixtureLaurent.lean` (`q0_entry_taylor4`) + `Analysis/Taylor4Calculus.lean` (the `p*` lemmas) |
| MPOLY.2 | *(MPOLY pipeline, ✓ re-verified 2026-07-31)* reciprocal series `1/Δ_Q`: `δ₀=1/d₀`, `δₙ=−(1/d₀)Σₘ dₘδₙ₋ₘ` ([LN] Eq 136–137) | ✓ DONE (2026-07-16, axiom-clean): `taylor4_recip` — `Tendsto` convention, `rₖ` as the Eq-137 hyps; cleared convolution `e0..e4` + tail identity `1−P₄Q₄=z⁵(c₅+…)` via `linear_combination (-1)*e0+(-z)*e1+…`. **Route note: `PowerSeries.inv` is a TRAP** (no `FormalMultilinearSeries.inv`/`HasFPowerSeriesAt.inv`; `AnalyticAt.inv` coefficient-free; 0 `PowerSeries` uses in `LeanCode/`). Numerically pre-checked (exact rational + 60-dp δ₅) | **`Analysis/Taylor4Calculus.lean`** (ns `FMSA.Taylor4`; moved by MRS.9d 2026-07-27 — the note's `MixtureLaurent.lean` is stale) |
| MPOLY.3 | *(MPOLY pipeline, ✓ re-verified 2026-07-31)* `Δ_Q=q₁₁q₂₂−q₁₂q₂₁` order-4 coeffs (from MPOLY.1) + `[Q̂₀⁻¹]₀₁=−q₁₂·(1/Δ_Q)` order-4 series ([LN] Eq 130; **analytic at 0** — `1/s⁵` pole is in `S_ij`, not here) | ✓ DONE (2026-07-16, axiom-clean): order-4 Taylor **calculus** `taylor4_tendsto_const`/`taylor4_mul` (Cauchy product)/`taylor4_sub`/`taylor4_neg`, then `taylor4_deltaQ` (Eq 131/135) + `taylor4_inv_entry` (Eq 130) as pure compositions. **Deps MPOLY.1, MPOLY.2 — NOT MML.1** (MML.1 is over ℂ, the MPOLY Taylor line is over ℝ; Eq 131 is stated algebraically over abstract `q_ij`, sidestepping the seam). `d₀≠0` stays a hypothesis (`norm_num` per-params, cf. `dij_cubic_nonzero`) | **`Analysis/Taylor4Calculus.lean`** (the `taylor4_*` calculus) + **`HSMixture/MixtureLaurent.lean`** (`taylor4_inv_entry`) |
| MPOLY.4 | *(MPOLY pipeline)* Laurent→coeff machinery (Eq 105/120) | ❌ **NOT COMPLETABLE (2026-07-17)** — the target does not exist. (a) It was to "extend GAP.8", but **GAP.8 is vacuous** (`∃` closed by 5 `rfl`s; its `AnalyticAt` hypothesis is unused) ⇒ nothing to inherit. (b) More fundamentally: [LN] Eq (101)'s **single** deg-≤4 polynomial `P_ij` is **falsified for unlike pairs** (see MPOLY.5) ⇒ there is no `P_ij` to extract. **Survives**: the generic order-4 Taylor calculus (MPOLY.2/3) + `taylor4_coeff_unique`/`poly4_eq_zero_of_littleO`, proved, axiom-clean, and **now abstracted into `Analysis/Taylor4Calculus.lean` as project-independent Taylor arithmetic** (MRS.9d) | ~~`MixtureLaurent.lean`~~ → survivors in `Analysis/Taylor4Calculus.lean` |
| MPOLY.5 | *(MPOLY pipeline, ex-CRUX)* exact `S_01` ⇒ `D_01=R'_01(0)/6` | ❌ **NOT COMPLETABLE (2026-07-17)** — **premise falsified**. Decisive evidence (structural, not a fit): reading the shipped, validated `fmsa_double_prop` closed form (2YK) shows the **unlike** pair (0,1) inner core **splits at λ_ij**, with different rate-0 polynomials on the two sides — `(0,λ)`: `f_poly = 1.111·r` (degree 1, zero constant ✓ [LN] Eq 102 origin regularity); `(λ,R)`: `−1.281 + 1.189r + 0.203r² + 0.0447r⁴` (degree 4, nonzero constant). Like pairs (λ=0) have a single piece ⇒ **Eq (101) holds for like, fails for unlike**. MPOLY's target is precisely the unlike `D_01` ⇒ it does not exist. Rigidity proof: `not_single_poly_of_pieces_ne` (IB.4). Also: the shipped identity `Ĉ₁=Q̂₀(−k)B₁(k)Q̂₀ᵀ(−k)` has **no Q̂₀⁻¹ and no HS poles**, contradicting the MPOLY umbrella's "`S_ij` involves `Q̂₀⁻¹`" (HS poles belong only to the RDF `ĥ₁`). **Re-verified exactly 2026-07-31** — symbolic odd part of the current closed form gives `+1.1115r` vs `−1.2813+1.1890r+0.2029r²+0.0447r⁴`, digit-for-digit as recorded, `‖ΔP‖_∞ = 1.2813`; like pair `(0,0)` single-piece ⇒ Eq (101) holds there | ~~new `YukawaDCF/…`~~ |

### Group PYE — No-Pull-Back Construction ≡ First-Order PY *(closures)*

The `pullback_passes=0` YK-tail FMSA-DP construction **is** first-order PY; the only error vs true
first-order PY is the finite-Yukawa-tail re-approximation of the outer closure `g_HS·(−βu)`. Sits on
Groups Y1 (DP linearity) + MRS (finite closed form). Source: `hncb_first_order.md`, `HNCB_FMSA_dp.py`.
See [proof_notes_closures.md](proof_notes_closures.md) Group PYE.

| Task | Title | Status | Lean file |
|------|-------|--------|-----------|
| PYE.1 | PY closure to O(coupling): outer `c₁ = g_HS·(−βu)` (`y₀=g_HS`) | ✓ DONE (2026-07-31, axiom-clean): `py_first_order_outer`, `cavity_zero_coupling`, `py_first_order_outer_cavity`, `msa_first_order_outer`; **+ the γ₁-collapse** `pass_zero_eq_py_first_order` (with `hncbClosure`, `hncb_first_order_outer`, `hncb_outer_zeroth_order_eq_zero`) | `Closures/ClosureExpansions.lean` |
| PYE.2 | DP first-order solution map **linear** in the outer closure (corollary of Y1 `bMulti`/`Ĉ₁` linearity) | ✓ DONE (2026-07-31, axiom-clean): `b1OfCouplingLinear` (Y1.5 `bMulti` linear in `K`) ∘ `starConjLinear` ((★), MRS.3) = `dpDCFLinear`, fixed-rate family `dpTailsLinear`; stage 1 `outerTransform_add/_smul`. ⚠ needs the rates **frozen** — free rates sit in `s + z_mp` and the map is not linear | `Closures/ClosureExpansions.lean` |
| PYE.3 ⭐ | **DCF error = DP-map[finite-tail fit residual]** ⇒ "only error = the YK re-approximation"; needs `wh_solution_operator_bounded` *(recommended first target)* | ✓ DONE (2026-07-31, axiom-clean): `dp_error_eq_dp_of_residual` (= `map_sub`), physical form `dp_sub_py_first_order_eq_conj_residual` (via MRS.2 `star_of_first_order_oz`), headline `dp_error_entry_norm_le`. **`wh_solution_operator_bounded` is a THEOREM, not a new axiom** — explicit `whOperatorBound = T·N⁴·Qb²·Ab²/δ` | `Closures/ClosureExpansions.lean` |
| PYE.4 | Abstract equivalence `DP-map[g_HS·(−βu)] = OZ+PY first order` (needs WH map lifted to an L¹/L² function-space closure) | ◑ partial (2026-07-31, axiom-clean): `dp_eq_py_first_order_of_exact_fit` + `dpDCF_entry_tendsto`/`dp_tendsto_py_first_order`; the lift itself is carried by the `Prop` `TailFitWHConvergent` (**not** an axiom) — see the Open-tasks row | `Closures/ClosureExpansions.lean` |
| PYE.5 | **`k → r` bridge** — k-space `Ĉ₁` identity ⇒ real-space `c₁(r)` on the core `(0,R)` | ✓ DONE (2026-08-03, axiom-clean): `radial_fourier_injective` (on the project's own `radial_inversion`, a theorem not an axiom), dictionary `IsRadialEntry`, `inner_core_dcf_eq_of_khat_eq`, capstone `inner_core_dcf_eq_of_exact_fit` + structural non-vacuity `example`. ⚠ open core only (contact jump), and **no real-space error bound** (`k·Ĉ₁(k) ∉ L¹` under a k-uniform bound) | `Closures/ClosureExpansions.lean` |
| PYE.6 | the zeroth-order factor **is** the PY hard-sphere Baxter factor | ✓ DONE at `N = 1` (2026-08-03, axiom-clean) — **the library already had it**: BAXTER.3 `baxter_wiener_hopf_complex` (= MRS.8 at `N=1`) + `pyhs_no_spinodal` + the MRS.0b chain `Q0phys_n1`/`Qppphys_n1`/`qhat_complex_eq_mixture_kernel`. Wired in as `pyBaxterMat`/`pyT0Mat`/`pyT0Mat_entry`/`pyT0Mat_isUnit_det`/`dp_zeroth_order_is_py_n1`/`dp_eq_py_first_order_n1` ⇒ `hfact`+`hT0symm`+`hTS` discharged with the physical PY objects, only `hoz` left. **General `N` = MRS.8, open** | `Closures/ClosureExpansions.lean` |
| PYE.7 | density of finite Yukawa-tail fits ⇒ PYE.3's residual → 0 on the outer window | ✓ DONE (2026-08-03, axiom-clean): `exists_yukawa_tail_fit` (+ `_pyOuter` for the actual target `r·g_HS·(−βu)`). **Route: Weierstrass after `t = e^{−r}`, NOT Stone-Weierstrass** — polynomials in `e^{−r}` *are* exponential sums, so no adjoin/span extraction; one `δ`-shift makes the `k=0` constant a legal tail. Rates come out **unit-spaced** (third independent argument for fixed separated rates). ⚠ **does not close PYE.4**: window not half-line, and function residual ≠ WH-datum distance | `Closures/ClosureExpansions.lean` |

### Group HNCB — First-Order HNCB Bridge Pull-Back Construction *(closures)*

The whole **fixed-rate** first-order HNCB (`'solve'`) = closure expansion + finite `(I−L)⁻¹` + DP map
+ linear OZ apply — finite linear algebra, the finite-tail truncation the only approximation. Exact
self-consistent HNCB-1 is the **RDF** ⇒ HS poles ⇒ not finite-closed-form (HNCB.4, boundary). See
[proof_notes_closures.md](proof_notes_closures.md) Group HNCB.

| Task | Title | Status | Lean file |
|------|-------|--------|-----------|
| HNCB.1 | HNCB closure to O(coupling): outer `c₁ = −βu + B·h₁`, `B=h_HS/g_HS=1−1/g_HS` | ✓ **DONE (2026-08-03, axiom-clean)**. `exp`-form half (PYE.1 by-products): `hncbClosure`, `hncb_outer_zeroth_order_eq_zero`, **`hncb_first_order_outer`**, `pass_zero_eq_py_first_order`. Audit additions: `hncb_base_point_gamma_eq` (the two hypotheses **force `γ₀ = g_HS−1 = h_HS`**) + non-vacuity `example`. **Pivot (2026-08-03)**: `bridgeCoeff` (`B = 1−1/g_HS`), **`hncb_contractive_pivot`** (`c₁ = −βu + B·h₁`, needing exactly the OZ relation `γ₁ = h₁−c₁` and `g_HS ≠ 0` — `h_HS = g_HS−1` is *not* an extra hypothesis, it is already `hncb_first_order_outer`'s `γ₁` coefficient), **`hncb_first_order_outer_contractive`** (derivative level — the form HNCB.2/3 iterate). **The `B<1` remark is now a theorem pair**: `bridgeCoeff_mem_Ico` (`g_HS ≥ 1 ⇒ B ∈ [0,1)`, contracts) vs `exp_form_feedback_ge_one` (`g_HS ≥ 2 ⇒ h_HS ≥ 1`, does not) — i.e. iterating the `exp` form is Picard-unstable **exactly where the theory is used** (`g_HS` at contact ≳ 2). Cross-checked numerically: the two forms agree to **7.1e-15** over 1e5 random `(g_HS, βu, γ₁)` ⇒ an identity, no sign/factor slip. | `Closures/ClosureExpansions.lean` |
| HNCB.2 | Fixed-rate pull-back map **affine** `K̃'=a+L·K̃` (reuses Y1 `Ĥ₁`) | ☐ not started | new `…Closures.lean` |
| HNCB.3 | Fixed point `K̃*=(I−L)⁻¹a`; `(I−L)` invertible (finite matrix) | ☐ not started | new `…Closures.lean` |
| HNCB.4 | *(record only)* Exact HNCB-1 carries the RDF `h₁` ⇒ HS poles ⇒ not finite-closed-form (MZERO/MML.5 wall) | ☐ **boundary marker, NOT a to-do** — nothing to prove; the `☐` reads like unstarted work and is not | — |

### Group GA — FMSA_GA_matrix_mix Inner-Core Conditioning Failure *(failures)*

| Task | Title | Status | Lean file |
|------|-------|--------|-----------|
| GA.1 | Unlike-pair two-exp base unbounded `K·exp(z·R)→∞`; additive HS-pole sum (≤ C/z²) cannot cancel it | ✓ DONE, axiom-clean — `unlike_pair_twoexp_unbounded` + `hs_pole_additive_insufficient` + `residue_propagator_bound` (the last **discharges** the `hB` bound structurally: HS-only `A_k,s_k` are `z`-independent ⇒ `‖B_k‖ ≤ (4/3)·C·K/z²` for `2‖s_k‖≤z`; **no open hypothesis**) | `FMSAPoly/PolyApproxFails.lean` |
| GA.2 | *(opt.)* Off-diagonal `G_{01}(z)→0` exponentially as `z·(σ₁−σ₀)→∞`; structural cause of unlike-pair divergence | ✓ DONE, axiom-clean — mechanism (`g_mat_offdiag_decay'` Tendsto form / `g_mat_offdiag_decay` exp-bound form) + concrete N=2 discharge `g_mat_offdiag_decay_concrete` (`Q0_mat_phys 0 1/det→0`); rests on `p1/p2_tendsto_zero`⇒`Q0_mat_phys_offdiag01_tendsto_zero` + `Q0_mat_phys_det_tendsto_one` (`det→1`, **not** via the `Q0_moment_det_pos` axiom) | `YukawaOZMix/OffDiagDecay.lean`, `HSMixture/Q0DetLimit.lean` |
| GA.3 | **GA-matrix split's** unlike-pair ratio unbounded: `K·exp(z·R_{01}) / ‖c_HS_{01}‖ → ∞` as `z·R → ∞`. Corollary of GA.1: the split's growing branch dwarfs the `z`-independent HS reference ⇒ **the GA-matrix inner formula is unusable at 2YK**. ⚠ **Scope corrected 2026-07-17**: this is about the **GA split**, *not* about perturbation theory — the **true** `c^(1)` is O(1)-bounded (**0.332** vs the GA formula's **1.78×10⁸**, pair (1,2)) and first order at **full** coupling is **1–2%** accurate in `ĉ(0)`, so the expansion is **valid**; the huge ratio measures the split's catastrophic cancellation (`true_first_order_probe.md`). Same over-reach that retired GA.4. | ✓ DONE, axiom-clean — `perturbation_ratio_unbounded` (∀ `K>0`, fixed `z`-independent HS bound `M_HS>0`, target `M`: ∃ `(z,R)`, `M ≤ K·exp(z·R)/M_HS`); direct corollary of GA.1 at target `M·M_HS`; `M_HS` threaded as hypothesis (cf. `hs_pole_additive_insufficient`'s `hB`) | `FMSAPoly/PolyApproxFails.lean` |
| ~~GA.4~~ | ~~*(post-MML.8 corollary)* MSA `ε`-series convergence radius `R_conv ≤ C·e^{−z·R_{01}} → 0` as `z·R_{01} → ∞`~~ — **RETIRED 2026-07-17, doubly falsified.** (i) *Mechanism gone*: it rested on "the Mittag-Leffler poles of the exact inner **DCF** (`det Q̂₀=0`)", but (★)/Group MRS proves the inner DCF has **no HS poles** — there are none to migrate with `ε`. (ii) *Conclusion refuted numerically*: `numerical_notes/results/true_first_order_probe.md` (non-circular certified ground truth) says verbatim "the claimed convergence radius `R_c ~ e^{−zR} ≈ 10⁻⁶` **is refuted**" — `c(s·K)` is smooth on `s∈[0,1]` (OZ converges in 28 iters at every `s`, no singularity) and first order at **full** coupling `s=1` is **1.3%/1.4%** accurate in `ĉ(0)` for (1,2)/(2,2), 2nd-order fraction growing only linearly (0.05→0.2) — a well-behaved series at `s=1`, the opposite of `R_conv≈2×10⁻⁶ ≪ |K₂|≈2.3`. (iii) *Root cause*: it conflated **the GA-matrix split's** ill-conditioning (real — GA.1–GA.3) with **first-order perturbation theory itself** diverging (false) — the conflation flagged in `true_first_order_probe.md` §5.4. Never started; no Lean code to retract. | ✗ retired | — |


### Group P — FMSA_poly Failure Analysis *(failures)*

| Task | Title | Status | Lean file |
|------|-------|--------|-----------|
| P.1 | E_ij is sum of decaying exponentials | ✓ DONE | `FMSAPoly/EijStructure.lean` |
| P.2 | Origin constraint p₀=−E_ij(0) | ✓ DONE | `FMSAPoly/OriginConstraint.lean` |
| P.3 | No polynomial approximates exp(+z·(R−r)) under normalisation | ✓ DONE | `FMSAPoly/PolyApproxFails.lean` |
| P.4 | E_ij contact value matches outer-core MSA | ✓ DONE | `FMSAPoly/ContactValue.lean` |
| P.C1 | Corollary: normalisation forces large error | ✓ DONE | `FMSAPoly/PolyApproxCorollary.lean` |
| P.C2 | Two-endpoint bound | ✓ DONE | `FMSAPoly/PolyApproxFails.lean` |
| P.B1 | Exponential basis: 2×2 system always solvable | ✓ DONE | `FMSAPoly/ExpBasis.lean` |
| P.B2 | Exponential basis: zero endpoint errors | ✓ DONE | `FMSAPoly/ExpBasis.lean` |

### Group OZ — Ornstein-Zernike Structure *(hard_sphere)*

| Task | Title | Status | Lean file |
|------|-------|--------|-----------|
| OZ.1 | PY closed-form DCF for hard spheres | ✓ DONE | `HardSphere/PYDCF.lean` |
| OZ.2 | g₀_HS via OZ fixed point | ◑ fixed-point framework (`oz_h`, `oz_fixed_pt_unique`) live and reused by the Fourier line. See [proof_notes_hard_sphere.md](proof_notes_hard_sphere.md) | `HardSphere/PYOZ_GHS.lean` |
| OZ.2b | Gap A: exterior 3D-OZ equation for `oz_h` (`oz_h_satisfies_conv_ext`) | ✓ DONE, transform-independent — reused verbatim by OZ.7. See [proof_notes_hard_sphere.md](proof_notes_hard_sphere.md) | `HardSphere/OZExteriorBridge.lean` |
| OZ.3 | g₀_HS via OZ Laplace inversion | ◑ conditional on OZ.2 + `oz_h_exterior_regularity` axiom (see CONTACT.5) | `HardSphere/PYOZ.lean`, `HardSphere/JumpAsymptotic.lean` |
| OZ.4 | Linearised OZ: Ĥ¹=Ĉ¹·S₀ | ✓ DONE | `HardSphere/PYOZ.lean` |
| OZ.6 | Radial sine/Fourier transform convolution theorem | ✓ DONE, no axiom | `HardSphere/RadialFourier.lean` |
| OZ.7 | Fourier-domain exterior OZ equation (Gap A ∪ Gap B) | ✓ DONE, conditional only on OZ.9 (`hcore`) | `HardSphere/OZFourierBridge.lean` |
| OZ.8 | Closed-form sine-transform formula for `c_HS` + bridge to `C_HS_laplace`/`S0` via `s↔-ik` | ✓ DONE (Parts A+B), no axiom/sorry. Bridge to `g0_HS_contact_value` — see Group CONTACT `CONTACT.1`/`5` | `HardSphere/RadialFourierCHS.lean` |
| OZ.9a | PY core closure (Gap B) for `r < σ` — promoted to a named, numerically-verified axiom | ✓ DONE (axiom `oz_core_closure`, Route A; not proved from Mathlib real-analysis — needs Group BAXTER's full construction, staged not started) | `HardSphere/PYOZ_GHS.lean` |
| OZ.9b | `oz_fourier_oz_eq_of_PY_core`: OZ.7 specialized to consume `oz_core_closure` instead of an externally-supplied `hcore` — most complete/trustworthy result in the whole OZ chain (Gap A + convolution theorem + Gap B all proved/axiomatized by name, only routine integrability hypotheses remain) | ✓ DONE | `HardSphere/OZFourierBridge.lean` |
| OZ.10 | Uniqueness of the OZ fixed point (`oz_fixed_pt_unique`) | ✓ **NO LONGER AN AXIOM** — `oz_fixed_pt_unique_thm` (`OzWienerHopfBounded.lean:153`) is a theorem. ⚠ It is *not* axiom-clean: `#print axioms` (2026-08-04) gives the standard three **plus** `radialShell_bounded_injective` (MA.15), `wiener_causal_resolvent` (MA.13) and `zeroFree_lowerHalfPlane_of_homotopy` (MA.14) — all **math** axioms, so the physics assumption is gone but three Mathlib gaps remain. Historical note: dilute proved (OZ.10-dilute); mid/high density is **TRUE** (hard spheres have no spinodal). **Compactness is a red herring**: `oz_linear_op` K *is* a non-compact half-line Wiener–Hopf operator and Mathlib's compact Fredholm indeed doesn't apply — but the route never needed it. Baxter factorization (BAXTER.3) gives `(I−K) = (I−K₊)(I−K₋)` with **each one-sided factor Volterra (spectral radius 0), hence invertible with no compactness/Fredholm at all** ⇒ `(I−K)` invertible ⇒ existence + uniqueness. **Re-scoped 2026-07-17:** the missing piece is the **real-space** operator factorization — i.e. `OZFIX.15`'s `ψ ⋆ Q₊ ⋆ Q₋ = φ` (`Q₊ := δ − q0·1_{[0,σ]}` is one-sided ⇒ `⋆Q₊` is the Volterra factor) — **not** "POLE.4's `h_explicit` inverse + Group OZFIX's assembly" as previously stated: that Fourier/contour assembly is refuted for `oz_core_closure` by `OZFIX.14` and is not what uniqueness needs. Shares its bottleneck with OZ.9a and OZ.3 — all three funnel through `OZFIX.15`. | `HardSphere/PYOZ_GHS.lean` |
| OZ.10-dilute | Dilute-regime (`eta<1`, `24·eta·bracket<1`, i.e. `eta≲0.088`) Banach existence/uniqueness for `oz_fixed_pt_unique`, exterior-only | ✓ DONE — `oz_fixed_pt_unique_dilute`, genuine theorem, no axiom/sorry. Mid/high density (`eta≈0.3–0.5`) is TRUE but gated by the BAXTER line (K non-compact ⇒ not compact Fredholm; `24η·bracket` = K's spectral radius = the dilute threshold; Baxter factorization + POLE.4/Group OZFIX explicit inverse), not attempted here — see [proof_notes_hard_sphere.md](proof_notes_hard_sphere.md) Task OZ.10-dilute. | `HardSphere/OzFixedPtDilute.lean` (+ `c_HS_abs_t2_integrableOn` in `HardSphere/PYDCF.lean`) |
| OZ.18 | Hard-sphere `λ_ij` kink: `c_HS,ij` is C⁰ (slope kink) at `λ_ij=\|d_i−d_j\|/2`, unlike pairs *(formerly B.19)* | ✓ **DONE, slope now CLOSED-FORM (2026-07-16, axiom-clean)** — C⁰ + both one-sided slopes; **`cHS_core_deriv_at_cutoff`: `F′(λ)=2(χ₂₂−χ₁/(4π))`** (all `χ₃/χ₂/χ₁` cancel; indep. of `Rᵢ,Rⱼ,χ₀,χ₂,χ₃`) ⇒ the ex-numerical `hslope` is now the elementary `χ₂₂≠χ₁/(4π)` (`cHS_core_deriv_at_cutoff_ne_zero_iff`, `cHS_FMT_not_differentiableAt_of_chi`). Sharp: kink fails iff `χ₂₂=χ₁/(4π)` | `HSMixture/CHSKink.lean` | **✅ 2026-07-17: kink now UNCONDITIONAL on `0<η<1`** — `cHS_FMT_kink_WB`/`_fmt` (`CHSKinkWB.lean`, axiom-clean) discharges `χ₂₂≠χ₁/(4π)` via the closed form `χ₂₂−χ₁/(4π)=E_WB=n₂·g(η)/(12η²v²)` + `g_wb_pos` (`g>0` on `(0,1)` by nested monotonicity `g′=4p`, `p′=−log(1−η)>0`); vanishes only at the ρ→0 boundary. Left-slope cleaned by OZ.19.
| OZ.19 | `c_HS,ij` **exactly constant on `(0, λ_ij)`** — interval flatness, **functional-agnostic** (weight-support geometry, no Φ). **Strengthens OZ.18**: left slope 0 ⇒ its kink. Numerically exact (spread `0.000e+00`, FMT). *(new 2026-07-17)* | ✓ **DONE 2026-07-17, axiom-clean** — `cHS_flat_of_convs_const` (reduction, ∀ Φ) + `cHS_fmt_inner_const` + the OZ.18-cleanup kink lemmas `hasDerivWithinAt_Iic_zero_of_flat` / `not_differentiableAt_of_flat_below_of_right_deriv` (kink off `clampedBelow`) | `HSMixture/CHSFlatInner.lean` |

**Tasks OZ.5, OZ.11–OZ.17 (and `OZ.13`) have moved to Group BAXTER**

### Group BAXTER — Baxter Q-Factor & Wiener–Hopf Factorization Foundations *(hard_sphere)*

*Depends on Group OZ above (uses `oz_h`, `c_HS`, `radial_fourier`, `oz_core_closure`, etc.);
Group OZ does not depend on this group. Feeds Groups CONTACT/POLE/OZFIX below. See
[proof_notes_baxter.md](proof_notes_baxter.md).*

| Task | Title | Status | Lean file |
|------|-------|--------|-----------|
| `BAXTER.1` | Baxter real-space convolution identity *(formerly OZ.5)* | ✓ DONE | `HardSphere/BaxterRealSpace.lean` |
| `BAXTER.2` | Re-derive Baxter's second relation (Route B) from a primary source *(formerly OZ.11)* | ☐ not started — residue-series construction validated 3 independent ways (ground truth, pole-growth law, real-space OZ check); staged into Group POLE (`POLE.1`–`5`) + Group OZFIX (`OZFIX.1`–`8`) | — |
| `BAXTER.3` | Baxter's Wiener–Hopf factorization `(1-ρQ̂(k))(1-ρQ̂(-k)) = 1-ρĈ_sine(k)` *(formerly OZ.12)* | ✓ DONE — `baxter_wiener_hopf_factorization`, genuine theorem, no sorry/axiom; uses existing `q0_poly` | `HardSphere/BaxterWienerHopf.lean` |

### Group CONTACT — `g0_HS_contact_value` via the Jump-Asymptotic Route *(hard_sphere)*

*Split from Group BAXTER (`BAXTER.4`–`8`). Depends on Group BAXTER (`BAXTER.1`–`3`)
and Group OZ (`oz_h`, OZ.8/OZ.9b). See [proof_notes_contact.md](proof_notes_contact.md).*

| Task | Title | Status | Lean file |
|------|-------|--------|-----------|
| `CONTACT.1` | Derive `g0_HS_contact_value` from OZ.8's Fourier-domain closed form (full residue inversion) *(formerly `BAXTER.4`/OZ.13)* | ☐ deliberately parked — fully absorbed into `BAXTER.2`'s scope, not independent | — |
| `CONTACT.2` | Extract `g0_HS_contact_value` via a jump-asymptotic argument on `Ĥ(k)`'s large-`k` behavior *(formerly `BAXTER.5`/OZ.14)* | ✓ DONE as a whole — split into `CONTACT.3`/`4`/`5`, all three done | — |
| `CONTACT.3` | General jump-asymptotic lemma for `radial_fourier`: `f k = 4πσJ·cos(kσ)/k² + o(1/k²)` for `f` with jump `J` at `σ` *(formerly `BAXTER.6`/OZ.15)* | ✓ DONE — `radial_fourier_jump_asymptotic`, genuine theorem, no sorry/axiom; via one IBP + Mathlib's Riemann-Lebesgue lemma | `HardSphere/JumpAsymptotic.lean` |
| `CONTACT.4` | Concrete closed-form asymptotic of `Ĥ(k)=Ĉ(k)/(1-ρĈ(k))`: leading coefficient `4πσ(1+η/2)/(1-η)²` *(formerly `BAXTER.7`/OZ.16)* | ✓ DONE — `Hhat_closed_asymptotic`, genuine theorem, no sorry/axiom | `HardSphere/RadialFourierCHS.lean` |
| `CONTACT.5` | Assembly: apply `CONTACT.3` to `oz_h`, match against `CONTACT.4`, conclude `g0_HS_contact_value` *(formerly `BAXTER.8`/OZ.17)* | ✓ DONE — `g0_HS_contact_value_of_oz_h_regularity` (conditional theorem, no sorry/axiom) **plus** its unconditional consequence: the bare `g0_HS_contact_value` axiom is now **retired**, replaced by `oz_h_exterior_regularity` bundling `CONTACT.5`'s `oz_h` hypotheses, from which `theorem g0_HS_contact_value` is proved. **Updated OZFIX.22 (2026-07-17):** `oz_h_exterior_regularity` itself is now a **theorem** (via the bridge `oz_h = baxterPsi/·`), so `#print axioms g0_HS_contact_value` → `[propext, Classical.choice, Quot.sound, baxter_exterior_regularity, oz_fixed_pt_unique]` (no `g0_HS_contact_value`, no `oz_core_closure`, no `oz_h_exterior_regularity` axiom). Retires OZ.3's physical-number axiom. | `HardSphere/JumpAsymptotic.lean` |

### Group POLE — Complex-Analytic Pole/Residue Construction for the Baxter Q-Factor *(hard_sphere)*

*Split from Group BAXTER (`BAXTER.9`,`10`,`11`,`12`,`14`). Depends on Group BAXTER
(`BAXTER.1`–`3`); feeds Group OZFIX. See [proof_notes_pole.md](proof_notes_pole.md).*

| Task | Title | Status | Lean file |
|------|-------|--------|-----------|
| `POLE.1` | `Qhat_complex : ℂ → ℂ` in closed form, proved entire *(formerly `BAXTER.9`)* | ✓ DONE — `Qhat_complex_entire` + `Qhat_complex_formula`, genuine theorems, no sorry/axiom; entireness via `entire_poly_exp_integral` (dominated-convergence differentiation under the integral), closed form via new `zeta0`/`zeta1`/`zeta2` moment lemmas; numerically cross-checked against the raw integral and against `BAXTER.3`'s real-`k` formulas | `HardSphere/BaxterZeros.lean` |
| `POLE.2` | Numerical/symbolic feasibility check for the Banach-pole-existence strategy *(formerly `BAXTER.10`)* | ✓ DONE — **GO**: uniform contraction bound (`max_L→0.369`, margin `≈63%`) found for `n≥10`, robust across `η∈[0.05,0.45]` | — (Python, not Lean) |
| `POLE.3` | Pole existence in Lean via Banach contraction *(formerly `BAXTER.11`)* | ✓ DONE (conditional) — `Qhat_complex_zeros_infinite`, no sorry/axiom; all magnitude bounds, branch-safety, Lipschitz/MVT, Banach wiring, distinctness/infinitude proved unconditionally for general `η∈(0,1),σ>0,ρ>0`; one explicit "good guess" hypothesis (`hstep`, numerically validated by `POLE.2`) not yet discharged for the specific asymptotic guess formula. **✅ `G_baxter`'s zero condition double-counted-`ρ` bug FIXED** (was `1-ρ·Qhat_complex(k)=0`, corrected to `1-Qhat_complex(k)=0`; `baxterP0/1/2` unchanged, `Dpoly`'s affine coeffs lose one `ρ` power, `hrho` strengthened `rho≠0→0<rho` throughout `BaxterPoles.lean`/`BaxterResidue.lean`/OZFIX files). Full project `lake build` green; `G_baxter`'s own first pole now matches `BAXTER.2`'s ground truth `6.058015+1.436794i` to machine precision; `h_explicit(2.0)` converges to the known value `0.005663`. See `proof_notes_pole.md` `POLE.3` for the full writeup. **Group OZFIX's aggregate identity (`OZFIX.9`) was subsequently re-scoped on the corrected family and CONFIRMED — the earlier `~50%` residual was truncation (N=11 poles at slow-converging `r=1.5`); per-pole exact for `r≥2σ`, slowly convergent (`~1/N`) for `σ≤r<2σ`. See `proof_notes_ozfix.md` `OZFIX.9`.** | `HardSphere/BaxterPoles.lean` |
| `POLE.4` | Residue formula + convergence for `h_explicit` *(formerly `BAXTER.12`)* | ✓ DONE — `residue_of_simple_pole`, `Chat_complex`, `G_baxter_deriv`, `Hhat_residue_at_pole`, `G_baxter_zero_mirror`, `h_explicit`/`h_explicit_term`/`h_explicit_summable`, and `G_baxter_pole_family_exists` all done; `POLE.5` (below) closes the one remaining gap (the magnitude-bound hypothesis, now proved rigorously not just numerically). No sorry/axiom. | `Analysis/ResidueAtSimplePole.lean`, `HardSphere/BaxterResidue.lean`, `HardSphere/RadialFourierCHSComplex.lean`, `HardSphere/BaxterPoles.lean` |
| `POLE.5` | Rigorous `n^{1-2r/σ}` magnitude bound + concrete `h_explicit` instantiation *(formerly `BAXTER.14`)* | ✓ DONE — `residue_term_norm_bound` (rigorous `‖residue_term(k)‖≤C·‖k‖^{1-2r/σ}`, assembled from new `Npoly_upper_bound`/`Dpoly_upper_bound`, `G_baxter_deriv_lower_bound_of_zero`/`G_baxter_neg_lower_bound` (direct lower-bound versions of the old non-vanishing theorems), `abs_exp_neg_ikn_sigma_lower/upper`, `abs_exp_ikr_eq_rpow`/`abs_exp_ikr_upper_of_zero`, `Chat_F_norm_bound`/`Chat_complex_norm_bound`, and two existential wrappers `exists_hkN_hkT_threshold`/`exists_D_for_exp_neg_bound` working around `baxterP0/1/2`'s cross-file privacy); `h_explicit_term_norm_bound` (pole+mirror pairing); `h_explicit_summable_of_pole_family` (general `Summable` theorem for any zero family with linear `‖k_n‖` growth — wiring to `G_baxter_pole_family_exists_growth`'s concrete witness DONE as `POLE.6`). No sorry/axiom (one `set_option maxHeartbeats` bump, a performance not correctness issue). | `HardSphere/BaxterResidue.lean`, `HardSphere/BaxterPoles.lean`, `HardSphere/RadialFourierCHSComplex.lean` |
| `POLE.6` | Concrete pole family wired into `h_explicit`'s summability | ✓ DONE (axiom-clean) — `h_explicit_summable_concrete` consumes `G_baxter_pole_family_exists_growth`'s conclusion tuple (existentially, since that theorem's hypotheses mention the `private` `baxterP0/1/2` — same cross-file-privacy pattern as POLE.5's wrappers) + a new centre hypothesis `hk1im : r ≤ Im(k1 n)`, and yields an injective, `G_baxter`-zero, upper-half-plane family with `Summable (h_explicit_term …)` for every `y>σ`. **Fixes the `hkfam_im` hidden gap**: `_growth`'s output lacked `0 ≤ Im(g n)` (which every `h_explicit` lemma consumes) — recovered via `pole_family_im_nonneg` (`Complex.abs_im_le_norm`). Conditional only through `hfam` — fires unconditionally the moment POLE.3's `hstep` lands. See [proof_notes_pole.md](proof_notes_pole.md) `POLE.6`. | `HardSphere/HExplicitConcrete.lean` |
| `POLE.7` | Discharge `hstep` via the derived log-lift guess | ✓ **DONE — `hstep` PROVED (eventually form), axiom-clean, no sorry, full build green (2026-07-16)**: `k1_guess_hstep_eventually` — for the fit-free guess `k1_guess(n)=2πn/σ+(i/σ)·ln(‖Npoly(xₙ)‖/‖Dpoly(xₙ)‖)` (supersedes POLE.2's fitted `2ln x−2.12`; `hk1re` exact by construction), `∃N, ∀n≥N, dist(k1_guess n, baxterPhi n (k1_guess n)) ≤ rr(1−KK)` for **every** positive target and all physical params — verbatim the log-map `hstep` shape `G_baxter_pole_family_exists` consumes. ~70 declarations in 5 stages: `rfl` privacy bridges (`guessP0/1/2` defeq to private `baxterP0/1/2`), real-axis/off-axis two-sided N/D norm bounds + segment MVT, log-ratio/Jordan-arg/perturbation helpers (**key: `Complex.log`'s definition splits the residual exactly — no branch-cut analysis or log-MVT needed**), master `k1_guess_hstep_of` (hstep ⇒ 5 explicit real inequalities), envelope constants + `eventually_log_cap` (`log =o(id)`) + threshold assembly. Scoping (2026-07-15): residual `≈3.5·ln(xₙ)/xₙ→0`, numerical `N=4/4/6/17` at `η=.05/.1/.3/.45`. **Follow-on = `POLE.8`/`POLE.9` below.** See [proof_notes_pole.md](proof_notes_pole.md) `POLE.7`. | `HardSphere/BaxterPoleGuess.lean` (new) |
| `POLE.8` | Fire the log-route family: POLE.3 unconditional | ✓ **DONE (2026-07-16) — `POLE.3` CLOSED UNCONDITIONALLY**, axiom-clean, sorry-free, first build green: **`Qhat_complex_zeros_infinite_unconditional`** + `G_baxter_zeros_infinite_unconditional` (hypotheses = physical parameters only!) + POLE.6 payoff **`h_explicit_summable_unconditional`** (`∃ g` injective `G_baxter`-zero family, `Im≥0`, `Summable (h_explicit_term … y g)` for every `y>σ` — the concrete `h_explicit` pole family is now fully unconditional). Discharged the whole remaining hypothesis block (`hMball`/`hkN`/`hkD`/`hM1`/`hC`/`hK1`/`hDball`) for `k1 := k1_guess` with concrete `r = min 1 (π/2σ)`, `M = max(…)`, `K = toNNReal((8+4A+2B)/(σM)) ≤ 1/2`; main work `hDball` via `(k²).re = k.re²−k.im²` disk geometry + `y ≤ x/2` cap; 12-conjunct package `k1_guess_family_conditions`; guessP↔baxterP by defeq. See [proof_notes_pole.md](proof_notes_pole.md) `POLE.8`. | `HardSphere/BaxterPoleFamilyConcrete.lean` (new) |
| `POLE.9` | Chord `ChordPoleFamily (G_baxter)` (shared MZERO.5 obligation) | ✓ **DONE (2026-07-16)** — `chordPoleFamily_G_baxter_exists` + `G_baxter_zeros_infinite_chord` (axiom-clean, sorry-free, zero warnings): the 1-freq chord obligation is CLOSED for all physical params; pivotal lemmas `exp_at_k1_guess` (exact phase kill via `exp_int_mul_two_pi_mul_I`) and `norm_sub_ratio_mul_le` (algebraic unit-phase-difference bound, no arg/trig); `r=1/(10σ)`, `K=1/2`; 2-freq `det_c` extension (= MZERO.5) now has a complete worked template. Original plan: ◐ opened 2026-07-16 — construct the chord-Newton family at `r := c₀/σ` (`c₀≈0.3`; numerics `K≈0.35`, `N=17/15/9/2`); two structural facts make it tractable: **exact phase kill** `e^{−i·k1_guess·σ} = ‖N(x)‖/‖D(x)‖` (real positive!) and `‖G′(k1)‖ ≳ σx³` (exp term dominates). Fires `G_baxter_zeros_infinite_of_chordPoleFamily`; 2-freq `det_c` extension (= MZERO.5 discharge) is the mixture-session follow-on. See [proof_notes_pole.md](proof_notes_pole.md) `POLE.9`. | `HardSphere/BaxterChordFamily.lean` (new) |
| `POLE.10` | **Pole-family EXHAUSTION**: the constructed family enumerates *all* UHP zeros of `G_baxter`, injectively, with non-degenerate mirror pairing | ☐ **opened 2026-07-16 — on the critical path for BOTH remaining `hcollapse` routes** (`OZFIX.14` options (b) and (c)). Needed: `Function.Injective kfam`; `∀ z, 0 < z.im → G_baxter … z = 0 → ∃ n, z = kfam n ∨ z = -conj (kfam n)`; and pairing non-degeneracy (`(kfam n).re ≠ 0`, else `k = -conj k` and `Hterm`/`Kterm` double-count; `kfam m ≠ -conj (kfam n)`). **`POLE.8`'s `Qhat_complex_zeros_infinite_unconditional` gives infinitude, NOT exhaustion** — the gap is real. Why it matters: option (c) needs it for the half-disk residue theorem (must know there are no other UHP poles); option (b) needs it or the axiom is **FALSE** (verified counterexample: `kfam' n := k_{2n}` satisfies `hkfam_zero/im/re/ne` yet breaks (★) — `0.982` vs target `1.602` at `u=0.7`; see `OZFIX.14`). **⚠ 2026-07-17 — investigated; it is RESEARCH-SCALE and OFF the winning critical path.** (i) **Mathlib gap confirmed:** the pinned Mathlib has **no** Rouché, argument principle, winding number, or zero-*count* — `MeromorphicOn.divisor` exists but the project's Jensen route (`MixtureHSCounting`, MZERO) extracts only an asymptotic *lower* bound on the count ⇒ **infinitude, never exhaustion**. So the sketched Rouché route needs a from-scratch argument-principle build (≈ the whole Group-MA contour effort) OR a direct uniform bound `|G_baxter|>0` on the full UHP outside the construction disks (a hard log-strip estimate, no existing machinery). (ii) **Marginal value is now LOW:** POLE.10 is needed only for `OZFIX.14`'s Fourier routes (b)/(c), which `OZFIX.14` showed are circular/inferior to route **(a) real-space Baxter** — and (a) (`OZFIX.15`–`20`) reaches Phase C / retires `oz_core_closure` **without any pole-family exhaustion** (its core value is definitional, no residue enumeration). POLE.10 would only matter for the *separate* `h_explicit = ψ/r` closed-form identification, a downstream nicety, not the core-closure goal. **Recommendation: shelve until wanted.** ⚠ 2026-07-17: the argument-principle blocker is now **partly lifted** — `MA.11` **DONE** (`Analysis/ArgumentPrinciple.lean`, `argumentPrinciple_count`: `∮ logDeriv f = 2πi·∑ nᵢ`, derived from MA.1, no new axiom). But MA.11 is **necessary, not sufficient** for POLE.10: it gives the *count* of zeros inside a contour **once you already know the contour avoids all zeros and encloses exactly the construction disks** — POLE.10 still needs `|G_baxter|>0` on a pole-avoiding contour (the hard log-strip estimate, POLE.5 partial) and one-zero-per-disk (POLE.8/9) to feed MA.11's hypotheses. So MA.11 removes the 'no zeros elsewhere in the bulk' *conclusion* step but not the contour-construction *inputs*. Marginal value still LOW (see (ii) below). Original sketch: argument-principle/Rouché count against the `Npoly`/`Dpoly` leading behaviour (reuse `POLE.5` magnitude + `POLE.9` chord contours). cf. the mixture analogue `HSMixture/MixtureHSCounting.lean`. | `HardSphere/BaxterPoleFamilyConcrete.lean` or new |

### Group OZFIX — `h_explicit`'s Closed-Form Assembly into `OzFixedPt` *(hard_sphere)*

*Split from Group BAXTER (`BAXTER.13`/`15`) — both retired, replaced by the eight
topic-scoped tasks below. Depends on Group BAXTER (`BAXTER.1`–`3`) and Group POLE. See
[proof_notes_ozfix.md](proof_notes_ozfix.md).*

| Task | Title | Status | Lean file |
|------|-------|--------|-----------|
| `OZFIX.1` | Strategy scoping (`B.0`) + zeroth-moment inner integral (`B.1`/`B.2`) | ✓ DONE — `moment0_formula`; `B.0` (pole-family completeness) resolved as not needed for the termwise strategy. No sorry/axiom. | `HardSphere/BaxterResidue.lean` |
| `OZFIX.2` | Complex-`k` Wiener–Hopf bridge (`OZFIX.6`'s key prerequisite) | ✓ DONE — `baxter_wiener_hopf_complex`, via analytic continuation/identity theorem, unconditional, no new axiom | `HardSphere/BaxterWienerHopfComplex.lean` |
| `OZFIX.3` | Sum/integral interchange machinery (`B.3`–`B.4` core) | ✓ DONE — `h_explicit_series_hasDerivAt` (`hasDerivAt_tsum_of_isPreconnected` payoff), `h_explicit_series_integral` (two-sided FTC, `lo>σ`), `s_mul_h_explicit_integral` (closed-form value `oz_linear_op` needs). No sorry/axiom. | `HardSphere/BaxterResidue.lean` |
| `OZFIX.4` | The `σ`-boundary case | ✓ DONE (conditional on `hint`) — `residue_term_norm_bound` weakening, `Hterm_uniform_summable_bound_of_pole_family`, `h_explicit_series_integral_from_sigma` (one-sided FTC); `hint` is a confirmed-genuine integrability gap, not Lean bookkeeping (see `proof_notes_ozfix.md` for the `g0_HS_contact_value`/Group CONTACT investigation that ruled out a quick reuse). No sorry/new axiom. | `HardSphere/BaxterResidue.lean` |
| `OZFIX.5` | Outer `t`-integral assembly (`B.3` proper) | ✓ DONE — `oz_forcing_add_linear_op_h_explicit_eq_outer_integral` eliminates the raw inner `∫s` integral entirely (closed `Hterm` form), case split `r-t≷σ` via `inner_h_explicit_integral_bridge`; no sorry/axiom | `HardSphere/OzFixedPtHExplicit.lean`, `HardSphere/BaxterResidue.lean` |
| `OZFIX.6` | Algebraic collapse (`B.5`, the mathematical payoff) | ◑ **split → `OZFIX.11` (`r≥2σ`, ✓ DONE 2026-07-16) + `OZFIX.12`/`OZFIX.13` (`σ≤r<2σ` + endpoint, scoped)**. The old scoping's "per-pole route FALSE (`-2.72`)" is now fully understood: it is specific to `σ≤r<2σ` (where `oz_forcing≠0`); for `r≥2σ` the per-pole collapse is TRUE and **proved** (collapse factor `ρ·Ĉ(k_n)=1`, see `OZFIX.11`). Aggregate target well-confirmed numerically. | see `OZFIX.11`–`13` |
| `OZFIX.7` | Regularity (`B.6`): `ContinuousOn`+boundedness | ✓ DONE on `(σ,∞)`/`[r0,∞)`, `r0>σ` — `h_explicit_continuousOn_Ioi`, `h_explicit_bounded_on_Ici`, no sorry/axiom. Closed endpoint `r=σ` remains open (same `σ`-boundary gap as `OZFIX.4`/`6`) | `HardSphere/HExplicitRegularity.lean` |
| `OZFIX.8` | Final assembly (`B.7`): invoke `oz_fixed_pt_unique` | ✓ DONE, conditional on `hcollapse` (`OZFIX.6`) + `hcont_sigma` (`OZFIX.7`'s σ-endpoint gap) — `oz_h_eq_spliced_h_explicit`, `#print axioms` only `oz_fixed_pt_unique` beyond the standard three; retiring `oz_core_closure` itself is a separate Phase C follow-on. **2026-07-16: hypothesis weakened** — corollary `oz_h_eq_spliced_h_explicit_of_inner_collapse` (`OzCollapseTwoSigma.lean`) needs the collapse only on `σ≤r<2σ` (`hcollapse_inner`; the `r≥2σ` half discharged by `OZFIX.11`; adds `heta_def`+`hkfam_ne`), same axiom footprint. | `HardSphere/OzFixedPtHExplicitFinal.lean`, `HardSphere/OzCollapseTwoSigma.lean` |
| `OZFIX.9` | `hcollapse` via **Route A** (`oz_forcing` termwise Mittag-Leffler expansion), alternative to Route B (whole-series `radial3d_conv` + Mathlib-axiom, done elsewhere) | ☐ **Direction (user-confirmed 2026-07-15): Route B closes `hcollapse`** (favorable axiom trade — physics `oz_core_closure` → standard/reusable residue axiom); Route A kept as the future *unconditional* (axiom-free) path, `r≥2σ` part now tractable. **UNBLOCKED — the POLE fix resolved the aggregate; Route A viable but research-scale (≈ Route-B difficulty).** Re-scoped against the now-fixed `G_baxter=(-ik)³(1-Q̂)`: (a) anchor `h_explicit(2.0)=0.005688` = ground truth; (b) **aggregate `oz_forcing+oz_lin=h_explicit` HOLDS** — exactly per-pole for `r≥2σ` (`oz_forcing=0`; the earlier "−2.72 per-pole false" was the wrong pole family), slowly convergent (`~1/N`) for `σ≤r<2σ`. The earlier "~50% fail" was **truncation** (N=11), now retracted (both here and in `proof_notes_pole.md`). (c) Route A `oz_forcing=Σ Re[H_n−L_n]` holds numerically but `R_n=H_n−L_n` is **not** a clean `ô_forcing(k_n)` coefficient (ratio oscillates), so it's the aggregate rearranged, not obviously cheaper than Route B. Realistic path: `r≥2σ` per-pole tractable; `σ≤r<2σ` needs the genuine termwise argument. **2026-07-16: the `r≥2σ` per-pole piece is PROVED (`OZFIX.11`)** — the clean per-pole factor is `ρ·Ĉ(k_n)=1` (WH factorization at `G_baxter` zeros), not an `ô_forcing(k_n)` coefficient; only `σ≤r<2σ` remains (`OZFIX.12`). | new `HardSphere/OzForcingResidue.lean` |
| `OZFIX.10` | `hcollapse` via **Route B** (growing-contour Fourier inversion: `∫_{-R}^{R} k·Ĥ·e^{ikr} → 2πi·Σres`, then back to `radial3d_conv`) — the user-confirmed closing route (see `OZFIX.9`) | ◑ **infrastructure DONE, arc-vanishing OPEN.** Done (2026-07-15, `lake build` clean): (a) **`jordan_lemma_arc_bound`** (`Analysis/JordanLemma.lean`) — quantitative Jordan's lemma `‖∫_arc g·e^{iaz}‖≤πM/a`, **proved outright, NO axiom** (`Real.mul_le_sin` + ML-inequality + `Complex.norm_exp` + FTC); (b) **half-disk residue theorem** — axiom `halfDiskBoundary_eq_sum_of_small_circles` + genuine theorem `halfDiskBoundary_eq_sum_two_pi_I_mul_of_simple_poles` (`Analysis/ContourDeformation.lean`, see Axioms table + `MATH_AXIOMS.md`); (c) Step-0 numerics: `residue_term` = true residue of `k·Ĥ·e^{ikr}` to machine precision, `2πi` normalization exact. **OPEN**: `\|k·Ĥ(k)\|` *grows* `≈1.745·R` on the arc (numerically confirmed, plateau over `θ∈(10°,170°)`), so Jordan's crude sup-bound gives a growing estimate; the arc's real `O(1/R)` decay (confirmed to `N=55`, `R≈349`) is **oscillatory cancellation** — needs a non-stationary-phase/Van-der-Corput estimate, deliberately NOT axiomatized (would assume the theorem's hardest part). **2026-07-16 UPDATE — the MA.2-pointwise assembly is BLOCKED by a FALSE identity, and superseded.** Scoping the termwise inversion of `x·Ĥ·e^{ixr}` found the `O(R)` moment `W₁=∫_{-R}^{R}x·e^{ixr}dx` enters with total coefficient `Ĥ(0)+Σ'res_n/p_n`, which is **NOT 0**: the finite-`N` partial sums equal the circle-mean of `Ĥ` → **`−1/ρ`** (smoking gun: the recorded circle bounds `sup‖Ĥ‖=1.7453`/`1.1636` are exactly `1/ρ` at η=0.3/0.45; re-confirmed numerically, mean −1.73 at R≈763). No ML kernel degree or `±`-pairing removes `W₁` — pointwise inversion stays genuinely blocked (same content as the arc obstruction, re-expressed). **2026-07-16 (2nd pass) — the ORIGINAL arc blocker is ALSO FALSE, RETRACTED.** "`‖kĤ‖` grows `1.745R` ⇒ Jordan fails ⇒ needs VdC" is a **bookkeeping error**: Jordan needs `sup_arc‖g‖→0` for `F=g·e^{iaz}`, not `sup‖kĤ‖`. Split `r=b+a`, `σ<b<r`: `kĤe^{ikr}=[zĤe^{izb}]·e^{iaz}` and the bracket's sup **does** →0 (the `1.745R` plateau sits exactly where `e^{izb}` is exponentially tiny). Provable in 2 regimes from **existing** lemmas: near-axis `‖Ĉ‖≲e^{Im z·σ}/‖z‖²` (`Chat_complex_norm_bound`) ⇒ `≲R^{δσ−1}`; interior `‖Ĥ‖≤M` + `‖e^{izb}‖≤R^{−δb}`; pick `1/b<δ<1/σ` (possible **iff `b>σ` ⟺ `r>σ`** — exactly the physical domain). **Numerically confirmed** (`ozfix12_arc_check.py`, `R≤952`): `sup‖zĤ‖/R = 1.7453` constant (reproduces the record, and pins `1.7453 = 1/ρ`), but `sup‖zĤe^{izb}‖` decays with **slope exactly −1.00** for `b∈{1.05,1.2,1.4}`; boundary confirmed at `b>σ`. ⇒ **VdC (MA.4) is not needed here and has no consumer; MA.2/MA.3's "dissolves the arc blocker" framing is moot (there was no blocker)**. But this does NOT close `hcollapse` — the real obstruction is circularity (`OZFIX.14`). **`hcollapse` never needed it**: after `oz_linear_op`'s own `s`- and `t`-integrations every kernel piece is Jordan-tractable — Route B decomposes as `OZFIX.11` (`r≥2σ`, ✓ DONE, no contour machinery at all) + `OZFIX.12` (`σ≤r<2σ`, smoothed-kernel contour argument, scoped). See [proof_notes_ozfix.md](proof_notes_ozfix.md) `OZFIX.10`–`12`. | `Analysis/JordanLemma.lean`, `Analysis/ContourDeformation.lean`; application file TBD |
| `OZFIX.11` | `hcollapse` for `r ≥ 2σ` — per-pole collapse via `ρ·Ĉ(k_n)=1` | ✓ **DONE (2026-07-16), axiom-clean** (`#print axioms` = standard three; NO `hint`-family hypothesis — vacuous on this region): `oz_collapse_of_two_sigma_le` — `OZFIX.5`'s outer integral collapses **per pole**: each pole's `t`-integral closes to `residue_term(r,k)·Ĉ(k)/(2π)` (`integral_Chat_poly_exp_pair` + `Chat_F_neg_sub_eq`, pure `Chat_F`/`Chat_J` moment algebra), and **`ρ·Ĉ(k)=1` at every `G_baxter` zero** (`rho_mul_Chat_complex_eq_one_of_G_zero` = `Qhat_pole_iff_G_baxter_zero` + `baxter_wiener_hopf_complex` — this explains `OZFIX.9`'s numerically-exact per-pole collapse); mirror pole via `G_baxter_zero_mirror`; `t`-integral ↔ pole-sum interchange via `intervalIntegral.hasSum_integral_of_dominated_convergence` + `OZFIX.4`'s `y≥σ`-uniform `Hterm` bound (valid incl. `t=σ`, `r=2σ`). New hypotheses vs. `OZFIX.8`: `heta_def` (WH) + `hkfam_ne`. **Downstream unlock**: `oz_h_eq_spliced_h_explicit_of_inner_collapse` (`OZFIX.8` with `hcollapse` weakened to `σ≤r<2σ`). Numerically gated first per project discipline (`ozfix11_stage_check.py`, scratch: `ρĈ(k_n)−1`~1e-14 incl. mirrors, per-pole identity ~1e-13, end-to-end at quadrature tol). See [proof_notes_ozfix.md](proof_notes_ozfix.md) `OZFIX.11`. | `HardSphere/OzCollapseTwoSigma.lean` (new) |
| `OZFIX.12` | `hcollapse` for `σ < r < 2σ` — exact defect algebra + reduction to the core-closure series identity (★) | ◑ **Reduction target IDENTIFIED + numerically CONFIRMED; `Kterm` infra ✓ DONE (axiom-clean, build green); assembly remains.** Derived the exact defect: `oz_forcing + oz_linear_op[h_explicit] = h_explicit + (ρ/r)·Re ∑'ₙDₙ(r)` with `Dₙ(r) = ∫_{r−σ}^σ Chat_poly(r−u)[Hterm n u − Hterm n σ]du` (uses `integral_Chat_poly_mul_Hterm_pair`, which needs **no** `r≥2σ`) ⇒ `hcollapse` ⟺ **(★) `∑'ₙ[Hterm n u − Hterm n σ] = π(σ²−u²)` on `(0,σ]`** = *the exterior residue series continued into the core gives `h=−1`* (Wertheim–Thiele). **Numerically confirmed** (`ozfix12_star_check.py`, 400 poles): converges `~1/N` at every `u∈{0.55..0.99}` (diff `−1.9e−3` at N=400, halving per doubling); `∑'Dₙ→−2πΦ` at `r∈{1.2,1.5,1.8,1.95}`; `Hterm` exactly real. **Trap found:** `‖Hterm n u‖≲‖kₙ‖^{−2u/σ}` ⇒ (★) absolutely summable only for `u>σ/2`, so a `tsum`-form (★) on `(0,σ]` would be a **FALSE (vacuous) hypothesis** — fixed by one more antiderivative: **`Kterm`** (`‖Kterm n u‖≲‖kₙ‖^{−1−2u/σ}`, summable ∀`u>0`), giving the always-summable **(★K)** = Lean predicate `CoreSeriesClosure` + `coreSeriesClosure_summand_summable` (non-vacuity). Landed: `Kterm`, `Kterm_hasDerivAt`, `Kterm_uniform_summable_bound_of_pole_family` (`OZFIX.4` analogue, threshold `y0>0`), `Kterm_summable_of_pole_family`. Remaining = per-pole IBP + interchange + polynomial algebra (mechanical). **`hcomplete`/`hHbound`/circle data/defect-rate/VdC all RETRACTED as unnecessary** (the contour route that needed them is circular — `OZFIX.14`). See [proof_notes_ozfix.md](proof_notes_ozfix.md) `OZFIX.12`. | `HardSphere/OzCollapseInner.lean` (new) |
| `OZFIX.13` | `σ`-endpoint + full `hcollapse` discharge wiring | ☐ **scoped**: `r=σ` from `hcont_sigma` + continuity of the LHS at `σ⁺` (forcing explicit; linop via `OZFIX.5`'s closed form + `Σ'Hterm` continuity down to `σ`, `OZFIX.4` machinery); then the fully-discharged `oz_h_eq_spliced_h_explicit` variant (hypotheses = `OZFIX.11`'s + `OZFIX.12`'s bundle + `hcont_sigma`). Note: at `r=σ` the `h_explicit_term` series is only marginally summable (numerics: pair terms ~`n^{−1.6}`, likely absolutely summable, but the `1/n` worst-case bound can't prove it) — the continuity route avoids relying on it. | TBD |
| `OZFIX.14` | **Circularity: (★) ⟺ core closure ⇒ Group OZFIX CANNOT retire `oz_core_closure`** | ✗ **NEGATIVE RESULT (2026-07-16), closed.** Closing the UHP contour on (★)'s pole sum **provably cannot** prove it. With `ρĈ(kₙ)=1` the summands are `Res_k[S]·Ξ(k)/(iρ)`, `S:=1/(1−ρĈ)=z⁶/(G(z)G(−z))`, `Ξ` **entire**; writing `S=1+ρĤ`, the `ρĤ` arc →0 (Jordan + two-regime) and the `1` part cancels **exactly at every R** between line and arc (`∮Ξ=0`) ⇒ `2πi·∑'Res_k[S]Ξ(k) = ρ∫_ℝ Ĥ(x)Ξ(x)dx`, i.e. **(★) ⟺ `∫_ℝ Ĥ(x)(e^{ixu}−e^{ixσ})dx = −2π²(σ²−u²)`** = "inverse FT of `Ĥ` is `−1` on the core" = the content of the existing physics axiom `oz_core_closure`. The contour is *value-neutral* — it computes the pole sum **in terms of** the core value. **⇒ the stated "Phase C retires `oz_core_closure`" plan is IMPOSSIBLE via OZFIX** (`hcollapse ⟸ (★) ⟺ core closure`); supersedes the whole `OZFIX.9`/`OZFIX.10` Route-A-vs-B framing (both are Fourier-side, both circular). **Scope:** this rules out *Fourier/contour* routes, i.e. Group OZFIX. It does **not** touch real-space Baxter, where the core value is definitional — see `OZFIX.15`, which reaches Phase C. **Three ways forward:** **(a) real-space Baxter/Wertheim–Thiele = `OZFIX.15`, the genuinely axiom-free path — RECOMMENDED, seed CONFIRMED at machine precision**; (b) **swap the axiom** — take (★K) `CoreSeriesClosure` instead of `oz_core_closure`: buys real checkability (concrete computable series vs. the opaque `Classical.choose` `oz_h`) **but the naive form is FALSE ⇒ inconsistent** (verified: `kfam' n := k_{2n}` satisfies `hkfam_zero/im/re/ne` — `hkfam_re` explicitly with `c=d=6` — yet gives `0.982` vs target `1.602` at `u=0.7`; odds `0.618`; drop-first-5 `0.077`; full family `1.600`); an honest form needs the `POLE.10` completeness bundle, so price it with that task included — **the earlier "same count, strictly better quality" is RETRACTED as too glib**; (c) (★) ⟸ `oz_core_closure` + radial-FT inversion = **`MA.9`** (opened; Mathlib *has* `fourierInv_fourier_eq`, so prove don't axiomatize) + `POLE.10`. (b)/(c) are user decisions, not taken here. See [proof_notes_ozfix.md](proof_notes_ozfix.md) `OZFIX.14`. | — |
| `OZFIX.15` | **Real-space Baxter/Wertheim–Thiele — the ψ construction (claims A/B/C ⇒ `ψ ⋆ Q₊ ⋆ Q₋ = φ`)** | ✓ **DONE 2026-07-17 — axiom-clean, `lake build` green** (`HardSphere/BaxterRenewal.lean`). Target **`baxter_psi_conv_eq_phi`**: `(ψ⋆Q₊⋆Q₋)(r) = r·c_HS(r) = φ(r)` for **every** `r>0`, every object concrete. **(1) Global ψ by uniqueness-as-glue:** `MA.10` gives a different `C(Icc σ b, ℝ)` per `b` (dependent type, useless to a convolution identity on `ℝ`); `volterraSol_compat` (the `[a,b']` solution restricted to `[a,b]` still solves it — for `r ≤ b` the range `[a,r]` never leaves `[a,b]` — hence **is** it, by uniqueness) ⇒ `volterraGlobal` (evaluate the `b := r` solution at its own right endpoint) ⇒ **`volterraGlobal_spec`** (renewal eq. at every `r≥a`, same function under the integral) + `volterraGlobal_continuousOn`. **(2)** `baxterPsi` + `baxterPsi_core` (**definitional — zero proof obligation**, the reason real space escapes `OZFIX.14`) + `baxterPsi_odd`. **(3) `baxter_u_outer`** = claim (A) final form (`u := ψ⋆Q₊ ≡ 0` outside), **no `r ≷ 2σ` case split**: `s := r−t` ⇒ `q0`'s `[0,σ]` support kills `s<r−σ` ⇒ range **opens up to `[0,r]` for free** ⇒ splits at `σ` into `baxterForcing` + outer = `volterraGlobal_spec`; `r≥2σ` ⇒ forcing ≡ 0 (exact; previously only numeric at `1e-17`). **(4)** `baxterU`/`_outer`/`_core` (discharges `baxter_u_core`'s `hcore`) + `baxter_seed_at_psi` (seed for the **constructed** `u`) + `baxter_psi_conv_eq_phi`. ⚠ **CORRECTION: no Fubini here** — the identity is *parenthesised* `(ψ⋆Q₊)⋆Q₋`, so `baxterUQm` **is** the LHS definitionally; associativity is `OZFIX.20`'s problem, not this task's. **Proof shape:** every `a.e.` is a **single endpoint** (`measure_singleton` + `integral_congr_ae`). ⚠ **Lean:** `integral_congr_ae`'s a.e. filter is over **`volume`**, NOT `volume.restrict (Ι a b)`. → [ozfix](proof_notes_ozfix.md) `OZFIX.15` | `HardSphere/BaxterRenewal.lean` |
| `OZFIX.16` | **The `1D-odd ↔ 3D-radial` bridge: `d/dr[r·(f ⊛₃ g)(r)] = −2π·(f̃ ⋆ g̃)(r)`** — turns the real-space Baxter identity `ψ⋆Q₊⋆Q₋=φ` into OZ for all `r>0` | ✓ **DONE 2026-07-17 — BOTH HALVES PROVED, axiom-clean** (`HardSphere/BaxterRenewal.lean`). Target theorem **`hasDerivAt_radial3d_conv_bridge`**. Chain: `radial3d_conv_eq_oddExt` (kills the `\|r−t\|` via `integral_shell_eq_oddExt`) + **brick 1 `hasDerivAt_shell`** (FTC on the shell: `d/dx ∫_{x−t}^{x+t} φ = φ(r+t)−φ(r−t)`, needing continuity of `φ` **only at the two endpoints**; two `integral_hasDerivAt_right` against a common base + `integral_interval_sub_left`) + **brick 2 `hasDerivAt_tIntegral_shell`** (differentiate under the `t`-integral) + `oddExt_conv_fold` (the measure-theoretic half: `∫_ℝ f̃(t)g̃(r−t)dt = −∫_{Ioi 0} t·f(t)(g̃(r+t)−g̃(r−t))dt`, by `ℝ = Ioi 0 ⊎ Iic 0` + reflection). **Key insight — the obvious lemma is the WRONG one:** the physical `g` is `oz_h`, which **jumps at contact**, so `hasDerivAt_integral_of_dominated_loc_of_deriv_le` (derivative for **all `x` in a ball**) has a **false** hypothesis here — every ball around `r` catches a positive-measure set of `t` whose shell is non-differentiable at some `x` with `x±t=±σ`. Use **`hasDerivAt_integral_of_dominated_loc_of_lip`**: `h_diff` only **at the base point `r`, a.e. in `t`** (at fixed `r` just `t=\|σ∓r\|` is bad = measure zero), with a **Lipschitz** hypothesis carrying the ball — and a jump does not destroy Lipschitz. Domination data carried as explicit hypotheses (conditional-theorem pattern); dischargeable for FMSA (`f`=`q0_poly` compactly supported, `g̃` locally bounded). Helper `intervalIntegrable_of_locallyIntegrable`. **Non-circular** (pure analysis ⇒ no `OZFIX.14` value-neutrality trap). Consumer needs **no FT injectivity** — an antiderivative argument suffices (matches `MA.9`). → [ozfix](proof_notes_ozfix.md) `OZFIX.16` | `HardSphere/BaxterRenewal.lean` |
| `OZFIX.17` | **Assembly ⇒ `oz_core_closure` becomes a THEOREM (Phase C)** | ☐ **RE-SCOPED 2026-07-17 — was NOT one task.** Investigating the chain found a structural obstacle the old sketch hid: **`Q̂` is a 1D transform but `Ĉ` is a 3D radial one** (`Qhat_complex` of `q0_poly` vs `radial_fourier` of `c_HS`), so `OZFIX.2`'s `(1−Q̂(k))(1−Q̂(−k)) = 1−ρĈ(k)` does **not** make `1−ρĈ` the transform of `δ−ρc̃`; the two differ by `2πi/k` = an **antiderivative** — exactly the `d/dr` on the left of `OZFIX.16`'s bridge. *The old plan's "then FT injectivity or a real-space projection" underestimated this.* **Repair = Baxter's `K`** (bricks landed, axiom-clean): `K(v) := 2π∫_{\|v\|}^σ s·c_HS(s)ds` has `F[K] = Ĉ` (integrate by parts), so the factorization reads **in real space** `Q₊⋆Q₋ = δ−ρK`, and `ψ⋆(δ−ρK) = φ` **is** OZ. Landed: `baxterK`, `baxterK_neg`, **`baxterK_outer`** (`K≡0` off the core ⇒ **no improper integral**), **`hasDerivAt_baxterK`** (`K' = −2π·c̃` on `(0,σ)` ⇒ **`OZFIX.16` is the *differentiated* form of what this needs**). `v<σ` is **not** convenience: `c_HS` **jumps at contact** ⇒ FTC's `ContinuousAt` genuinely fails at `σ`. **Now depends on `OZFIX.18`+`OZFIX.19`+`OZFIX.20`**; this task = final assembly ⇒ `ψ/r` satisfies `OzFixedPt` (core branch definitional via `baxterPsi_core`; exterior via `oz_forcing_add_linear_op_eq_radial3d_conv`) ⇒ `oz_h = ψ/r` by `oz_fixed_pt_unique` ⇒ **`oz_core_closure` retired** (it is exactly OZ on `(0,σ)`). ⚠ Consumes `oz_fixed_pt_unique` (OZ.10) ⇒ net 3→2 physics axioms unless `MA.10` also retires OZ.10 (Volterra factors ⇒ `(I−K)` invertible), which would retire **both** + unblock **OZ.3**. → [ozfix](proof_notes_ozfix.md) `OZFIX.17` | `HardSphere/PYOZ_GHS.lean`, `HardSphere/BaxterRenewal.lean` |
| `OZFIX.18` | **Real-space Baxter factorization `Q₊ ⋆ Q₋ = δ − ρ·K`** | ◑ **CORE FORM `rho_baxterK_eq_q0_self_conv` PROVED 2026-07-17, axiom-clean**: `ρ·K(v) = q0(v) − ∫_v^σ q0(t)·q0(t−v)dt` for `v∈(0,σ)` (the double integral IS `(q0·1_{[0,σ]} ⋆ q0(−·)·1_{[−σ,0]})(v)`). Proof = **two FTC evaluations + `field_simp`/`ring` under `heta_def`** (degree-4 antiderivative `Gpoly` for `∫ s·c_HS`, degree-5 `Fpoly` for `∫ q0·q0`; `HasDerivAt` via `hasDerivAt_pow`+`congr_of_eventuallyEq`), mirroring `baxter_factorization_inner`. Numerically+symbolically pre-verified (5 params to 1e-15, sympy `LHS−RHS≡0`). Plus sub-lemma `F[K]=Ĉ` PROVED (`baxterK_cos_eq_radial_fourier`: `2∫₀^σ K(v)cos(kv)dv = radial_fourier(c_HS)(k)`, one IBP via the proved `hasDerivAt_baxterK` — boundary term dies since `K(σ)=0`, `sin(0)=0`; support reduction reuses `radial_fourier_c_HS_eq_intervalIntegral`; **no decay needed**, `K`/`c_HS` compactly supported). Continuity of `K` on `[0,σ]` handled via the `polyC` primitive (`intervalIntegral.continuous_primitive`). **Remaining = the real-space factorization itself.** Split from `OZFIX.17` 2026-07-17. The missing link that makes `OZFIX.2` usable in real space. Two routes: transport `baxter_wiener_hopf_complex` across FT injectivity, or (likely cleaner, and the project's precedent) prove it directly by Baxter's classic algebra alongside `baxter_factorization_inner` (the proved `c ↔ q` half, same file). **Sub-lemma worth stating separately: `F[K] = Ĉ`** (`radial_fourier (c_HS) k = ∫_ℝ K(v)e^{−ikv}dv`), an integration-by-parts identity — the boundary term dies and `(4π/k)∫₀^∞ s·c(s)sin(ks)ds` reappears. `baxterK`/`baxterK_outer` already landed. → [ozfix](proof_notes_ozfix.md) `OZFIX.17` | `HardSphere/BaxterRenewal.lean` |
| `OZFIX.19` | **The antiderivative bridge `r·(f ⊛₃ g)(r) = (g̃ ⋆ K_f)(r)`** | ✓ **DONE 2026-07-17, axiom-clean** (`radial3d_conv_eq_baxterK_shell`): `r·(c_HS ⊛₃ g)(r) = ∫₀^σ K(u)·(g̃(r−u)+g̃(r+u))du` for `r>0` (= `(K⋆g̃)(r)` folded onto `[0,σ]` by `K` even). **KEY REALIZATION: it is NOT a differentiation argument — just Fubini** (differentiate-in-`u` would break at `u=σ−r` where `g̃=ψ` jumps; Fubini only needs integrability, jump-proof). Expand `K(u)=2π∫_u^σ s·c(s)ds` and swap the `(u,s)` order over the triangle `{0≤u≤s≤σ}`; inner change-of-var gives `∫_{r−s}^{r+s}g̃`. Reusable helper **`intervalIntegral_triangle_swap`** (`∫₀^a(∫_u^a p)q = ∫₀^a p(∫₀^s q)`, via `integral_integral_swap`+indicators) also landed, axiom-clean. Conditional-theorem hyps: shell interval-integrability + triangle joint-integrability (dischargeable for concrete ψ, locally bounded). **The un-differentiated `OZFIX.16`.** **Both halves of the derivative are already proved**: `OZFIX.16`'s `hasDerivAt_radial3d_conv_bridge` gives `d/dr[r(f⊛₃g)] = −2π(f̃⋆g̃)`, and `hasDerivAt_baxterK` gives `K_f' = −2π·f̃` ⇒ both sides have **equal derivatives**. Remaining = pin the constant (both sides vanish for large `r`; `K_f` is compactly supported by `baxterK_outer`) — i.e. an `eq_of_hasDerivAt`-style argument, **no FT injectivity** (consistent with `MA.9`, where the pointwise-inversion hypothesis provably fails at a contact jump and the antiderivative form is the usable one). → [ozfix](proof_notes_ozfix.md) `OZFIX.17` | `HardSphere/BaxterRenewal.lean` |
| `OZFIX.20` | **Convolution associativity `(ψ ⋆ Q₊) ⋆ Q₋ = ψ ⋆ (Q₊ ⋆ Q₋)` (Fubini)** | ✓ **DONE 2026-07-17, axiom-clean** (`dbl_conv_reindex`): `∫₀^σ∫₀^σ q0(t)q0(s)ψ(r+t−s) = ∫₀^σ(∫_u^σ q0(t)q0(t−u)dt)(ψ(r−u)+ψ(r+u))` for **any** ψ (numerically verified 3 ψ to 1e-16). **Both sides reduce to `∫ t in 0..σ,∫ s in 0..t, q0(t)q0(s)(ψ(r+t−s)+ψ(r+s−t))`**: RHS by `intervalIntegral_triangle_swap_gen` (the new 2-var triangle Fubini) + inner change-of-var `s=t−u`; LHS by splitting the inner at the diagonal `s=t` and mapping the `{t<s}` half onto `{s<t}` via a second `_gen` swap (bound-var relabel) + integrand symmetry. Stated with 6 integrability hyps (conditional-theorem pattern, dischargeable for `baxterPsi`). *This* is where associativity is genuinely needed — **not** in `OZFIX.15`, whose statement is left-parenthesised by construction (`u := ψ⋆Q₊` is built first, then `Q₋` applied), so it carries no associativity obligation. Needs integrability side-conditions; `Q₊`'s kernel is compactly supported on `[0,σ]` and `ψ` is locally bounded (piecewise continuous, one jump at `σ`), so Tonelli/Fubini should apply on the relevant compacts. → [ozfix](proof_notes_ozfix.md) `OZFIX.17` | `HardSphere/BaxterRenewal.lean` |
| `OZFIX.21` | **OZ★ linchpin + axiom consolidation** | ✓ **OZ★ DONE 2026-07-17, axiom-clean** (`baxterPsi_eq_phi_add_rho_conv`): `baxterPsi(r) = r·c_HS(r) + ρ·r·radial3d_conv(c_HS, baxterPsi/·)(r)` ∀`r>0` — the concrete real-space **OZ equation** solved by the constructed `baxterPsi`, assembled from `baxter_psi_conv_eq_phi` (`OZFIX.15`) + `radial3d_conv_eq_baxterK_shell` (`OZFIX.19`) + `rho_baxterK_eq_q0_self_conv` (`OZFIX.18` KDEF) + `dbl_conv_reindex` (`OZFIX.20`), with brick **`oddExt_div_self_eq_baxterPsi`** folding `oddExt(baxterPsi/·)=baxterPsi`. Two `have`s share one RHS `E=Aminus+Aplus−Adouble`; close by `rw [claimA, claimB]`. **14 integrability hyps** (conditional, **decay-FREE** — dischargeable from `baxterPsi` bounded-on-compacts, NOT decay). ⚠ Lean: `integral_congr_ae` gives the *implication* ae-form `∀ᵐ x, x∈uIoc→f=g` (`filter_upwards [hne] with u hune hmem`), not the `=ᵐ[restrict]` form. **This is the shared linchpin for the axiom consolidation (`OZFIX.22`).** ✓ Consumed by OZFIX.22. → [ozfix](proof_notes_ozfix.md) `OZFIX.21` | `HardSphere/BaxterRenewal.lean` |
| `OZFIX.22` | **Axiom consolidation — RETIRE `oz_core_closure` + `oz_h_exterior_regularity` (net 3→2)** | ✓ **DONE 2026-07-17, full build green (8653 jobs), no cycles.** New file `HardSphere/OzCoreClosure.lean`: `radial3d_conv_cHS_congr` (decay-free brick: `radial3d_conv c_HS g r` depends on `g` only on `(0,r+σ)`), `oz_core_closure_of_bridge` (decay-free reduction: closure = OZ★ ⊘ r + bridge), `ozBaxterFixedPt := if r<σ then -1 else baxterPsi r/r`, `oz_h_eq_ozBaxterFixedPt` (**the bridge**, `oz_h = ozBaxterFixedPt` via `oz_fixed_pt_unique`: `ozBaxterFixedPt` is a bounded exterior-continuous `OzFixedPt` — fixed-pt from `oz_forcing_add_linear_op_eq_radial3d_conv` + OZ★ + domain-congruence; bounded from decay). **RETIRED:** `oz_core_closure` (theorem in `OzCoreClosure.lean`, axiom deleted from `PYOZ_GHS.lean`) + `oz_h_exterior_regularity` (theorem in `JumpAsymptotic.lean` by **one `rw` of the function-equality bridge** into `baxter_exterior_regularity`'s clause; axiom keyword removed). **KEPT:** `oz_fixed_pt_unique` (irreducibly Wiener–Hopf — bounded uniqueness ⇏ from decay; `L¹` contraction REFUTED). **NEW axiom:** `baxter_exterior_regularity` (explicit `baxterPsi`, replaces 2 opaque-`oz_h`/physics axioms with 1). ⚠ **Finding:** full decay-free retirement of all 3 is IMPOSSIBLE — the earlier "decay-free 3→2 merge" (Consolidation B) and "3→1" (Consolidation A) designs were over-optimistic (causal-renewal conversion for a *general* fixed point needs the factorization; uniqueness is irreducible). Ceiling = 3→2. → [ozfix](proof_notes_ozfix.md) `OZFIX.22` | `HardSphere/OzCoreClosure.lean` |
| `OZFIX.24` | **RETIRE `ozExterior_triple_shell_sin_integrable` (6h): axiom → theorem** | ✓ **DONE 2026-07-19, full build green (8675 jobs), no new axiom, no `sorry`** — `axiom` keyword deleted, name/signature unchanged so no consumer edit needed. It had been axiomatized on an **effort** argument ("formalizing the triple `lintegral` chain is substantial"), not a gap argument — in tension with Group MA admissibility rule (c); same category as MA.10/11/12, all retired by proving. Four steps: `volume_ozShell_slice_le` (slice ⊆ `Icc |s−t| (s+t)`, length `2·min(s,t) ≤ 2t`, axiom-clean, needs no positivity) + `lintegral_shell_weight_c_HS_lt_top` (`t`-factor, `c_HS_outer`+`c_HS_bddOn`) + `r_mul_ozBaxterFixedPt_integrableOn_Ioi_zero` (`s`-factor: bounded core ∪ **MA.13's strengthened `IntegrableOn`** exterior) + `lintegral_ozShellMajorant_lt_top` (assembly). ⚠ **`lintegral_prod_symm` to put the `a`-integral INNERMOST is the whole trick** — in the measure's own nesting the unbounded `a`-axis is outermost and the estimate does not close; the swap turns the shell indicator into a *measure*, which is where the finite `2t` comes from. Sketch was correct (no statement bug). `#print axioms` → standard three + MA.13 + MA.14, both pre-existing upstream via the exterior-`L¹` input ⇒ **no downstream footprint grew** (6j has the identical list). Math axioms **10 → 9**; cluster 6g/6h/6j now **entirely axiom-free**. → [ozfix](proof_notes_ozfix.md) `OZFIX.24` | `HardSphere/BaxterExteriorConvIntegrable.lean` |

### Group F — Free Energy Integrals *(free_energy)*

| Task | Title | Status | Lean file |
|------|-------|--------|-----------|
| F.1 | Outer-core free energy integral | ✓ DONE | `FreeEnergy/OuterIntegral.lean` |
| F.2a | Inner-core energy integral (E_ij part) | ✓ DONE | `FreeEnergy/InnerIntegral.lean` |
| F.2b | LJ inner-core integral identity | ✓ DONE | `FreeEnergy/LJIntegral.lean` |
| F.3a | Free energy convergence (FMSA_GA_matrix_mix route) | ✓ DONE | `FreeEnergy/Convergence.lean` |
| F.3b | Free energy convergence (LJ/FMSA_poly route) | ✓ DONE | `FreeEnergy/Convergence.lean` |
| F.4 | Compressibility sum rule | ✗ deleted | — |
| F.5 | Contact-value approximation error | ✓ DONE | `FreeEnergy/ContactError.lean` |
| F.6 | FMSA_GA_matrix_mix exact vs LJ free energy comparison | ✓ DONE | `FreeEnergy/SumRule.lean` |

**F.4 deletion note:** The compressibility sum rule `∂(βP)/∂ρ = 1−ρĉ(0)` mixes the energy-route
free energy (what FMSA computes: `∫ u g₀ r² dr`) with the compressibility-route DCF (`ĉ(0)` from
integrating c(r)). For any first-order approximate theory these two routes give different numbers —
the gap measures route inconsistency, not model quality. Verifying route inconsistency numerically
is not a useful model check. Deleted.

### Group FW — White-Bear FMT / BMCSL Mixture Thermodynamics *(hard_sphere)*

| Task | Title | Status | Lean file |
|------|-------|--------|-----------|
| FW.1 | FMT species symmetry: `betaf_hs` depends on `rho` only through `∑ᵢρᵢ` when all diameters equal | ✓ DONE, no axiom (`betaf_hs_species_symmetry`) | `HSMixture/WhiteBearFMT.lean` |
| FW.2 | BMCSL/White-Bear thermodynamic consistency: virial pressure (from `g0_bmcsl`) = FMT scaled pressure (from `betaf_hs`) | ✓ DONE, no axiom (`bmcsl_virial_eq_fmt_pressure`) | `HSMixture/WhiteBearFMT.lean` |


### Group 5 — Matching at Contact *(yukawa_dcf)*

| Task | Title | Status | Lean file |
|------|-------|--------|-----------|
| 5.1 | Inner/outer matching at r=R_ij (2YK only) | ✓ structural (I1/I2=0) / **⊥ physical claim disproved** | `YukawaOZ/ContactMatching.lean` |

**5.1 disproof note:** The Lean proof establishes only that `I1(0) = I2(0) = 0` (integral over an
empty interval — trivially true) and `eij(R,R) = ΣA_k`. It explicitly states in a comment that
full DCF continuity additionally requires the MSA closure condition `K = A_k` at each Yukawa
pole, which is NOT proved and does NOT hold.

Numerical evidence from `FMSA_pure` (verify_pure.py V.1): the first-order c₁ gap at r = d_bh is
~1.1–1.7 across liquid-range state points, far from the ~1e-6 numerical-precision level.  The
total-c gap is smaller (~0.02–0.7) only because the FMT c₀ accidentally partially compensates.
Since the pure-fluid limit is the simplest case, the same discontinuity must appear in mixtures.
The intended physical matching property (DCF continuity at contact for the 2YK FMSA) is false.

---

*Numerical verification tasks are in `../todo/todo_numerical.md`.*


### General-N (2026-07-29) — ✅ mixtureDet_pole_free_N UNCONDITIONAL, all N, no new axiom
`MixtureDetGeneralN.lean`: det Q0_mat_c_phys(I·z,σ,ρ)≠0 open LHP, **any N**, physical mixture; `#print axioms`=same existing 2. Via `matDet_zeroFree_lowerHalfPlane_of_homotopy` {N} on entire ψ-matrix `Mdens` (hholo=`Mdens_hholo`, hcont=`Mdens_hcont`, hbase=dilute I, hreal=no_spinodal {N} + k=0 compressibility `detMdens_origin_ne_zero` axiom-clean via `Q0_mat_phys_det_ge_one_N`=rank2+moment_key). **Escape `hbound` SOLVED** (`exists_uniform_escape`, axiom-clean): phase `e^{−λ_ij s}=e^{σᵢs/2}e^{−σⱼs/2}`=diagonal similarity ⇒ `det Q̂₀=det Mgauge` (phase stripped) [`det_Q0_eq_det_gauge`]; `Mgauge=1−A`, `‖A‖_{L∞op}≤Abnd/‖s‖→0` (φ bounds `‖φ₁‖≤(2+σ)/‖s‖` via `‖e^{−sσ}‖=e^{−σRe s}≤1`; uniform-t via `Abnd_cont`+compact) ⇒ unit ⇒ det≠0. Cleaner than N=2 monomial bridge, no complex VU. **OZFIX.17 matrix pole↔decay RESOLVED ALL N.**


### MML.8 re-evaluation (2026-07-29) — OZ★-route assembled up to one scoped gap
`MixtureRDFUniqueness.lean` (axiom-clean, ledger 7): matrix OZ★-route to the inner-core RDF *value* (non-circular OZFIX.22 analog) now built up to a single gap. `matOzStar_sub_hom` (diff of two MatOZStar solutions solves homogeneous `MatOZHom`) + `matOzStar_unique_of_injective` (conditional matrix `oz_fixed_pt_unique`, gap = `hinj` matrix WH injectivity) + `matStructureFactor_isUnit_of_det_ne_zero` (`det Q̂₀≠0` ⇒ `T₀=Q̂₀(k)Q̂₀(−k)ᵀ` unit via `det_eq_of_wienerHopf_factorization` — coercivity input from `mixtureDet_pole_free_N`+no_spinodal). **Stale blocker cleared**: "awaits matrix OZ★" — OZ★ now built. **Remaining gap = `hinj`** = matrix analog of scalar KEPT axiom `radialShell_bounded_injective` (L∞-Wiener-algebra gap, MA.13-class; kept as hypothesis, no new axiom). **Literal pole-series collapse `MixRDFInnerCollapse` stays circular** (Fourier, inherited from scalar CoreSeriesClosure).

### L² matrix Wiener–Hopf PROVED via matrix Plancherel (2026-07-30) — axiom-clean, mirrors MA.12
`Analysis/MatrixWienerHopf.lean` `FMSA.matWienerHopf_positive_symbol_injective` (standard-3 only, NO axiom): matrix analog of MA.12 `wienerHopf_positive_symbol_injective`. New ingredient `matrix_quadratic_coercive` (real coercivity ⇒ complex via Re/Im split); reuses scalar mulLp/𝓕/inner_fourier_eq/norm_fourier_eq componentwise (tuple Fin N→Lp ℂ 2). Chain: `mulLp_inner_re`→`matMulLp_coercive`→`matFourierMul_coercive`→WH injectivity. **Matrix L²/L∞ pair now mirrors scalar MA.12/MA.15**: L²=theorem, L∞=axiom `matRadialShell_bounded_injective` (bounded, MML.8 value route). Ledger unchanged 8.
### Group PYE PYE.1–PYE.3 CLOSED (2026-07-31) — no-pull-back construction ≡ first-order PY, **no new axiom**
`Closures/ClosureExpansions.lean` (ns `FMSA.FirstOrderClosure`; wired into `LeanCode.lean`). **Re-audited 2026-08-02: `#print axioms` = standard three on ALL 39 declarations** (the older "26" predates the PYE.4 block), no `sorry`, build 8718. **One gap found and closed:** PYE.3/PYE.4's physical-form theorems (`dp_sub_py_first_order_eq_conj_residual`, `dp_eq_py_first_order_of_exact_fit`, `dp_tendsto_py_first_order`) all carry MRS.2's **five simultaneous matrix equations** with **no non-vacuity certificate** — had they been unsatisfiable all three would be vacuously true and `#print axioms`/build/review would stay silent (the `b4_origin_bc_abstract`/`b9_d_ij_nonzero_example`/GAP.8 failure mode). A **non-degenerate** witness (`Qm ≠ 1`, `C₁^PY ≠ 0`, i.e. not the trivial `Q̂₀=I` dilute point) is now a Lean `example` in the file: `Q=!![2]`, `T₀=Q·Qᵀ`, `S₀=T₀⁻¹`, `C₁=Q·B₁·Qᵀ`, `H₁=(Qᵀ)⁻¹B₁Q⁻¹`. ⚠ Read it as non-vacuity only — `hT0symm` is **automatic when `Qp=Qm`** (`(QQᵀ)ᵀ=QQᵀ`), which the physical case does *not* enjoy (distinct `Q̂₀(k)`, `Q̂₀(−k)`; that is why MRS.7 needed KEY-1/KEY-2 and a five-exponential cancellation). **PYE.1**: PY `c=(e^{−βsu}−1)y` ⇒ `c₁ = y₀·(−βu)` (`py_first_order_outer` — the `y₁` term drops *because* `mayerF 0 = 0`, which is also `c₀(r>R)=0`), `y₀=g_HS` via the cavity function (`cavity_zero_coupling`); MSA is the `y₀↦1` case. The group's identification is `pass_zero_eq_py_first_order`: the HNCB coefficient `g_HS(−βu)+h_HS·γ₁` at `γ₁≡0` **is** PY's. **PYE.2**: three linear stages — Y1.2 transform, Y1.5 `bMulti` (linear in `K`), MRS.3 (★) conjugation (linear in `B₁`, no `Q̂₀⁻¹`) — bundled as `dpDCFLinear`/`dpTailsLinear`; ⚠ linearity **requires frozen rates** (free rates sit in `s+z_mp`), which is independently why the numerics needed `tail_mode='linear'` (`hncb_first_order.md` §4(b) overflow). **PYE.3**: `DP[fit]−DP[exact]=DP[fit−exact]` is `map_sub`; the planned analytic input **`wh_solution_operator_bounded` was NOT needed as an axiom** — the entrywise chain `bMulti_entry_norm_le` ∘ `starConj_entry_norm_le` gives the explicit `whOperatorBound N T Qb Ab δ = T·N⁴·Qb²·Ab²/δ` (`δ` = distance to the Yukawa poles), so `dp_error_entry_norm_le` reads `‖DCF error‖ ≤ whOperatorBound·‖fit residual‖`. Against the exact PY DCF (MRS.2 `star_of_first_order_oz`) the error is the (★) assembly of the WH-datum residual. **PYE.4 ◑**: exact-fit and limit halves proved; the L¹/L² lift stays a `Prop` hypothesis (`TailFitWHConvergent`), per the `MixRDFInnerCollapse` discipline. **Axiom tables unchanged** (still 10 raw `^axiom` hits = 2 prose false positives + 7 math + 1 physics). Side effect: HNCB.1's `exp`-form half now exists (`hncb_first_order_outer`) — extend, don't re-derive.

### PYE.5 + PYE.6 (2026-08-03) — the k→r bridge, and the zeroth-order factor IS PY's (N=1)
Two gaps between Group PYE's k-space theorems and the physical claim "the **inner-core** DCF of the no-pull-back construction is the first-order PY one" are now closed in `Closures/ClosureExpansions.lean` (58 decls, all `#print axioms` = standard 3, full build 8718 jobs, axiom count unchanged at 10 raw hits). **PYE.5 (k→r bridge)**: `radial_fourier_injective` on the project's own `radial_inversion` (`Analysis/RadialFourierInversion.lean` — a THEOREM; the MA.9 axiom was never taken) + the one-pair dictionary `IsRadialEntry` (`Ĉ_ij(k) = √(ρᵢρⱼ)·𝓕_r[c_ij](k)`, an explicit hypothesis because PYE characterizes `Ĉ₁` axiomatically) ⇒ `inner_core_dcf_eq_of_khat_eq` and the capstone `inner_core_dcf_eq_of_exact_fit`: MRS.2 equations at every real `k` + exact fit ⇒ `c₁^fit(r) = c₁^PY(r)` **pointwise on `(0,R)`**; structural non-vacuity (5 MRS.2 equations + both dictionaries, arbitrary `c`, `Qm ≠ 1`) certified by `example`. ⚠ **open core only** — the DCF jumps at contact and `radial_inversion` needs continuity there (the `radial_inversion_antideriv` trap); ⚠ **no real-space error bound** — `whOperatorBound` is k-uniform, so `k·Ĉ₁(k)` is `O(1)` and not `L¹`; a weighted k-space estimate would be needed. **PYE.6 (zeroth-order factor)**: the identification was **already proved in the library** — BAXTER.3 `baxter_wiener_hopf_complex` gives `(1−q̂₀(k))(1−q̂₀(−k)) = 1−ρĈ_PY(k)` with `q0_poly`'s coefficients being Wertheim–Thiele `q_prime_py`/`q_doubleprime_py` and `c_HS` the PY DCF, i.e. **MRS.8 at `N=1`**; `pyhs_no_spinodal` (OZ.20) gives invertibility; the MRS.0b chain (`Q0phys_n1`/`Qppphys_n1`/`qhat_complex_eq_mixture_kernel`) makes it the same `Q̂₀` the mixture construction uses. Wired in as `pyBaxterMat`/`pyT0Mat`/`pyT0Mat_entry`/`pyT0Mat_isUnit_det`/`dp_zeroth_order_is_py_n1`/`dp_eq_py_first_order_n1` ⇒ at `N=1` MRS.2's `hfact`+`hT0symm`+`hTS` are **discharged with the physical PY objects** and only `hoz` (the first-order OZ equation) remains an input; non-vacuity for every `H₁` by `example` (`C₁=T₀H₁T₀`, `B₁=Q̂₀ᵀH₁Q̂₀`) with the genuinely distinct `Q̂₀(k)`/`Q̂₀(−k)`. **General `N` stays MRS.8** (deferred, off the critical path).

### PYE.7 CLOSED (2026-08-03) — Yukawa-tail fits are dense; PYE.4 still open
`exists_yukawa_tail_fit` (+ `exists_yukawa_tail_fit_pyOuter` for the construction's actual target `r·c₁ = r·g_HS·(−βu)`), axiom-clean, in `Closures/ClosureExpansions.lean` (61 decls; full build 8718 jobs; axiom count unchanged at 10). Every function continuous on `[R, R+L]` is uniformly approximable there to any `ε` by `Σ_{t<n} A_t e^{−z_t r}` with **strictly positive, strictly increasing** rates ⇒ the residual PYE.3's bound multiplies is not bounded away from 0. **The planned Stone-Weierstrass route was not used and is not the cheapest**: `t = e^{−r}` maps the window homeomorphically onto `[e^{−(R+L)}, e^{−R}]` and turns *polynomials in `t`* into *exponential sums*, so Mathlib's `exists_polynomial_near_of_continuousOn` gives the density directly — no subalgebra, no `SeparatesPoints`, and crucially no `Algebra.adjoin`→span extraction (where the SW route's effort would have gone: recovering an explicit finite sum from abstract adjoin membership). One extra `δ`-shift `k ↦ k+δ` turns the `k=0` constant into a legal decaying tail, cost `≤ (Σ|c_k|)·δ·(R+L)`. Bonus: the rates are `k+δ`, **unit-spaced** — a third independent argument (after PYE.2's linearity and §4(b)'s overflow) for fixed, well-separated rates. ⚠ **PYE.4 is NOT closed by this**: (a) the fit is uniform on a *compact window* while the WH projection integrates over `[R,∞)` (fitted amplitudes may grow as `ε→0`, so their tail is uncontrolled), and (b) this is a sup-norm **function** residual, whereas PYE.3's bound consumes the **coupling-matrix** distance `‖K^fit − K^exact‖` — converting between them *is* the function-space WH projection, i.e. the lift itself.

### PYE.4 REDUCED to one classical input (2026-08-03) — the function-space lift, built
`Closures/ClosureExpansions.lean` (71 decls, all `#print axioms` = standard 3, full build 8718 jobs, axiom count still 10, no sorry). **Proved**: (i) `exists_yukawa_tail_fit_halfLine` — a closure vanishing at infinity is uniformly approximable by Yukawa tails on the **whole half-line** (same `t = e^{−r}` substitution as PYE.7 but on the **closed** `[0, e^{−R}]`, into which `[R,∞)` maps; vanishing-at-∞ = continuity at `t=0` with value 0, which also lets the constant term be dropped ⇒ rates ≥ 1); (ii) `exists_yukawa_tail_fit_L1` — the **L¹** version for exponentially decaying closures, by fitting the *weighted* closure `e^{a(r−R)}Ψ` and absorbing the weight into the rates, `‖·‖₁ ≤ (εa)(1/a) = ε` (+ `integral_exp_neg_shift`, `integrableOn_exp_neg_shift`, `integrableOn_tailSum`); (iii) `norm_transform_le_L1` — `‖Û₁(k)‖ ≤ ‖Ψ‖_{L¹}` **uniformly in k**; (iv) the reduction `tailFitWHConvergent_of_L1_tendsto` + capstone `exists_tailFit_whConvergent` ⇒ **`TailFitWHConvergent` is now DERIVED** from L¹ convergence + one operator bound, and PYE.4b supplies the L¹-convergent family. **Remaining = `WHProjectionL1`** (hypothesis, not an axiom): the WH projection is L¹-bounded, which is `Q̂₀⁻¹`'s kernel ∈ L¹ = **Wiener's 1/f for the causal convolution algebra** = the MA.13/MA.15 family (kept axioms precisely because Mathlib has no Wiener algebra; a second copy inside a physics file would be wrong). **Three findings**: (a) **the amplitude route is a DEAD END** — `closure ↦ amplitudes` is unbounded (near-degenerate rates, `hncb_first_order.md` §4(b) measured `K̃=[−441,+435]`), so PYE.3's `whOperatorBound` cannot be lifted; the lift must bound the WH data directly in L¹; (b) **the norm is forced** — the causal projection is Riesz-type and is NOT sup-norm bounded on the k-axis, so the "obvious" uniformly-in-k hypothesis would be the *wrong* statement; real-space L¹ is where every stage is bounded; (c) **a vacuity trap, live for one iteration** — quantifying `WHProjectionL1` over ALL `Ψ : ℝ → ℝ` silently forces `B1of ≡ 0` (junk `∫|Ψ| = 0` at non-integrable inputs kills the map, then `map_sub` propagates it to every integrable function), so both fields are now restricted to integrable inputs and a non-degenerate `example` (`B1of Ψ = (∫Ψ)·I`, `C = 1`, nonzero on `e^{−(r−R)}`) certifies the class is inhabited. Same failure family as the 08-02 five-equation audit.

### MML.16 (2026-08-03) — the RDF "even pseudo-trace" is annihilated EXACTLY, and [LN] deviates from Tang & Lu here
Question: the WH machinery runs on the **unilateral** `Ĥ₁` while the physical object is the **sine** transform `H̃₁` (odd extension of `r·h₁`) — can the even part contaminate `B₁`? **Checked against the 1995 original** (`pdf/sources_paywall/Analytical solution of the Ornstein-Zernike equation for mixtures.pdf`, Mol. Phys. **84**, 89–103, pp. 91–94), not the notes: Tang & Lu **keep the exact link** `H̃(k) = [Ĥ(−k)−Ĥ(k)]/(ik)` (p. 92) and therefore **write both parity partners explicitly** in Eq. (9)/(10) — `Q̂₀ᵀ(k)Ĥ₁(−k)Q̂₀(k)` (RDF) and `−[Q̂₀(−k)]⁻¹Ĉ₁(−k)[Q̂₀ᵀ(−k)]⁻¹` (DCF); the space analysis (p. 93) puts them in `[−∞,R]` and the WH argument (p. 94, "both sides should vanish") discards them, giving Eqs. (14)/(15). **So the artifact is O(1) and exactly annihilated by support disjointness — neither small nor convergent-to-zero.** Lean (`YukawaOZMix/ReflectedTermSupports.lean`, ns `FMSA.ReflectedSupports`, axiom-clean, wired into `LeanCode.lean`): `support_comp_neg`/`reflected_support_Iic` (reflection reverses support; `h₁` on `[R,∞)` at every order ⇒ time-reversed on `(−∞,−R]`), `Icc_add_Iic_subset`/`Iic_add_Icc_subset`/`Iic_add_Iic_subset` (Minkowski, mirror of `MixtureConvolution`'s `Ici` helpers), **`reflected_rdf_conj_support`** (Proof 2 RDF half ⇒ `(−∞,−λ_ij]`), **`antiCausal_triple_support`** (Proof 2 conjugation half — the `S₁` term at `b:=R` and the DCF partner at `b:=0` in one lemma), **`parity_partners_causal_projection_zero`** (projection of a `(−∞,R)`-supported term is *the zero function*), `causal_projection_with_reflection`(+`_open`, endpoint-free — needed because like pairs have `λ_ii=0` and T&L's two spaces overlap at `r=R`), and a non-vacuity `example` **with `Wrefl ≠ 0`** (annihilation is about *where*, not *how big*). ⚠ **[LN] transcription seam**: `lecture_notes_OZ_Yukawa.tex` asserts `H̃₁(k)=Ĥ₁(k)` "since `h^{(1)}(r)=0` for `r<0`" — false in its own normalization (off by `1/(ik)` and the partner) and the stated reason is not the operative one; its `eq:rearranged` then carries a vacuous `−L+L` where the original has the partners, **while its own Proof 2 still discusses `Ĥ₁(−k)`**. Conclusion unaffected (Eq. 62/68 = original 14/15), but derive this step from the paper, not the notes. Contrast the **DCF** side, where the reflection is load-bearing: `oddPart_reduction` keeps `𝒲(r)−𝒲(−r)` and the C++ `reflection` razor moves **107 %** without it. **Atomic supports discharged too (same day)**: `q0MixEntryRefl_support_subset` (the **transposed** Baxter factor on `[−R_ij,−λ_ij]` — the atom Y1.3a lacked), `support_subset_Ici_of_hardCore`/`support_subset_Icc_of_window` (physics ⇒ support), `reflected_rdf_edge_eq` (`−λ_im+(−R_mn)+R_nj = R_ij−σ_m`: intermediate `n` cancels, `m` survives as a *margin*), `reflected_rdf_conj_support_physical` ⇒ `(−∞, R_ij−σ_m]` **from physics only**, and `reflected_rdf_conj_eq_zero_of_contact_le` ⇒ vanishing on the **closed** `[R_ij,∞)` (endpoint included, since `σ_m>0` = `Mix.hσ`) ⇒ the endpoint caveat is void for the partner. **Fourth atom closed WITHOUT MA.13 (2026-08-03)** — the finding: the Neumann series does diverge (`‖q‖₁>1`, the reason `wiener_causal_resolvent` is an axiom), but **the support question is local** and locally the renewal equation is unconditionally solvable by **MA.10** (proved, axiom-free): `volterra_convolution_existsUnique` gives existence+uniqueness on every window with no smallness (factorial iterate bound beats `‖q‖₁`), and `volterraGlobalE` is **zero off `[0,∞)` by construction**. So `renewalResolvent`/`renewalResolvent_support`/`renewalResolvent_spec`/`renewalResolventRefl_support` + `antiCausal_conj_delta_support` (conjugation by `δ+anti-causal` preserves `(−∞,b]`) + `eq10_antiCausal_side` put **both** remaining Eq. (10) terms in `(−∞,R_ij]`, `#print axioms` = standard three. **MA.13 is needed only for the GLOBAL L¹ resolvent** (POLE.11 decay), a strictly stronger claim — the two were conflated. Residual = a *dictionary* item (`[Q̂₀(−k)]⁻¹ = δ + R`, the real-space reading of `Q̂₀Q̂₀⁻¹=I`), algebra not analysis. Write-up: `FMSA_dp_output/md/rdf_parity_no_pseudotrace.md`.
