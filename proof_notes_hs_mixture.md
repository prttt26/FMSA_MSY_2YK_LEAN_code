# Proof Notes: Pure Hard-Sphere Mixture — Baxter factor, seed route, `c_HS` DCF (Group MIXCHS)

Proof records for the **pure hard-sphere mixture** layer — the `M`-independent Baxter/DCF content that
uses only `σ/ρ/Q0/Qpp` (the `Mix` fields *other* than the Yukawa-tail `zp`/`cb`). Carved out
(2026-08-11) as its own layer, distinct from the two Yukawa-tail mixture files:
`proof_notes_mixture_rdf.md` (Mittag-Leffler RDF, Groups MML/MZERO) and `proof_notes_mixture_dcf.md`
(inner-core DCF via the mediated `I1cl`/`I2cl` = `∫(α−v)e^{zv}`, Groups MRS/MPOLY) — **both** of which
are Yukawa-tail-level.

**Lean home.** `HSMixture/HSMix.lean` defines the Yukawa-free structure `HSMix N` (σ/ρ/Q0/Qpp/hσ) with
`R`/`lam`/`q0MixEntry`; the Yukawa `Mix N M` connects via `Mix.toHSMix` (`WHSupports.lean`), and
`q0MixEntry X = (X.toHSMix).q0MixEntry` is `rfl` for any `M` (M=0 = pure HS). So the pure-HS content
lives at the HSMixture layer and the Yukawa layer bridges at `M=0` (design validated 2026-08-11,
std-3, build 8763). The bulk of the content still physically sits in `YukawaOZMix/` pending the
incremental port (MIXCHS.5); this file is its authoritative record + index.

---

## Group MIXCHS — content index

All std-3, build 8763. Currently in `MixtureHSDCFFin1.lean` (YukawaOZMix) pending the port; being
re-based onto `HSMix`.

- **MIXCHS.1 ✅ N=1 Baxter ODE.** `baxterODE_n1`: `matDCFreCore'(s) = −2πρ·s·c_HS(s)` on `(0,σ)`, from `baxter_factorization_inner` + `hasDerivAt_corrM` + `q0_ibp` + the FTC/IBP tower.
- **MIXCHS.2 ✅ `cHSmixRenewal` + N=1 physical reduction.** `cHSmixRenewal := matBaxterUQmSymFullExt / r`; `= scalar c_HS` at equal diameters (`cHSmixRenewal_eq_cHS_of_equalDiam`); N=1 **unconditional** (`cHSmixRenewal_physMixN_fin1_uncond`) via the constructed Banach–Volterra renewal (`physMixPsiouter`), all three renewal conditions discharged.
- **MIXCHS.3 ✅ Windowing loss — both seed arms, general-N.** Un-transposed `matBaxterUExt` (largest species → explicit Lebowitz poly-moment loss `∑ₖ∫_{λ}^0 (Q0(t−R)+Qpp(t−R)²/2)·Ψ`, smallest → none) + transposed `matBaxterUtExt` (dual, via the `_eq_transpose` rfls). SUM = symmetrized fold-kernel `Ĉ₀` sub-zero tail (`windowing_loss_sum_eq_symTail`, any `N`; `physMixN_windowing_sum`, N≥3). Hint-free + physically instantiated at σ=max diameter. Numerically <1e-3 + grid-precision sum identity at N=2,3,4 (`mixwt_windowing_{largest,transposed,bootharms,genN}_check.py`); at N≥3 the middle species carries BOTH arms.
- **MIXCHS.4 ✅ CLEARED 2026-08-25 — REDUNDANT, not a live gap.** The unequal-σ pair DCF is owned unconditionally by the done value route (MML.8 `matOzStar_unequalDiam_unique_uncond` + MIXCHS.7-N `shellForcing_eq_cMixDCFN`); the extended-seed Lean formalization is a large redundant undertaking, left by design (see the ✅ EXTENDED-SEED note below). *(was: ◑ PARTIAL — moment ingredients COMPLETE, final assembly scoped, 2026-08-11)*
  - **Key structural finding:** the reduction `matBaxterUQm_coreSeed_moment` (`MixtureBaxterSeed.lean`) is **already general-N with NO equal-diameter assumption** — it proves the seed (core value `Ψ=−v`) EQUALS the explicit moment expression `(r·(∑ₖM0ᵢₖ−1) − ∑ₖM1ᵢₖ) − ∑ₖ∫₀^{σ−r} Qᵢₖ·((r+t)(∑ₗM0ₖₗ−1) − ∑ₗM1ₖₗ)`, in the windowed moments `M0ᵢₖ=∫₀^σ Qᵢₖ`, `M1ᵢₖ=∫₀^σ t·Qᵢₖ` — **no OZ solution `Ψouter`** (only the core value). The equal-diameter `matBaxterUQm_momentSeed_of_rowSum` collapses this to `r·c_HS` via `hrow`; the unequal-diameter assembly instead **evaluates** the moments.
  - **Ingredients all PROVED std-3** (`HSMixture/PhysHSMixWindowing.lean`): `baxterQuad_integral`/`baxterQuad1_integral` (FTC for the Baxter quadratic and its `t`-weight), `q0MixEntry_baxterMoment` (M0 on `[λ,R]`), `q0MixEntry_baxterMoment1` (M1 on `[λ,R]`), `q0MixEntry_affineMoment` (`∫(A+Bt)·q0MixEntry = A·M0 + B·M1` — the coupling-integrand form), `q0MixEntry_subZeroTail_mass` (the `g=1` windowing-loss moment). **Algebraic simplification:** `λᵢₖ − Rᵢₖ = −σᵢ` (independent of `k`), so M0/M1 are clean polynomials in `σᵢ`, `Q0`, `Qpp`.
  - **Assembly bridges PROVED std-3 (2026-08-11):** the windowed moment and `σ−r` truncation now reduce to the explicit support moments. `q0MixEntry_fullLine_mul` (`∫_ℝ q0MixEntry·g = ∫_{λ}^{R} q0MixEntry·g`, support argument, no integrability needed); **`q0MixEntry_boundedIntegral_split`** (for ANY upper bound `b ≥ Rᵢₖ`: `∫₀^b q0MixEntry·g = ∫_{λ}^{R} q0MixEntry·g − ∫_{Iic 0} q0MixEntry·g` — covers the windowed moment `b=σ` and the deep-core coupling truncation `b=σ−r≥R`, all rows; both RHS terms explicit via M0/M1/affine `−` sub-zero-tail); **`q0MixEntry_truncIntegral_poly`** (larger species `λ≤0`, shallow `b≤R`: `∫₀^b q0MixEntry·g = ∫₀^b (poly)·g`) and **`q0MixEntry_truncIntegral_poly_smallest`** (smallest species `0≤λ≤b≤R`: `∫₀^b q0MixEntry·g = ∫_λ^b (poly)·g`, the `[0,λ)` part null via an a.e.-at-endpoint argument). ⇒ **every windowed `∫₀^σ` moment and every `σ−r`-truncated coupling integral now reduces to explicit closed forms** (M0/M1/affine `−` sub-zero-tail, or a bare-poly interval), across **all species and all `r`-regimes** — the windowed-moment/truncation assembly is complete.
  - **The cubic's source, made explicit (2026-08-11, std-3):** `baxterQuadAffine_integral` — `∫ₐᵇ (A+B·t)·(Q0(t−R)+Qpp(t−R)²/2) = A·M0[a,b] + B·M1[a,b]` (a quadratic × linear = cubic). ⭐ **Corrected structural understanding:** the coupling's `r`-dependence is NOT just the linear affine coefficient `(r+t)(S0−1)−S1`; it is the **variable upper bound `b = σ−r`** of `∫₀^{σ−r}` — with `b=σ−r` the coupling is a **quartic in `r`**, giving `c_ij` its cubic form (consistent with the scalar `c_HS eta σ r = −(a0 + a1(r/σ) + a3(r/σ)³)`, `PYDCF.lean`). The "deep core" `σ−r ≥ R` (untruncated) is **empty at equal diameters** (`R=σ`) — so the truncation always bites, and `truncIntegral_poly`(`_smallest`) + `baxterQuadAffine_integral` are exactly what compute it. **⇒ every integral in the moment expression — leading term, windowed row moments, AND the `σ−r`-truncated coupling — is now an explicit closed form.**
  - **⭐⭐ GRAND ASSEMBLY LANDED — `matBaxterUQm = r·c_ij` (2026-08-11, std-3, build 8766).** Two theorems (`MixtureBaxterSeed.lean` + `MixtureHSDCFFin1.lean`):
    - **`matBaxterUQm_eq_rcMixMomentDCF`** (general-`N`, arbitrary `Ψ,Q`): `matBaxterUQm Ψ Q σ i j r = r · cMixMomentDCF Q σ i j r`, where **`cMixMomentDCF Q σ i j r`** := (the Wertheim–Thiele moment expression)/r on `(0,σ)`, `0` outside — the **explicit mixture DCF**. The general-`N` analog of the equal-diameter `matBaxterUQm_eq_rcHS_of_rowSum`, **without** the `hrow` row-collapse (composes `matBaxterUQm_eq_rPhi_of_momentSeed` with the tautological seed `moment_expr = r·(moment_expr/r)`).
    - **`matBaxterUQm_physMix_eq_rc`** (physical **binary, unequal diameters**): the above instantiated at `physMix` with the constructed renewal `physMixPsiouter` and the `q0MixPolyMat` kernel at `σ=max(σ₀,σ₁)` — `hUouter`=`physMix_hUouter`, `hint`=`physMix_hint`, `hQ0/hQ1`=kernel continuity, `hcore`=`matBaxterPsi_core`. **⇒ `matBaxterUQm[phys] = r·c_ij` is a proved theorem at a physical unequal-diameter mixture.**
  - **Collection lemmas PROVED std-3 (2026-08-11):** the physical assembly uses the `q0MixPolyMat` (clamped-poly) kernel, and `q0MixPoly = poly` for `t ≤ R` / `= 0` for `t ≥ R`, so every `∫₀^b q0MixPoly·(affine)` in `cMixMomentDCF` reduces to a **bare-polynomial** integral: `q0MixPoly_eq_poly_of_le`, `q0MixPoly_affineInt_le` (shallow `b ≤ R`: `= ∫₀^b poly·affine`), `q0MixPoly_affineInt_ge` (full `b ≥ R`: `= ∫₀^R poly·affine`) — then `baxterQuadAffine_integral` gives the explicit polynomial. The `b = R` split IS the `r = σ − Rᵢₖ` breakpoint. **⇒ every integral in `cMixMomentDCF` (physical) is now an explicit polynomial with the piecewise structure identified.**
    **Explicit row-moment constants** (std-3): `q0MixPoly_windowM0` (`∫₀^b q0MixPoly = −Q0ᵢₖRᵢₖ²/2 + QppₖRᵢₖ³/6`, `b≥R`) and `q0MixPoly_windowM1` (`∫₀^b t·q0MixPoly = −Q0ᵢₖRᵢₖ³/6 + QppₖRᵢₖ⁴/24`) — the `r`-independent constants in `cMixMomentDCF`'s leading term.
  - **⭐⭐ NUMERICAL FINDING (2026-08-11, `test_cMixMomentDCF_regularity.py`) — `cMixMomentDCF` is NOT a polynomial at unequal diameters; the polynomial `c_ij` needs the *extended* seed.** The regularity constant `Q_i = −S1ᵢ + ∑ₖS1ₖM0ᵢₖ − ∑ₖ(S0ₖ−1)M1ᵢₖ` (= `lim_{r→0} matBaxterUQm`; `Q_i = 0 ⟺ cMixMomentDCF` is a polynomial) was tested against the physical PY `Q0phys`/`Qppphys`, via the closed-form formula AND direct `r→0` numerical integration of the full seed (moments cross-checked to 1e-14):
    - **`Q_i = 0` EXACTLY at equal diameters** (both kernels) — consistent with `cMixMomentDCF = c_HS` (`baxter_core_seed`), a polynomial.
    - **`Q_i ≠ 0` at UNEQUAL diameters, for BOTH the `q0MixPolyMat` (clamped) AND the `q0MixEntry` (`[λ,R]`-restricted) kernels** (values from `~1` to `~10⁶`). So the WINDOWED-seed `cMixMomentDCF` has a genuine **`1/r` pole** at unequal diameters ⇒ **not a polynomial** — the kernel choice does not fix it.
  - **Interpretation:** `matBaxterUQm` is the *windowed* seed carrying a windowing loss at unequal diameters, so it does **not** vanish at `r→0` (`→ Q_i ≠ 0`); it is the wrong object for the physical DCF. The polynomial Lebowitz `c_ij` must come from the **loss-free EXTENDED seed `matBaxterUQmSymFullExt` (= `cHSmixRenewal`)** — which recovers the windowing loss (its core reduction `matBaxterUQmSymFullExt_core_reduce` uses `∫_λ^{σ−r}` and the transposed `matBaxterUtExt`, a genuinely different moment structure).
  - **⇒ "collect `cMixMomentDCF` into a piecewise polynomial" is refuted for the windowed seed** — it is a rational function, not a polynomial. The correct next direction is an extended-seed assembly `matBaxterUQmSymFullExt[phys] = r·c_ij`. `matBaxterUQm[phys] = r·cMixMomentDCF` stays a correct theorem (with `cMixMomentDCF` a rational DCF). MML.8 is discharged independently via `matOzStar_unique`.
  - **⭐⭐ Extended-seed `Q` CONFIRMED `= 0` — the loss-free seed is regular (2026-08-11, `volterra_extseed.py`).** The earlier deep-core (`Ψ=−v`) probe was inconclusive because the extended seed's `∫_ℝ` convolutions reach *outside* the core (`Ψ=Ψouter≠−v`). So I **numerically solved the matrix Volterra renewal** `Ψouter(r) = F(r) + ∫_σ^r Q(r−t)·Ψouter(t) dt` (`Q=q0MixPolyMat`, `F=matForcingCore`, exactly per `matBaxterPsiOuterFun_renewal`; `F` is `j`-independent ⇒ `Ψouter_ij = ψ_i(r)`), built the full `Ψ` (core `−v` + `ψ` on `[σ,∞)` + reflected), and evaluated `matBaxterUQmSymFullExt[Ψ, q0MixEntry]` (compact kernel ⇒ `∫_ℝ` converges):
    - **`N=1` validation:** `→ 0` as `r→0`, `seed/r → −14.55` (finite, converged) — regular, `= r·c_HS`.
    - **`N=2` equal & UNEQUAL diameters:** `matBaxterUQmSymFullExt → 0` as `r→0` for both rows, all tested `(ρ,σ)` (e.g. unequal: `−256→−84→−35` at `r: 0.20→0.06→0.02`). ⇒ **`Q_extended = lim_{r→0} matBaxterUQmSymFullExt = 0` at unequal diameters** — the extended seed IS regular, **unlike the windowed seed** (`Q ≠ 0`).
    - ⚠ **[SUPERSEDED — root cause found]** With the BARE kernel the small-`r` `seed/r` was erratic. This turned out to be TWO stacked problems, both now resolved below: (a) a wrong-object error (missing `ρ_k` weight), (b) a `Ψ`-kink quad artifact in the sub-zero tail.
  - **⭐⭐⭐ ROOT CAUSE + RESOLUTION — the physical seed needs `ρ_k`-WEIGHTED kernels; once weighted, `c_ij = seed/r` is REGULAR and matches analytic `c_HS` to `1.5e-9` (2026-08-11, `volterra_extseed.py`, now `ρ`-weighted).** The proved `cHSmixRenewal_eq_cHS_of_equalDiam` needs `hrow: ∑ₖ Qᵢₖ(u) = q0_poly(η,σ,ρ,u)` — the SCALAR one-component Baxter `q`, which carries an explicit `ρ` factor (`q0_poly = ρ·q'_py·φ₁ + ρ·q''_py·φ₂`, `BaxterRealSpace.lean`). But `Q0phys`/`Qppphys` are the `ρ`-STRIPPED coefficients, so `hrow` holds ONLY when each species-sum is weighted by the summed index's `ρ_k` — **verified EXACTLY**: `∑ₖ ρ_k·Q0phys_ik = q0_poly`, while bare `∑ₖ Q0phys_ik` (= `q0_poly`/⟨ρ⟩, off ~5×) AND `ρ_geo = √(ρᵢρ_k)` both FAIL at unequal `ρ` (equal-σ, `ρ=[.15,.25]`: only plain `ρ_k` matches). So the physical seed that equals `r·c_ij` uses `ρ_k`-weighted convolution kernels in BOTH the renewal (`F`, Volterra `Q`) AND the two seed convolutions. **The bare `q0MixEntry(physMix)` kernel in the current Lean `matBaxterUQmSymFullExt` def is off by `ρ`** — its `seed/r` is a differently-normalised object, NOT the physical Lebowitz `c_ij` (consistent with the docstring's "acknowledged open numerical residual"; the definitional `matBaxterUQm[phys] = r·cMixMomentDCF` stays true but `cMixMomentDCF ≠ Lebowitz`).
    - **VALIDATION:** `ρ_k`-weighted `seed/r` = analytic Wertheim–Thiele `c_HS` to **`1.5e-9`** at equal diameters — for equal AND unequal `ρ` at equal σ (both species → same `c_HS(η)`), pinning `ρ_k` (not `ρ_geo`).
    - **UNEQUAL-DIAMETER REGULARITY (the physical DCF):** the **smallest-species row** (`λ_0k ≥ 0`, no sub-zero tail, no `Ψ`-kink crossing) gives `c_ij = seed/r` **DEAD-FLAT to 5 digits** across `r=0.015..0.12` (e.g. `−11.181` ×4, `−75.216` ×4, `−14.660` ×4; `a₀ ~ 1e-3`) ⇒ **manifestly REGULAR, no `1/r` pole**. The larger-species row has a mild ~2–3% drift (`a₀` 0.4–2% of mag), traceable to the sub-zero-tail `Ψ`-kink quad — a residual numerical limitation, not a pole. ⇒ **`c_ij = seed/r` CONVERGES to a finite (Lebowitz-polynomial) value at unequal diameters.**
  - **⇒ the regular DCF lives in the EXTENDED seed with `ρ_k`-weighted kernels (confirmed to `1.5e-9`), not the windowed bare one.** The windowed-seed `cMixMomentDCF` has a `1/r` pole (wrong object); the extended `ρ`-weighted seed vanishes at `r→0` and reproduces `c_HS` exactly. The Lean assembly to target is `matBaxterUQmSymFullExt[phys] = r·c_ij` — and to make its `c_ij` the physical Lebowitz DCF the seed's convolution sums must carry `ρ_k` (either fold `ρ_k` into a weighted kernel `ρ_k·q0MixEntry`, or add `ρ_k` to the `∑ₖ` in the seed/renewal defs). MML.8 is discharged independently via `matOzStar_unique`, so this is a value-correctness item, not a blocker.
  - **⭐⭐⭐ `ρ_k`-WEIGHTING IMPLEMENTED IN LEAN (2026-08-11, `HSMixture/PhysHSMixRhoWeight.lean`, std-3, build 8767).** New Yukawa-free HS-layer file:
    - **`q0MixEntryW X i j := X.ρ j · q0MixEntry X i j`** — the `ρ_k`-weighted (column-scaled) Baxter kernel. Column-scaling suffices for the WINDOWED seed because `matBaxterUQm` only ever sums the **second** index (`Q i k`, summed `k`): `∑ₖ q0MixEntryWᵢₖ = ∑ₖ ρ_k·q0MixEntryᵢₖ`. (The transpose complication — inner `matBaxterUtExt` sums the first index — only hits the EXTENDED seed; that's the still-open unequal-diameter piece.)
    - **`Q0phys_equalDiam`/`Qppphys_equalDiam`** — the two scalar collapse identities: at equal σ (using `π·ξ₂·s = 6η`, so `2vac+3η = 2+η` and `vac+3η = 1+2η`), `Q0phys = q'_py η s` and `Qppphys = q''_py η`. Proof: substitute `ξ₂ = 6η/(πs)` (needs `s>0`) then `field_simp; ring`.
    - **`physHSMixN_rhoWeighted_rowSum`** ⭐ — the enabling `hrow`: `∑ₖ ρ_k·q0MixEntry(physHSMixN)ᵢₖ(u) = q0_poly η s (∑ₖρₖ) u` on `[0,s]`. This is the fact the BARE kernel fails (off by the missing density weight), now PROVED for `physHSMixN`.
    - **`matBaxterUQm_rhoWeighted_physHSMix_eq_rcHS`** ⭐⭐ — the (conditional) capstone: the `ρ_k`-weighted physical windowed seed `matBaxterUQm[Ψ, q0MixEntryW(physHSMixN)] = r·c_HS` at equal diameters, via `matBaxterUQm_eq_rcHS_of_rowSum` (which needs **no `hsym`**) + the row-sum; `heta_def` proved at equal σ; `hQ0`/`hQ1` from `q0MixEntry_intervalIntegrable(_mul_id).const_mul`. Renewal side-conditions `hUouter`/`hint` are hypotheses (as in `_eq_rcHS_of_equalDiam`).
  - **⭐⭐⭐ `hUouter`/`hint` DISCHARGED — the reduction is now UNCONDITIONAL (2026-08-11, `YukawaOZMix/PhysMixRhoWeightRenewal.lean`, std-3, build 8771).** New Yukawa-layer file that constructs the actual `ρ`-weighted renewal on the **continuous column-scaled poly kernel** `q0MixPolyMatW X r := (ρ_k·q0MixPoly X i k r)ᵢₖ` (the renewal/`matBaxterPsi_hUouter` need continuity — `q0MixEntry` is discontinuous; the two agree on `[0,σ]`, all `matBaxterUQm` samples, exactly as the codebase's `matBaxterUQm_physMix_eq_rc` uses `q0MixPolyMat` not `q0MixEntry`):
    - `q0MixPolyMatW`(+`_continuous`/`_eq_zero_of_ge`); `q0MixPoly_eq_q0MixEntry_core` (the two kernels coincide on `[0,s]` at equal diam ⇒ `physHSMixN_rhoWeighted_rowSum_poly` transports the row-sum); `physHSMixNPsiW` (the constructed glued renewal).
    - **`matBaxterUQm_physHSMixRhoWeight_eq_rcHS`** ⭐⭐⭐ — `matBaxterUQm[physHSMixNPsiW, q0MixPolyMatW(physHSMixN)] = r·c_HS` at equal diameters with **only physical-data hypotheses** (`hsig`/`hs`/`hs0`/`hvac`/`hetalt`). `hUouter` = `matBaxterPsi_hUouter` (support `q0MixPolyMatW_eq_zero_of_ge` + `matForcingCore` def), `hint` = `matBaxterPsi_hint`, `hrow` = `physHSMixN_rhoWeighted_rowSum_poly`, `hcore` = `matBaxterPsi_core`, `hQ0`/`hQ1` from `q0MixPoly` continuity via `.matrix_elem`.
  - **⭐⭐⭐ EXTENDED-seed TWO-KERNEL `ρ`-weighting DONE (2026-08-12, `YukawaOZMix/ExtendedSeedRhoWeight.lean`, std-3, build 8773).** The extended seed's `ρ`-weighting genuinely needs TWO kernels — the inner convolution is TRANSPOSED (`matBaxterUtExt` sums the FIRST index `Q m i` ⇒ weight `ρ_m`), the outer sums the SECOND (`Q i k` ⇒ `ρ_k`); a single column-scaling gives `ρ_i` in the inner (wrong). New file:
    - **`rowScaleK ρ Q a b := ρ_a·Q a b`** (inner) / **`colScaleK ρ Q a b := ρ_b·Q a b`** (outer); **`matBaxterUQmSymFullExtRho Ψ Q ρ := matBaxterUtExt Ψ (rowScaleK ρ Q) − ∑ₖ ∫_ℝ (colScaleK ρ Q)ᵢₖ·matBaxterUtExt Ψ (rowScaleK ρ Q)ₖⱼ(r+t)`** — the two-kernel def.
    - **`matBaxterUt_rowScale_eq_matBaxterU_colScale_of_symm`** ⭐ — the `ρ`-analog of `matBaxterUt_of_symm`: at symmetric `Q`, `matBaxterUt Ψ (rowScaleK ρ Q) = matBaxterU Ψ (colScaleK ρ Q)` (both carry `ρ_m`, `Q m i = Q i m` reconciles). This is the key that lets the two kernels collapse at equal σ.
    - **`matBaxterUQmSymFullExtRho_zero_outer`** — outer-vanishing at UNEQUAL σ (the `σ−λ` bound), mirroring the unweighted `_zero_outer`.
    - **`matBaxterUQmSymFullExtRho_eq_matBaxterUQm_colScale`** ⭐⭐ — the generic equal-diameter collapse to the `ρ`-weighted WINDOWED seed (symmetric supported `Q`): extended `∫_ℝ` → windowed `∫₀^σ` (support) + transpose bridge.
    - **`matBaxterUQmSymFullExtRho_physHSMix_eq_rcHS`** ⭐⭐⭐ — the capstone: the physical two-kernel `ρ`-weighted extended seed (compact `q0MixEntry` factor, constructed renewal `physHSMixNPsiW`) `= r·c_HS` at equal diameters, ONLY physical-data hyps. `hUouter`/`hint` bridged from `q0MixEntry` to the continuous `q0MixPolyMatW` renewal kernel via `matBaxterU_core_congr` (they agree on `[0,σ]`) + `IntervalIntegrable.congr_uIoo`; `q0MixEntry_symm_equalDiam` (`Q0phys` symmetric, `Qpp` collapses) + `q0MixEntry_supp_equalDiam`. All std-3. Numerically the full `ρ`-weighted pipeline reproduces `c_HS` to `1.5e-9` and is regular at unequal σ; the analytic unequal-σ Lebowitz value stays the (unweighted-too) open numerical residual.
  - **✅ EXTENDED-SEED UNEQUAL-σ VALUE = the largest-species COLUMN of the pair DCF — CONFIRMED (2026-08-15, `mixchs4_extseed_vs_cMixDCFN.py`).** The extended seed reproduces the physical pair Lebowitz DCF: **`matBaxterUQmSymFullExt[phys](i,·)/r = c_{i,k*}` with `k* = argmax σ` (the LARGEST species)**. Since the physical renewal is `Ψ_ij=ψ_i` (j-independent), the seed's row `i` equals the fixed largest-species column `c_{i,k*}` — a genuine pair DCF, and it AGREES with the value route's `cMixDCFN(i,k*)`. Numerics: CONTROL equal σ=1.0 `seed/r=c_HS=cMixDCFN` to `6e-10`; UNEQUAL σ=[1.0,0.7] & [1.0,0.85,0.7] (k*=0): the **smaller**-species rows `i≠k*` match `cMixDCFN(i,k*)` **EXACTLY** (`0.00%`), the **largest**-species diagonal `i=k*` matches `cMixDCFN(k*,k*)` within `~2–4%` (the known sub-zero-tail Ψ-kink SOLVER error, grows with r, M-converged). ⚠ **CORRECTION of an earlier same-session WRONG "REFUTED" claim**: it compared the larger-species seed row to `cHS_analytic(η_mix,r)` as the mixture DIAGONAL — but that is DOUBLY wrong (`cHS_analytic` is hardcoded for σ=1, using `r` not `r/σ`; AND the mixture diagonal `c_ii` is NOT `c_HS(η_mix)` but the Lebowitz expression with σᵢ and the MIXTURE moments). With the correct diagonal `c_{k*,k*}=−5.31..−5.40`, the seed row (`−5.42`) matches it (not the bogus `c_HS=−4.57`). The tell was the M-INDEPENDENCE of the σ≠1 "control failures" (σ=0.9: 6.6% at both M=6000/12000; σ=1.2: 88%) — reference errors, not solver errors. **⇒ the extended seed and the value route give the SAME physical DCF; MIXCHS.4-unequal (`= r·c_ij`, largest-species column) is ACHIEVABLE and consistent** (a redundant confirmation of the value route, which already covers ALL pairs via `shellForcing_eq_cMixDCFN`). The full unequal-σ Lean formalization of the extended seed remains a large (redundant) undertaking; the pair DCF itself is owned by MIXCHS.7-N.
  - **⛔ LEAN OBSTRUCTION for the extended-seed unequal-σ value — PRINCIPLED, not tedium (2026-08-15).** Pursued the Lean via `matBaxterUQmSymFullExt_eq_rPhi_of_seed`, which reduces to `hseed` (the moment identity on `(0,σ−λ)`). `hseed` needs `matBaxterUtExt` on the **transition regime `(σ+λ, σ)`**: `matBaxterUtExt_core` (the affine `r(∑M₀−1)−∑M₁` form) holds only for `r < σ+λ`, but with the physical `σ = σ_max` the seed's convolution samples `matBaxterUtExt(r+t)` for `t` up to `R_max`, requiring `r < σ+λ−R_max = λ_min < 0` — **never**. So the clean core-moment substitution that closes the WINDOWED seed (`matBaxterUQm_coreSeed_moment` → `matBaxterUQm_eq_rcMixMomentDCF`) genuinely fails; the transition regime is unavoidable. On it, `matBaxterUtExt` samples the **outer renewal `Ψouter`** (`physHSMixNPsiouter`), which is only *constructed* (Banach fixed-point) and has **NO finite closed form** — solving OZ for the correlation given `c` inverts `(I−ρc⋆)`, an **infinite Mittag-Leffler series** (the project's own RDF "inverse-appears-twice" finding, `[[project_rdf_benchmark]]`). So the finite seed value `r·c_{i,k*}` (confirmed) arises only as an **infinite-series cancellation**; formalizing it re-derives — redundantly, and via the infinite series — the DCF that MIXCHS.7-N already gets DIRECTLY via the derivative/shellForcing route (which never touches the series). **⇒ the extended-seed unequal-σ value is confirmed (numerically), and its finite-Lean formalization is obstructed by the infinite-series outer renewal + redundant with MIXCHS.7-N. RECOMMENDED STOP: MIXCHS.7-N (`shellForcing_eq_cMixDCFN`) is the sound, finite, complete pair-DCF result; the extended-seed equal-σ `= r·c_HS` stays proved.**
- **MIXCHS.5 ✅ DONE (marker cleared 2026-08-25) — the low-layer HS refactor is complete** (`HSMix.lean`, `PhysHSMixWindowing.lean`; the full-repo task-group/proof_notes reorg was DEFERRED per user, and is NOT part of MIXCHS.5). *(was: ◑ REFACTOR — design validated + foundation migrated, 2026-08-11, build 8764)*
  Pure-HS `HSMix` at the low layer + `Mix.toHSMix` `M=0` bridge (rfl).
  - **Done — leaf lemmas (11 lemmas, std-3).**
    - `HSMixture/HSMix.lean` — structure + `R`/`lam`/`q0MixEntry` + `q0MixEntry_support_subset`.
    - `HSMixture/HSMixBaxterFactor.lean` — the analytic side-conditions `q0MixEntry_measurable`, `_intervalIntegrable`, `_mul_id_intervalIntegrable`, `_abs_le`, `_mul_exp_integrable`.
    - `HSMixture/HSMixWindowing.lean` — the windowing-loss structure `q0MixEntry_subZeroTail_zero`, `_intSplit`, `_intIic_lam_eq_zero`, `_subZeroTail_compact`, `_inner_expand`, `_subZeroTail_poly`.
  - **Done — seed-coupled windowing bridges (2026-08-11, build 8765, std-3).**
    - `HSMixture/MixtureSeedExtended.lean` (new) — **extracted the generic Matrix seed machinery** that was stranded at the Yukawa layer: the defs `matBaxterUt`, `matBaxterUExt`, `matBaxterUtExt` and the algebraic bridges `matBaxterUt_eq_transpose`, `matBaxterUtExt_eq_transpose`, `matBaxterUExt_eq_matBaxterU_sub_tail` (all `Matrix`-generic, no HS/Yukawa content; namespace `FMSA.MixtureOzStar`, imports only Mathlib + `MixtureBaxterSeed`). `MixtureRowSum`/`MixtureCorrectedSeed` **dropped their copies and re-use these by import** (build stays green — the "3 Mix hits" in `MixtureCorrectedSeed` were docstrings, so it was already content-generic).
    - `HSMixture/HSMixWindowingBridge.lean` (new) — the **five HS-native windowing bridges** on `HSMix`: `matBaxterUExt_eq_matBaxterU_of_row_lam_nonneg`, `matBaxterUExt_eq_matBaxterU_sub_polyTail`, `matBaxterUtExt_eq_matBaxterUt_of_row`, `matBaxterUtExt_eq_matBaxterUt_sub_polyTail`, `windowing_loss_sum_eq_symTail` (the two-arm sum = symmetrized-kernel sub-zero tail). Weave the extracted generic machinery with `HSMixWindowing`'s structural facts — Yukawa-free.
    - The five Yukawa `Mix`-side bridges in `MixtureHSDCFFin1.lean` are now **thin wrappers** (`:= FMSA.HSMix.<bridge> X.toHSMix …`); the downstream physical instantiations (`physMix_windowing_sum` N=2, `physMixN_windowing_sum` N≥3) that consume them stay std-3.
  - All 16 Yukawa `Mix`-side copies delegate through `toHSMix` (`rfl`/defeq); the pure-HS layer is the single source of truth, the `Mix` layer bridges at `M=0`.
  - **Done — physical mixture + N=1 ODE (2026-08-11, build 8766, std-3).**
    - `HSMixture/PhysHSMix.lean` (new) — the **HS-layer physical mixtures** `physHSMixN` (general N) and `physHSMix` (Fin 2), built from the Lebowitz-PY `Q0phys`/`Qppphys` (both `HSMixture/MatrixQ0.lean`, already Yukawa-free). The Yukawa `physMixN`/`physMix : Mix N 0` are their `M=0` lifts: `physMixN_toHSMix`/`physMix_toHSMix` (`MixtureDCFAEInjective.lean`) are `rfl`. The Yukawa defs are left untouched (26 `simp only [physMixN/physMix, …]` sites depend on the literal), mirroring the coexisting-with-rfl-bridge pattern used for `q0MixEntry`.
    - **`baxterODE_n1` needed no migration** — it is the *scalar* HS Baxter ODE `FMSA.HardSphere.baxterODE_n1` (`HardSphere/BaxterForcingDeriv.lean`, imports Mathlib + HardSphere only), already **below** the HSMixture layer and Yukawa-free. MIXCHS.1 was always anchored there.
  - **Done — constructed renewal + physical windowing-sum theorems (2026-08-11, build 8766, std-3).**
    - **Extracted the last generic Matrix helpers** to `HSMixture/MixtureSeedExtended.lean`: `matForcingCore`(+cont) (was in `MixtureHSDCFFin1`) and `matBaxterPsi_entry_measurable`/`_bddOn` (was in `MixtureRowSum`) — both content-generic, stranded at the Yukawa layer by import only.
    - `HSMixture/PhysHSMixWindowing.lean` (new) — the **entire pure-HS physical windowing stack**: the renewal kernel `q0MixPoly`(+cont/`_eq_zero_of_ge`)/`q0MixPolyMat`(+cont), the integrability discharge `q0MixEntry_mul_integrable`/`q0MixEntry_matBaxterPsi_integrable`(`_gen`), the five hint-free `_of_cont` bridges, the **constructed renewal `physHSMixPsiouter`/`physHSMixNPsiouter`**(+cont) + geometry `physHSMix_R_le_max`/`physHSMixN_R_le`, and the capstone **windowing-sum theorems `physHSMix_windowing_sum`** (N=2, + `_windowing_smallest`/`_largest`/`_t_largest`/`_t_smallest`) **and `physHSMixN_windowing_sum`** (general N).
    - In `MixtureHSDCFFin1.lean` the **17 corresponding theorems are now thin `toHSMix` wrappers** (`:= FMSA.HSMix.<x> X.toHSMix …` / `… rho sigma hsig …`); the four defs `q0MixPoly`/`q0MixPolyMat`/`physMixPsiouter`/`physMixNPsiouter` are kept as-is but are **`rfl`-equal to their HS counterparts** (`physMixPsiouter = physHSMixPsiouter`, verified) — their literal simp-sites (`q0MixPoly_hUouter` unfolds `q0MixPolyMat`) stay intact.
  - **Done — deep `Mix` restructure: `Mix extends HSMix` (2026-08-11, build 8766, std-3).** `InnerDecomp.lean`'s `structure Mix (N M) where {σ,ρ,zp,cb,Q0,Qpp,hσ}` became **`structure Mix (N M) extends FMSA.HSMix N where {zp, cb}`** — so `HSMix` is now the genuine structural base and `Mix` adds *only* the `M` Yukawa-tail residue data. `Mix.toHSMix` is now the **auto-generated parent projection** (the manual `def … := ⟨X.σ,…⟩` in `WHSupports.lean` deleted). Remarkably contained: no positional `⟨⟩`/`Mix.mk` constructions existed, no code referenced `Mix.σ`/`Mix.Q0` as explicit projections (only docstring prose), and the two constructions (`physMix`/`physMixN`) use flattened `where` syntax that Lean 4 accepts for `extends`. **Every `X.σ`/`X.Q0`/… access is now inherited, all rfl bridges survive** (`q0MixEntry_eq_toHSMix`, `physMix_toHSMix`, `physMixN_toHSMix`, `(physMixN …).toHSMix = physHSMixN …` all still `rfl`), full build green with zero downstream edits.
  - **MIXCHS COMPLETE.** The entire stack — leaf lemmas, seed bridges, physical mixture, renewal, windowing-sum theorems — lives Yukawa-free at the HSMixture layer, and `Mix` now literally extends `HSMix`. The only thing not done is the (separate) full task-group + proof_notes layer reorg, deferred per user.
- **MIXCHS.6 ✅ VALUE ROUTE — MRS.8's open Baxter–WH gap `shellForcing = c_HS` CLOSED at equal diameters (2026-08-12, `YukawaOZMix/MixtureBaxterODEEqualDiam.lean`, std-3, build 8774).** The OZ★ value route (`matOzStar_unique` → `MatSymbolCoercive` → Gram → MRS.8) had been reduced, in `MixtureDCFAEInjective.lean`, all the way to **one** pointwise ODE `(matDCFreCore/ρ)'(s) = −2π·s·c_HS(s)` on `(0,σ)` (`shellForcing_eq_cHS_of_baxterODE`, with every regularity/boundary/integrability input already discharged: `hShellDCF_construct`, `matDCFreCore_contDiffOn_upper`, `matDCFreCore_boundary`, `hint_discharged`, `forcing_eq_shellForcing_of_hShellDCF`). The **scalar N=1 case** of that ODE was proved long ago (`baxterODE_n1`, MIXCHS.1). This file reduces the **mixture** ODE at equal diameters to the scalar one and closes it:
  - **The equal-diameter collapse.** At equal σ the physical Baxter entry `q0MixEntry(physMix)ₐᵦ` is INDEPENDENT of `(a,b)` — a single windowed quadratic `1[0,σ]·q0_poly(η,σ,1)` (`q0MixEntry_physMix_eq_indicator`, from `Q0phys_equalDiam`/`Qppphys_equalDiam`) — and the ρ-geo product `rgᵢₘ·rgₖₘ = ρₘ·rgᵢₖ` (`rhoGeoPhys_mul_eq`) collapses the full-line correlation `matCorrFull(qWeighted)ᵢₖ = rgᵢₖ·(∑ρ)·(Qbar ⋆ Qbar)` (`matCorrFull_qWeighted_equalDiam`). The convolution `Qbar ⋆ Qbar` reindexes to the scalar autocorrelation `∫₀^σ q0_poly(t+v)q0_poly(t)` of `baxterODE_n1` (`q0_poly_indicator_conv` — supports intersect in `[v,σ]`, shift to `[0,σ−v]`, tail `(σ−v,σ]` null since `q0_poly(t+v)=0` for `t+v≥σ`). ⇒ **`matDCFreCoreᵢₖ = (rgᵢₖ/∑ρ)·(baxterODE_n1's function at ρ=∑ρ)` on `(0,σ)`** (`matDCFreCore_equalDiam_eq`, via `q0_poly_eq_rho_smul`: `q0_poly η σ ρ = ρ·q0_poly η σ 1`).
  - **`matDCFreCore_hasDerivAt_equalDiam`** ⭐ — the **mixture Baxter ODE**: `matDCFreCoreᵢₖ'(x) = −2π·rgᵢₖ·x·c_HS(η,σ,x)` on `(0,σ)`, by scaling `baxterODE_n1` (`.const_mul (rgᵢₖ/∑ρ)` + `HasDerivAt.congr_of_eventuallyEq` on the open `Ioo`; `heta_def` at equal σ = the `= π·(∑ρ)·s³/6` identity).
  - **`shellForcing_eq_cHS_equalDiam`** ⭐⭐ — feeding the mixture ODE into `shellForcing_eq_cHS_of_baxterODE` with the **per-entry physical normalization `ρ = rgᵢₖ = √(ρᵢρₖ)`** gives `shellForcing rgᵢₖ i k v = c_HS(η,σ,v)` on `(0,σ)`, **UNCONDITIONALLY at equal diameters** (only physical-data hyps `hsig/hs/hvac/hrho/hρT>0/hetalt/rgᵢₖ≠0`). The physical mixture forcing is `Φᵢⱼ = (rgᵢⱼ/ρ)·c_HS = √(ρᵢρⱼ)/ρ·c_HS` (matching `MixtureCoercivityReduction.matDCF_coreNSD_equalDiam`). **⇒ the sole remaining open Baxter–WH input of MRS.8 is discharged at equal diameters, std-3.**
  - **Honest scope.** This closes the *analytic* gap (`shellForcing_eq_cHS_of_baxterODE`'s `hODE`) that the codebase flagged "research-scale / NOT a transcription" — at equal diameters (the benchmarked case; MML.8 stays independently discharged via `matOzStar_unique`). What is new: the once-open ODE is now a **proved theorem** at equal diameters. (The unequal-σ generalization is MIXCHS.7.)
- **MIXCHS.7 ◑ UNEQUAL-σ VALUE IS ACHIEVABLE — codebase "needs renewal" REFUTED; Lean NOT yet done (2026-08-13, `mixdcf_unequal_value_check.py`).** The decisive question for the unequal-σ value route: is the correct SYMMETRIC `matDCFreCore`-derivative `c_ij = −matDCFreCore_ij'/(2π·rg_ij·v)` the true PY-HS mixture DCF at unequal diameters? **YES**, verified decisively — so the equal-σ MIXCHS.6 proof GENERALIZES (no renewal `Ψ` needed).
  - **⚠ REFUTES the prior `MixtureHSDCFFin1.lean:110` note** ("q0-only form is 5–13% off, needs the renewal `Ψ`; unequal-σ value = open residual"). That was an artifact of an **ASYMMETRIC** naive formula (`2π r c_ij = −q0MixDeriv_ij + ∑ₗ ρₗ ∫ q0MixEntry_il(r'−r)·q0MixDeriv_lj`) which **drops the reflected linear term `q0(j,i)(−v)`** and mis-weights (it fails `c_ij = c_ji`). The correct object is the full **symmetric** `matDCFreCore = Re matDCFfull` (a.e.-symmetric, `matDCFreCore_ae_symm`) with BOTH linear terms `rg_ij q0(i,j)(v) + rg_ji q0(j,i)(−v)` and the full correlation. Docstring CORRECTED 2026-08-13.
  - **Decisive numerics** (`mixdcf_unequal_value_check.py`, σ={1,0.7}, ρ={.3,.25}; validated exact 3D-radial DST-I transforms — fwd+inv exact to 5 digits): (i) **equal-σ candidate-side control** reproduces analytic scalar `c_HS(η_tot)`; (ii) **hard-core test** `g_ij(r<σ_ij)=0` (the DEFINING property of the true PY-HS DCF — plug candidate into ONE OZ pass, check g inside core) passes to `<1.2%` = the equal-σ transform-noise baseline, while a **sensitivity sweep** shows a mere 5%-scaled `c` gives `g~0.03–0.05`, 10% gives `~0.07–0.10`. So the candidate is the exact DCF, not 5–13% off. Cross-checked against an independent Picard PY-HS mixture OZ solve (agrees to the solver's ~2% Gibbs floor, same ratio as the proven equal-σ case).
  - **⇒ the mixture Baxter ODE `matDCFreCore_ij'(s) = −2π·rg_ij·s·c_ij(s)` holds at unequal σ** with the physical **two-piece** Lebowitz `c_ij` — a knot at `λ_ij = (σ_j−σ_i)/2`: reflected quadratic `rg_ji q0(j,i)(−v)` active on `(0,λ_ij)`, forward `rg_ij q0(i,j)(v)` on `(λ_ij, R_ij)`, correlation throughout. No scalar collapse (unlike equal σ), so it needs the genuine mixture Baxter factorization.
  - **Lean path (NOT yet done, bounded, multi-step)** = two-piece analog of `baxterODE_n1`: (a) mixture correlation derivative — differentiate `matCorrFull_ik(v) = ∑ₘ ∫ q0(i,m)(u+v)q0(k,m)(u)du` under the integral (MA.16, analog of scalar `hasDerivAt_corrM`; build blocks `q0MixDeriv`/`hasDerivAt_q0MixEntry`, `MixtureHSDCFFin1`); (b) two-piece `matDCFreCore` derivative (upper piece already C^∞ via `matDCFreCore_contDiffOn_upper`/`qpConv_contDiffOn_upper`; lower via `qpConv_contDiffOn_lower`, both general σ); (c) define the two-piece Lebowitz `c_ij` + prove the per-piece Baxter factorization polynomial identity (`field_simp;ring` after substituting coeffs, like scalar `baxter_factorization_inner`); (d) knot-aware FTC for `shellForcing` across `λ_ij`.
  - **⭐ FOUNDATION STARTED (2026-08-13, `YukawaOZMix/MixtureBaxterODEUnequalDiam.lean`, std-3, build 8614):** `q0MixEntry_physMix_leftEdge` — the physical Baxter kernel at its left edge `λᵢₖ` equals `−π σᵢ σₖ / vac`, **manifestly symmetric in `i,k`** (`λᵢₖ−Rᵢₖ=−σᵢ`, the `πξ₂` cross terms cancel; `field_simp;ring`, a formal field identity — no `vac≠0` needed); `qWeighted_forward_eq_zero_of_lt_lam`/`_reflected_eq_zero_of_gt_lam` (support vanishing); **two-piece decomposition** `matDCFreCore_upper_eq` (`v>λ`: `= qWeighted i k v − matCorrFull`, reflected off) / `matDCFreCore_lower_eq` (`v<λ`: `= qWeighted k i(−v) − matCorrFull`, forward off).
  - **⭐ CORRELATION DERIVATIVE (2026-08-13, `YukawaOZMix/MixtureCorrelationDeriv.lean`, std-3, build 8776).** The self-conv `qpConv X i k j = qFwd(i,k)⋆pMixEntry(j,k)` (`matCorrFull=∑ₖ qpConv/(2π)²`) is reduced to explicit per-piece closed forms + differentiated. `qpConv_upper_intervalForm` (`(λᵢⱼ,Rᵢⱼ)`: `∫ₓ₊Lⱼₖ^Rᵢₖ (fwd quad)(pMix quad)`, moving lower limit — the `hclosed` step of `qpConv_contDiffOn_upper` lifted to a named lemma) + `qpConv_lower_intervalForm` (`(0,λᵢⱼ)`: whole window `∫_{λᵢₖ}^{Rᵢₖ}`, FIXED limits, no split). ⭐ **`hasDerivAt_qpConv_lower`** — the correlation derivative on the lower piece via the generic **`hasDerivAt_fixedConv_quadParam`** (pull the `x`-dependent pMix coefficients `D0 x,D1 x` out of the fixed-limit integral ⇒ `qpConv=D0 x·I0+D1 x·I1+d2·I2`, constant moments `Iₙ=∫Fwd·tⁿ`; only `D0,D1` differentiate): `(qpConv)'(x) = rrⱼ(−Qⱼ+Pₖ(x+Rⱼₖ))·∫Fwd + rrⱼ(−Pₖ)·∫Fwd·t` (`rrⱼ=2π√(ρⱼρₖ)`), NO boundary term — the unequal-σ analog of the scalar `hasDerivAt_corrM`. The UPPER-piece derivative (moving lower limit ⇒ +boundary term) is the remaining derivative piece; its closed form `qpConv_upper_intervalForm` is ready.
  - **⭐ LOWER-PIECE matDCFreCore DERIVATIVE ASSEMBLED (2026-08-13, `MixtureBaxterODEUnequalDiam.lean`, std-3, build 8777).** `qpConvLowerDeriv` (named def for the lower-piece qpConv derivative value) ⇒ **`matCorrFull_hasDerivAt_lower`** (`matCorrFull' = ∑ₗ qpConvLowerDeriv(physMix)ᵢₗₖ/(2π)²` via `matCorrFull_eq_qpConv` + `HasDerivAt.fun_sum` + `hasDerivAt_qpConv_lower`) ⇒ ⭐ **`matDCFreCore_hasDerivAt_lower`** — the **lower-piece mixture Baxter ODE LHS**: `matDCFreCore_ik'(v) = −rgₖᵢ·q0MixDeriv(physMix) k i(−v) − ∑ₗ qpConvLowerDeriv(physMix)ᵢₗₖ(v)/(2π)²` on `(0,λᵢₖ)` (reflected-quad chain rule via `hasDerivAt_q0MixEntry` − correlation sum; assembled through `matDCFreCore_lower_eq`). The remaining step is matching this to `−2π·rgᵢₖ·v·c_ij(v)` (the lower-piece Baxter factorization `ring` identity, needs the explicit Lebowitz `c_ij`), plus the UPPER piece (via `qpConv_upper_intervalForm`, moving-limit boundary term) + knot-aware FTC.
  - **⭐⭐ EXPLICIT LEBOWITZ c_ij DERIVED + TRIPLE-VERIFIED (2026-08-14, `mixdcf_lebowitz_cij.py`).** Computed symbolically from the proven `matDCFreCore` derivative via `c_ij(v) = −matDCFreCore_ij'(v)/(2π·rg_ij·v)` (rg=√(ρᵢρⱼ)); conventions ξ₂=∑ρσ², η=(π/6)∑ρσ³, D=vac=1−η. **Two-piece structure:**
    - **LOWER piece `(0,λ_ij)`: c_ij = an explicit CONSTANT** in D,ξ₂ (numerator of `−matDCFreCore'/(2πv)` is linear in v with v⁰ term vanishing via the ξ₂ identity ⇒ no 1/v pole; verified dead-constant −3.44165 to 4 digits).
    - **UPPER piece `(λ_ij,R_ij)`: c_ij = a + b/v + c₂·v + e·v³** (NO v² term). ⭐ the **`b/v` coefficient ∝ (σ_i−σ_j)²** — a genuine 1/r term at unequal σ that VANISHES at equal σ. So the natural POLYNOMIAL object is `r·c_ij` (deg-1 lower, deg-4 upper), not c_ij itself.
    - **Verified 3 ways:** (i) symbolic coeffs == numerical fit of the candidate to 1e-10; (ii) candidate passes the hard-core OZ test `g_ij(r<σ)=0` (`mixdcf_unequal_value_check.py`); (iii) at equal σ BOTH pieces reduce EXACTLY to the scalar PY `c_HS=−(a0+a1(v/σ)+a3(v/σ)³)` (upper: a=−a0, c₂=−a1/σ, e=−a3/σ³, b→0). **⇒ the value ODE RHS `−2π·rg·v·c_ij` is now a known explicit polynomial** (per piece) — the lower-piece Baxter factorization is a `ring` identity vs `matDCFreCore_hasDerivAt_lower` (moments evaluated); the upper piece needs the moving-limit derivative first.
  - **⭐⭐⭐ LOWER-PIECE VALUE CAPSTONE — UNCONDITIONAL (2026-08-14, `MixtureBaxterODEUnequalDiam.lean`, std-3, build 8777).** `cLowerPiece rho sigma i k` = the explicit constant lower-piece DCF (the derived closed form). ⭐ **`matDCFreCore_hasDerivAt_lower_cLower`** — the lower-piece mixture Baxter ODE DISCHARGED: `matDCFreCore'(v) = −2π·rgᵢₖ·v·cLowerPiece` on `(0,λᵢₖ)`. Proof: `matDCFreCore_hasDerivAt_lower.congr_deriv`, evaluate the 4 moment integrals (`intervalIntegral_quad`/`_mul_id`, new FTC helpers), then `fin_cases i <;> fin_cases k <;> simp[…Fin.reduceFinMk…] <;> field_simp <;> ring` — the `fin_cases` identifies the pair (i,k) with the species sum {0,1} and collapses the diagonal `√(ρₗρₗ)=ρₗ` (via `Real.sqrt_mul_self`, `sqrt_rho_prod`) leaving a single `√(ρ₀ρ₁)`; the sqrt-free polynomial identity (with the ξ₂ v⁰ cancellation, vacMix kept opaque so `hvac` clears its powers) closes by `ring`. ⚠ key plumbing: `Fin.reduceFinMk` normalizes fin_cases's `⟨0,⋯⟩→0`. ⭐⭐ **`shellForcing_lower_eq`** (UNCONDITIONAL): `shellForcing rgᵢₖ i k v = cLowerPiece` on `(0,λᵢₖ)` — the OZ★ shell-inverse forcing IS the explicit constant physical DCF (from the ODE via `shellForcing = −matDCFreCore'/(rg·2πv)`, no shell integral). **⇒ the FIRST piece of the unequal-σ value route is CLOSED, std-3.** Remaining: the UPPER piece (needs the moving-limit `qpConv_upper_intervalForm` derivative + the `a+b/v+c₂v+ev³` upper c_ij) + knot-aware FTC.
  - **⭐⭐⭐ UPPER-PIECE VALUE CAPSTONE — DONE (2026-08-14, `MixtureBaxterODEUnequalDiam.lean`, std-3, build 8777).** ⇒ **BOTH pieces of the unequal-σ mixture DCF value are now CLOSED.** For the ordered pair `σᵢ<σₖ`, on `(λᵢₖ,Rᵢₖ)`: **`shellForcing_upper_eq`** = `shellForcing rgᵢₖ i k v = cUpperPiece = a+b/v+c₂v+e·v³` (the explicit upper Lebowitz DCF, coefficients `cUpperANum/BNum/C2Num/ENum` generated from `mixdcf_lebowitz_cij.py`). Via the **integral route** (avoids the moving-limit Leibniz): ⭐ **`matDCFreCore_upper_shellForm`** — the value identity `matDCFreCore = 2π·rgᵢₖ·shellUpperAnti` (=`2π·rg·∫_v^R s·c_upper`), a degree-5 v-polynomial identity via `matDCFreCore_upper_eq`+`matCorrFull_eq_qpConv`+`qpConv_upper_intervalForm`+`integral_quad_mul_quad`, then `fin_cases`+√-collapse+`field_simp;ring` (`maxHeartbeats 4000000`, `vacMix` opaque); **off-diagonal ONLY** (`hlt:σᵢ<σₖ` — the species sum needs BOTH diameters, so the diagonal-`(i,i)` coefficients would drop species-1; diagonal cases discharged by `absurd hlt`). `hasDerivAt_shellUpperAnti` (clean poly deriv, `Rᵢₖ` boundary constant) ⇒ **`matDCFreCore_hasDerivAt_upper_cUpper`** (the upper Baxter ODE `matDCFreCore'=−2π·rg·v·cUpperPiece`, the `v·(b/v)=b` via `field_simp`) ⇒ `shellForcing_upper_eq` via `shellForcing_eq_of_baxterODE`.
  - **⭐⭐⭐⭐ KNOT GLUING — DONE ⇒ MIXCHS.7 FULLY CLOSED END-TO-END (2026-08-14, std-3, build 8777).** The two pieces glue into the complete unequal-σ mixture DCF value. (1) **`cLowerPiece_eq_cUpperPiece_knot`** — the DCF is **C⁰ across the knot**: `cLowerPiece = cUpperPiece(λᵢₖ)` (lower constant = upper cubic AT `λᵢₖ=(σₖ−σᵢ)/2`), an explicit sqrt-free polynomial identity `field_simp; ring` (verified `= 0` symbolically first, `check_knot_exact.py`; the C¹ break is the physical kink). ⚠ `field_simp` needs `σₖ−σᵢ ≠ 0` in THAT form (`sub_ne_zero_of_ne (ne_of_lt hlt).symm`) — `(σₖ−σᵢ)/2 ≠ 0` alone leaves `(−σᵢ+σₖ)⁻¹` uncleared. (2) **`cMixDCF`** = the two-piece function `if v<λᵢₖ then cLowerPiece else cUpperPiece v` (C⁰ by (1)). (3) **`shellForcing_eq_cMixDCF`** — the CAPSTONE: `shellForcing rgᵢₖ i k v = cMixDCF v` on all of `(0,Rᵢₖ)∖{λᵢₖ}`, by `rcases lt_or_gt_of_ne hne` + the two piece-capstones. ⇒ the OZ★ shell-inverse forcing IS the explicit two-piece Lebowitz PY-HS mixture DCF at unequal diameters, everywhere on the core off the single knot point. **This completes the unequal-σ value route** (equal σ = MIXCHS.6); combined they discharge MRS.8's Baxter–WH `hODE` for the physical two-piece DCF at BOTH equal and unequal σ. Only the knot point `v=λᵢₖ` itself is excluded (matDCFreCore's non-smoothness there — a measure-zero point, irrelevant to the a.e. shell identity).
  - **⭐⭐⭐⭐⭐ MIXCHS.7-N — GENERAL-`N` unequal-σ value route CLOSED on BOTH pieces (2026-08-14, `MixtureBaxterODEUnequalDiamN.lean` + `MixtureCorrelationDerivUpper.lean`, std-3, full build 8779).** The binary (`Fin 2`) route collapsed the species sum via `fin_cases i <;> fin_cases k` + hard-coded pre-summed coefficients; the general-`N` route keeps `∑ₗ` **symbolic**. ⭐⭐ **CAPSTONE `shellForcing_eq_cMixDCFN`**: for `σᵢ<σₖ`, on all of `(0,Rᵢₖ)∖{λᵢₖ}`, `shellForcingN rgᵢₖ i k v = cMixDCFN v` — the OZ★ shell-inverse IS the explicit `N`-species-summed two-piece Lebowitz DCF (`cMixDCFN` = `cLowerPieceN` on `(0,λ)`, `cUpperPieceN` on `[λ,R)`; each `= cLead*PieceN + ∑ₗ c*CorrPieceN(i,l,k)`). **Foundations** (`physMixN` reused general-`N`): `qWeightedN`/`matDCFreCoreN` (structural `Re(matDCFfull)`), `matCorrFull_eq_qpConvN`, `matDCFreCoreN_upper/lower_eq` (all the `N=2` proofs verbatim — purely structural). ⭐⭐⭐ **KEY NEW INFRASTRUCTURE**: (a) **`hasDerivAt_movingConv_quadParam`** — the moving-lower-limit Leibniz (`intervalIntegral.integral_hasStrictDerivAt_left` `∘` the affine shift + FTC boundary term), giving **`qpConvUpperDeriv`/`hasDerivAt_qpConv_upper`** (the upper piece has a MOVING limit `x+λⱼₖ`, unlike the fixed-limit lower); (b) **the general-`N` `√`-collapse, NO `fin_cases`**: `qpConvUpperDeriv = (2π)²·(√(ρᵢρₗ)·√(ρₖρₗ))·Il'(v)` with the `√` product a single subterm, which **`rhoGeoPhys_mul_eq`** collapses to `ρₗ·√(ρᵢρₖ)` for EVERY `l` (`qpConvUpperDeriv_per_l`; residue √-free `field_simp;ring`). **Coefficient decomposition** (`genN_coeffs.py`/`genN_lower.py`, validated == binary): the `(i,k)` LEADING term → only `A,B`; each species `l` → `c*CorrANumN/BNumN/…(i,l,k)` **each LINEAR in `ρₗ`** (vs the binary `ρ²`-expanded form). ODEs `matDCFreCoreN_hasDerivAt_upper/lower_cUpper/cLower` assemble the structural derivative + leading identity + per-species identity under the sum (`Finset.sum_congr`+`Finset.mul_sum`+`ring`). ⭐⭐ **KNOT CONTINUITY — DONE (2026-08-14, was thought a hard `∑ₗ` `ξ₁/ξ₃` identity, turned out to close via `xi2` ALONE)**: `cLowerPiece_eq_cUpperPiece_knotN` proves `cLowerPieceN(λ)=cUpperPieceN(λ)` (σᵢ<σₖ) ⇒ `cMixDCFN` is C⁰. **Per-species continuity IS false** (the per-species C¹ breaks don't individually match), BUT the per-species defect `cCorrPieceN(l)(λ)−cLowerCorrPieceN(l)(λ) = cKnotSlopeN·(ρₗ·σₗ²)` is **purely `σₗ²`** — the `σₗ⁰,σₗ¹` parts cancel (verified: M0/M1/M4/M5 coefficients all vanish, `genN_knot_moments.py`; N=3 continuity 1e-15). So `∑ₗ` = `cKnotSlopeN·∑ₗρₗσₗ² = cKnotSlopeN·xi2` — **only ξ₂=xi2 needed, NOT ξ₀/ξ₁/ξ₃/ξ₄** (the earlier "needs ξ₁/ξ₃" guess was WRONG). `cKnotSlopeN = π·σᵢσₖ/(vac²(σᵢ−σₖ))`; leading defect `= −cKnotSlopeN·xi2` cancels. Lemmas `cCorrPieceN_sub_knot`/`cLeadPieceN_sub_knot` (√-free `field_simp;ring`) + assemble (`Finset.sum_sub_distrib`+`Finset.mul_sum`+`xi2` def+`linarith`). ⇒ MRS.8's Baxter–WH `hODE` FULLY discharged (both pieces + knot) at equal σ (MIXCHS.6), binary unequal σ (MIXCHS.7), and **`N`-ary unequal σ** (MIXCHS.7-N).
  - **◑ UPPER-PIECE VALUE — tool + plan (2026-08-14, superseded above by the capstone).** ⭐ **`shellForcing_eq_of_baxterODE`** — generalized the lower conditional capstone to ANY forcing value `c`: given `matDCFreCore'(v)=−2π·rgᵢₖ·v·c`, `shellForcing rgᵢₖ i k v = c` (`shellForcing=−matDCFreCore'/(rg·2πv)`, pointwise via `deriv`). Both pieces plug in; `shellForcing_lower_eq_of_baxterODE` is now its specialization. std-3, build 8777. **Value identity VERIFIED** (`verify_upper_integral.py`): on the upper piece `matDCFreCore_ik(v)/rg = 2π∫_v^{Rᵢₖ} s·c_upper(s)ds` to `1e-6`, and `matDCFreCore(Rᵢₖ)=0`. ⇒ **integral route** (avoids the moving-limit Leibniz): (1) `matDCFreCore_upper(v) = 2π·rg·(∫_v^R s·c_upper)` — a degree-5 `v`-polynomial identity via `qpConv_upper_intervalForm`+`integral_quad_mul_quad`+`fin_cases`+√+`ring` (the big remaining discharge, RHS uses the explicit `a,b,c₂,e` upper coefficients); (2) differentiate the clean antiderivative ⇒ `matDCFreCore'=−2π·rg·(a v+b+c₂v²+e v⁴)`; (3) `shellForcing_eq_of_baxterODE` ⇒ `shellForcing = a+b/v+c₂v+e v³ = cUpperPiece`. The `b` (constant of `−2πrg·v·c_upper`) is the shell image of c_upper's `b/v`. Remaining: the degree-5 discharge ring (+ knot-aware FTC gluing the two pieces on the continuous representative).
  - **⚠ Point-spike at the knot (discovered 2026-08-13).** With the closed-`Icc` indicator convention BOTH linear terms are "on" at the single point `v=λᵢₖ` (`λᵢₖ`=left edge of `[λᵢₖ,Rᵢₖ]` AND, via `−λᵢₖ=λₖᵢ`, left edge of `qWeighted k i`'s support), so `matDCFreCore λᵢₖ = 2·rgᵢₖ·leftEdge − matCorrFull` while the two-sided LIMIT is `rgᵢₖ·leftEdge − matCorrFull` (jumps otherwise cancel by the leftEdge symmetry). Removable measure-zero spike — irrelevant to Laplace/shell integrals + all a.e. statements, but ⇒ the knot-aware FTC / `hShellDCF_construct` at unequal σ must run on the **continuous representative** of `matDCFreCore`, not the verbatim def (the residual care for value-capstone steps c,d).
- **DEFERRED.** A full task-group + proof_notes layer reorg (carving every HS-vs-Yukawa boundary across all files) is postponed per user (2026-08-11); this file + the `HSMixture/` layer are the corrected home for the pure-HS-mixture content in the meantime.
